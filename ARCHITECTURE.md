# Architecture

## Tech stack

- **Next.js 16.2.6** App Router
- **React 19.2.4**
- **Supabase SSR** (`@supabase/ssr ^0.10.3`, `@supabase/supabase-js ^2.105.4`) — auth + database
- **Tailwind 4**
- **Vitest 4** — unit and integration tests

## Multi-tenant structure

Barn slug is the org boundary. All barn-scoped routes live under `/barn/[slug]`. Every domain table carries `barn_id` as a FK to `barns(id)`. Composite FK constraints (`barn_id, id`) on junction tables (`lesson_horses`, `lesson_riders`) enforce cross-barn integrity at the DB level — a horse or rider must belong to the same barn as the lesson.

## Role system

Three roles: `manager`, `trainer`, `rider`.

**Glossary:** a **boarder** is a rider who boards their horse at the stable.

### Permissions matrix

| Table | manager | trainer | rider |
|---|---|---|---|
| barns | SELECT, UPDATE (own barn) | SELECT | SELECT |
| barn_memberships | SELECT own + barn; INSERT/UPDATE/DELETE own; UPDATE approve pending in barn; UPDATE `can_instruct` for barn members; DELETE any in barn | SELECT/INSERT/UPDATE/DELETE own | SELECT/INSERT/UPDATE/DELETE own |
| horses | SELECT, INSERT, UPDATE, DELETE | SELECT | SELECT |
| lessons | SELECT, INSERT, UPDATE, DELETE | SELECT, INSERT, UPDATE own (any column; instructor_id locked by RLS) | SELECT (enrolled only), INSERT |
| lesson_horses | SELECT, INSERT, UPDATE, DELETE | SELECT, INSERT, UPDATE, DELETE own | SELECT (enrolled only), INSERT |
| lesson_riders | SELECT, INSERT, UPDATE, DELETE | SELECT, INSERT, UPDATE, DELETE own | SELECT (enrolled only), INSERT |
| lesson_tiers | SELECT, INSERT, UPDATE, DELETE (barn-scoped) | SELECT (barn-scoped) | — |
| lesson_series | SELECT, INSERT, UPDATE, DELETE (barn-scoped) | SELECT, INSERT, UPDATE own (`instructor_id` locked to caller's own membership) | — |
| profiles | SELECT own + barn members; UPDATE own + any barn member's managed/stub profile only (contact fields only); INSERT own | SELECT own + barn members | SELECT own + barn members; INSERT/UPDATE own |
| notifications | SELECT/UPDATE/DELETE own; INSERT any authenticated (cross-user UPDATE/INSERT also reachable via `create_or_update_notification` RPC, gated on active membership in the target barn) | SELECT/UPDATE/DELETE own; INSERT any authenticated (see manager column) | SELECT/UPDATE/DELETE own; INSERT any authenticated (see manager column) |
| horse_documents | SELECT, INSERT, UPDATE, DELETE | SELECT, INSERT (barn-scoped) | — |
| trainer_documents | SELECT, INSERT, UPDATE, DELETE | SELECT, INSERT, DELETE own rows only | — |
| rider_documents | SELECT, INSERT, UPDATE, DELETE | SELECT (barn-scoped) | SELECT, INSERT, DELETE own rows only |
| agreements | SELECT, INSERT, UPDATE, DELETE (barn-scoped, both kinds) | — | SELECT own rows only (both kinds) |
| agreement_charges | SELECT, INSERT, UPDATE, DELETE (barn-scoped, both kinds) | — | SELECT own rows only (via parent agreement, both kinds) |
| horse_expenses | SELECT, INSERT, UPDATE, DELETE (barn-scoped) | — | — |
| expense_horses | SELECT, INSERT, UPDATE, DELETE (barn-scoped) | — | — |

## DB schema

All tables are in the `public` schema with RLS enabled.

| Table | Key columns | Notes |
|---|---|---|
| `roles` | `name TEXT PK CHECK IN ('manager','trainer','rider')` | Lookup table |
| `barns` | `id UUID PK`, `name TEXT NOT NULL`, `slug TEXT UNIQUE NOT NULL`, `default_board_fee NUMERIC NOT NULL DEFAULT 1000`, `instructor_cut NUMERIC NOT NULL DEFAULT 25 CHECK (instructor_cut >= 0)`, `exhaustion_threshold_high INT NOT NULL DEFAULT 11 CHECK (>= 0)`, `exhaustion_threshold_moderate INT NOT NULL DEFAULT 5 CHECK (>= 0)` (table-level `CHECK (exhaustion_threshold_moderate < exhaustion_threshold_high)`), `created_at TIMESTAMPTZ` | `default_board_fee` seeds the fee suggested on new boarding agreements; non-retroactive to existing agreements. `instructor_cut` is the flat per-lesson instructor take, configurable in Manage Barn settings; computed live wherever finance figures are read, so changing it retroactively affects all historical collected/pending income the next time those figures are computed; netted out of every lesson-derived finance figure (not agreement charges) by `lesson-finances.ts`. `exhaustion_threshold_high`/`exhaustion_threshold_moderate` are the barn-level default exertion-sum thresholds for the projected horse exhaustion feature; used whenever a horse has no per-horse override (see `horses`) |
| `barn_memberships` | `id UUID PK`, `user_id UUID→auth.users` (nullable — null for managed stubs), `barn_id UUID NOT NULL→barns`, `profile_id UUID NOT NULL→profiles`, `role TEXT→roles`, `status TEXT CHECK('active','pending') DEFAULT 'pending'`, `can_instruct BOOLEAN NOT NULL DEFAULT false`, `invite_token UUID` (nullable, partial unique index when not null), `created_at TIMESTAMPTZ`; `UNIQUE(user_id, barn_id)` (partial, where user_id IS NOT NULL); `UNIQUE(barn_id, id)` | `can_instruct` is set to `true` for trainer role on activation. The composite `UNIQUE(barn_id, id)` is used as the FK target from `lesson_riders(barn_id, rider_id)`. `profile_id` is the direct join key to `profiles` (replaces the two-hop `user_id → auth.users → profiles`). Managed rider stubs have `user_id = NULL`, `invite_token` set; claiming sets both fields and clears the token. |
| `horses` | `id UUID PK`, `barn_id UUID NOT NULL→barns`, `name TEXT NOT NULL`, `is_active BOOLEAN NOT NULL DEFAULT true`, `is_available BOOLEAN NOT NULL DEFAULT true`, `unavailability_reason TEXT`, `deactivated_at TIMESTAMPTZ` (nullable), `exhaustion_threshold_high INT` (nullable, `CHECK (>= 0)`), `exhaustion_threshold_moderate INT` (nullable, `CHECK (>= 0)`; table-level `CHECK` requires `moderate < high` whenever both are set), `created_at/updated_at TIMESTAMPTZ`; `UNIQUE(barn_id, id)` | `is_active=false` soft-deletes a horse; `is_available=false` temporarily marks a horse out of rotation — still returned by `getHorsesByBarn`, shown as disabled in lesson forms; management queries filter to `is_active=true`; lesson history resolves horse names regardless; `deactivated_at` is set by `updateHorseDetails` when `is_active` transitions to `false` and cleared on reactivation — future barn-wide expense splitting will use it to determine which horses were active during a given period; `exhaustion_threshold_high`/`exhaustion_threshold_moderate` are per-horse overrides for the projected horse exhaustion feature — `NULL` means fall back to the barn's default (see `barns`) |

| `lessons` | `id UUID PK`, `barn_id UUID NOT NULL→barns`, `instructor_id UUID→barn_memberships(barn_id,id)`, `fee NUMERIC NOT NULL`, `lesson_at TIMESTAMPTZ NOT NULL`, `submitted_at TIMESTAMPTZ`, `lesson_type lesson_type NOT NULL DEFAULT 'normal'`, `jumping BOOLEAN NOT NULL DEFAULT false`, `payment_type payment_type_enum`, `tier_name TEXT NOT NULL DEFAULT 'Custom'`, `cancelled_at TIMESTAMPTZ`, `cancellation_notes TEXT`, `series_id UUID` (nullable, `FK(barn_id, series_id)→lesson_series(barn_id, id)`); `UNIQUE(barn_id, id)` | `lesson_type` enum: `'normal'` or `'group'`; `payment_type` enum: `'venmo'`,`'zelle'`,`'cash'`,`'check'`,`'freshbooks'`; NULL = unpaid; `cancelled_at IS NOT NULL` marks a soft-cancelled lesson — cancellation always sets `fee=0` and `payment_type=NULL`, so cancelled lessons drop out of `getOutstandingLessons` and the income-summary queries with no extra filtering; `instructor_id` is a `barn_memberships.id` (not an `auth.users.id`), mirroring `lesson_riders.rider_id` — this lets a managed (stub, `user_id IS NULL`) trainer be set as instructor; `ON DELETE SET NULL (instructor_id)` blanks only the instructor when their membership is removed, preserving the lesson/fee history; `series_id` is set only on a recurring lesson's first occurrence, created atomically alongside its `lesson_series` template row — `NULL` for all one-off lessons |
| `lesson_tiers` | `id UUID PK`, `barn_id UUID NOT NULL→barns`, `name TEXT NOT NULL`, `price NUMERIC(10,2) NOT NULL`, `is_default BOOLEAN NOT NULL DEFAULT false`, `is_active BOOLEAN NOT NULL DEFAULT true`, `default_exertion_level SMALLINT CHECK(1–5)` (nullable), `default_jumping BOOLEAN` (nullable), `created_at TIMESTAMPTZ`; `UNIQUE(barn_id, id)` | Barn-scoped fee tiers; `is_default` marks the tier pre-selected on new lessons; `is_active=false` soft-deletes a tier; `default_exertion_level`/`default_jumping` are optional defaults for the lesson form (NULL = no default) |
| `lesson_series` | `id UUID PK`, `barn_id UUID NOT NULL→barns`, `instructor_id UUID→barn_memberships(barn_id,id)`, `fee NUMERIC NOT NULL`, `lesson_type lesson_type NOT NULL DEFAULT 'normal'`, `jumping BOOLEAN NOT NULL DEFAULT false`, `tier_name TEXT NOT NULL DEFAULT 'Custom'`, `horse_ids UUID[] NOT NULL`, `exertion_levels SMALLINT[] NOT NULL`, `rider_ids UUID[] NOT NULL`, `is_active BOOLEAN NOT NULL DEFAULT true`, `created_at TIMESTAMPTZ`; `UNIQUE(barn_id, id)` | Template for a "Recurring (weekly)" lesson, created atomically with its first `lessons` row via `create_lesson_series_with_participants`. Stores the recurring shape as arrays rather than via `lesson_horses`/`lesson_riders` junction rows, since it's a template, not an actual lesson. No cadence/day-of-week column — cadence is implicitly weekly, and the anchor time is whatever `lesson_at` the first lesson used; a future nightly-generation feature derives the next occurrence from the most recent `lessons.lesson_at` in the series + 7 days. `is_active=false` will stop future generation (set by a future "stop series" feature — not yet wired to any route). `instructor_id` mirrors `lessons.instructor_id`'s composite FK and `ON DELETE SET NULL (instructor_id)` semantics, so removing a trainer's membership blanks the series' instructor rather than failing |
| `lesson_horses` | `id UUID PK`, `barn_id UUID NOT NULL→barns`, `lesson_id UUID NOT NULL`, `horse_id UUID NOT NULL`, `exertion_level SMALLINT DEFAULT 3 CHECK(1–5)`, `horse_notes TEXT`; `UNIQUE(lesson_id, horse_id)`; `FK(barn_id, lesson_id)→lessons`; `FK(barn_id, horse_id)→horses` | Trigger `lesson_horses_participant_count_check` enforces per-type counts (deferred) |
| `lesson_riders` | `id UUID PK`, `barn_id UUID NOT NULL→barns`, `lesson_id UUID NOT NULL`, `rider_id UUID NOT NULL`, `rider_notes TEXT`, `private_notes TEXT`, `cancelled_at TIMESTAMPTZ`; `UNIQUE(lesson_id, rider_id)`; `FK(barn_id, lesson_id)→lessons`; `FK(barn_id, rider_id)→barn_memberships(barn_id, id)` | `UNIQUE(lesson_id)` dropped; trigger `lesson_riders_participant_count_check` enforces normal=1 rider+1 horse, group=≥2 riders (deferred); `private_notes` visible to trainer/manager only (DAL-layer column projection); `cancelled_at IS NOT NULL` marks a soft-cancelled participation, independent of the whole-lesson `lessons.cancelled_at` — riders have no RLS `UPDATE` grant on this table, so cancellation is written exclusively through the `cancel_rider_participation` RPC |
| `profiles` | `id UUID PK`, `user_id UUID UNIQUE→auth.users` (nullable — null until first sign-in or for managed stubs), `email TEXT` (nullable — null for managed stubs; partial unique index when not null), `first_name TEXT NOT NULL`, `last_name TEXT NOT NULL`, `phone TEXT` (nullable), `emergency_contact_name TEXT` (nullable), `emergency_contact_phone TEXT` (nullable), `is_managed BOOLEAN NOT NULL DEFAULT false`, `created_at TIMESTAMPTZ` | User-level (not barn-scoped). Created by `upsertProfile` on first sign-in or registration, or as a stub by `createManagedMember`. `is_managed=true` marks stub profiles; `claim_managed_member` RPC sets `is_managed=false` when a real user claims the membership; a manager's RLS `UPDATE` grant on another member's profile is restricted to `is_managed=true` rows — once claimed, only the member themselves can edit their own contact fields. |
| `notifications` | `id UUID PK`, `user_id UUID NOT NULL→auth.users`, `barn_id UUID NOT NULL→barns`, `type TEXT NOT NULL`, `title TEXT NOT NULL`, `body TEXT`, `link TEXT`, `read_at TIMESTAMPTZ`, `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`; `UNIQUE(user_id, barn_id, type)` | In-app alert system. Types for release-2: `outstanding_payment`, `pending_approval`, `lesson_cancelled`, `incomplete_profile`, `member_incomplete_profile`. Release-3 adds `rider_participation_cancelled` — kept distinct from `lesson_cancelled` so a whole-lesson cancellation and a per-rider cancellation for a different lesson don't collide on the `(user_id, barn_id, type)` upsert key and silently overwrite each other. Upsert on `(user_id, barn_id, type)` prevents duplicates and resets `read_at = NULL` on conflict. `outstanding_payment` is populated by a nightly cron (`scripts/generate-outstanding-notifications.sh`/`.ts`, `.github/workflows/generate-outstanding-notifications.yml`) rather than at login, since a manager's long-lived "Keep me logged in" session may never hit `/auth/callback`; for every active manager it recomputes the barn's outstanding lesson + agreement-charge count/total (reusing `getOutstandingLessons`/`getOutstandingCharges` with `role: 'manager'`) and upserts directly against the table (not via `createNotification`'s RPC, which rejects service-role callers — see `create_or_update_notification` below), or deletes when zero; a per-manager try/catch keeps one failure from blocking the rest of the run. |
| `horse_documents` | `id UUID PK`, `barn_id UUID NOT NULL→barns`, `horse_id UUID NOT NULL`, `record_type horse_document_type NOT NULL`, `storage_path TEXT NOT NULL`, `file_name TEXT NOT NULL`, `file_size INTEGER NOT NULL`, `notes TEXT`, `reminder_date DATE` (nullable), `created_at/updated_at TIMESTAMPTZ`; `UNIQUE(barn_id, id)`; `FK(barn_id, horse_id)→horses` | Enum `horse_document_type`: `insurance_binder`, `coggins`, `shot_record`, `contract`, `other`. No unique constraint on `(horse_id, record_type)` — multiple records per type allowed. Storage objects at `{barn_id}/horses/{horse_id}/{filename}` in the `documents` bucket. `reminder_date` is the manager-set expiration trigger date (no separate expiry-vs-reminder concept) — a document is "due" once `today >= reminder_date`; surfacing that state is a separate feature. |
| `trainer_documents` | `id UUID PK`, `barn_id UUID NOT NULL→barns`, `trainer_id UUID NOT NULL→auth.users`, `record_type trainer_document_type NOT NULL`, `storage_path TEXT NOT NULL`, `file_name TEXT NOT NULL`, `file_size INTEGER NOT NULL`, `notes TEXT`, `reminder_date DATE` (nullable), `created_at/updated_at TIMESTAMPTZ`; `UNIQUE(barn_id, id)` | Enum `trainer_document_type`: `instructor_contract`, `other`. Storage objects at `{barn_id}/trainers/{trainer_user_id}/{filename}` in the `documents` bucket. `reminder_date`: see `horse_documents`. |
| `rider_documents` | `id UUID PK`, `barn_id UUID NOT NULL→barns`, `rider_id UUID NOT NULL→auth.users`, `record_type rider_document_type NOT NULL`, `storage_path TEXT NOT NULL`, `file_name TEXT NOT NULL`, `file_size INTEGER NOT NULL`, `notes TEXT`, `reminder_date DATE` (nullable), `created_at/updated_at TIMESTAMPTZ`; `UNIQUE(barn_id, id)` | Enum `rider_document_type`: `liability_waiver`, `lease_agreement`, `boarding_contract`, `other`. Storage objects at `{barn_id}/riders/{rider_user_id}/{filename}` in the `documents` bucket. `reminder_date`: see `horse_documents`. |
| `agreements` | `id UUID PK`, `barn_id UUID NOT NULL→barns`, `rider_id UUID NOT NULL→barn_memberships(barn_id,id)`, `horse_id UUID NOT NULL→horses(barn_id,id)`, `fee NUMERIC NOT NULL`, `kind agreement_kind NOT NULL DEFAULT 'lease'`, `cadence agreement_cadence NOT NULL`, `start_date DATE NOT NULL DEFAULT current_date`, `is_active BOOLEAN NOT NULL DEFAULT true`, `created_at/updated_at TIMESTAMPTZ`; `UNIQUE(barn_id, id)`; `CHECK(kind <> 'board' OR cadence = 'monthly')` | Persistent lease or boarding arrangement. Enum `agreement_kind`: `lease` (horse lease; one-time e.g. a horse show, or recurring month-to-month), `board` (boarding agreement / stall rent; always recurring monthly). Enum `agreement_cadence`: `one_time`, `monthly`. Manager-authored; rider is the counterparty. |
| `agreement_charges` | `id UUID PK`, `barn_id UUID NOT NULL→barns`, `agreement_id UUID NOT NULL→agreements(barn_id,id)`, `period DATE NOT NULL` (pinned to the 1st of the month via `CHECK`), `fee NUMERIC NOT NULL` (snapshot), `payment_type payment_type_enum` (nullable; NULL = unpaid), `created_at TIMESTAMPTZ`; `UNIQUE(barn_id, id)`; `UNIQUE(agreement_id, period)` | Billable income lines generated from an agreement — behave like lesson fees, each independently markable paid and folded into Finances totals. The `UNIQUE(agreement_id, period)` guards against duplicate charges for the same month. |
| `horse_expenses` | `id UUID PK`, `barn_id UUID NOT NULL→barns`, `expense_date DATE NOT NULL`, `expense_time TIME` (nullable), `amount NUMERIC(10,2)` (nullable), `recipient TEXT NOT NULL`, `expense_type TEXT NOT NULL DEFAULT 'Unspecified'`, `notes TEXT`, `applies_to_all_horses BOOLEAN NOT NULL DEFAULT false`, `created_at/updated_at TIMESTAMPTZ`; `UNIQUE(barn_id, id)` | Expense tracking; RLS is manager-only (#516). DAL in `expenses.ts` (#517); list UI at `/barn/[slug]/expenses` (#518). Supports planned expenses (future `expense_date`+`expense_time`, `amount` not yet known) and spontaneous expenses (`expense_date` only, `amount` set immediately). `applies_to_all_horses=true` means the expense covers every horse in the barn without an `expense_horses` row per horse; otherwise the specific horses are listed in `expense_horses`. |
| `expense_horses` | `id UUID PK`, `barn_id UUID NOT NULL→barns`, `expense_id UUID NOT NULL`, `horse_id UUID NOT NULL`; `UNIQUE(expense_id, horse_id)`; `FK(barn_id, expense_id)→horse_expenses`; `FK(barn_id, horse_id)→horses` | Junction splitting a `horse_expenses` row across specific horses; unused when `applies_to_all_horses=true`. |

If a 3rd+ distinct revenue type appears or the Finances UNION gets painful, consolidate `agreement_charges` + `expenses` into a `financial_impacts` ledger keyed by `date_realized`; lessons fold in last.

## RLS conventions

`auth_is_barn_manager(p_barn_id uuid)` is a `SECURITY DEFINER` SQL function that bypasses RLS when checking manager status. This breaks the infinite recursion that occurs when a `barn_memberships` policy queries `barn_memberships`. Any policy that must check whether the calling user is a manager uses this helper.

`auth_is_any_barn_manager()` is a `SECURITY DEFINER` SQL function that returns `true` if the calling user is an active manager of any barn. Used by the `profiles_manager_insert_managed` and `barn_memberships_manager_insert_managed` policies to allow managers to create managed-member stubs without service-role access.

`auth_get_profile_immutable_fields(p_id uuid)` is a `SECURITY DEFINER` SQL function that returns a single `profiles` row's non-editable fields (`user_id`, `email`, `first_name`, `last_name`, `created_at`). Used by the `profiles_manager_update` WITH CHECK to verify that a manager update does not change any immutable column. The SECURITY DEFINER is required because a plain `SELECT FROM profiles` inside a `profiles` WITH CHECK clause causes infinite recursion; this function bypasses RLS when fetching the pre-update row, breaking the cycle.

`auth_is_enrolled_rider(p_lesson_id uuid, p_barn_id uuid)` is a `SECURITY DEFINER` SQL function that returns `true` if the calling user has an active `barn_memberships` row enrolled (via `lesson_riders`) in the given lesson. Used by `lessons_select_rider`, `lesson_horses_select_rider`, and `lesson_riders_select_rider` to scope rider SELECT to enrolled lessons only. The SECURITY DEFINER is required because `lesson_riders_select_rider` needs to check `lesson_riders` from a policy defined on `lesson_riders` itself; a plain subquery there causes the same infinite-recursion class of bug as `auth_is_barn_manager`.

RLS policies always go in a **separate migration file** from schema changes.

`service_role` has `GRANT ALL ON ALL TABLES IN SCHEMA public` plus a default-privileges rule so future tables are covered automatically. Supabase normally applies this at project creation; it was made explicit in migration `20260614000000_service_role_grants.sql`.

## Routes

Protected barn routes (dashboard, lessons, horses, riders, finances, settings) live in a `(protected)` route group under `src/app/barn/[slug]/(protected)/`. The group layout (`layout.tsx`) centralises auth: absent or pending membership redirects to `/barn/[slug]/login`. Public routes (login, pending, register) stay outside the group and are unaffected.

The `(protected)` layout renders a persistent role-aware nav bar above `{children}` on every barn page, responsive at the `md` breakpoint (768px, Tailwind default). The barn name is rendered via a `BarnSwitcher` Client Component (`font-semibold`). For single-barn members it renders as a plain `BlockingLink` home link. For multi-barn members it renders the barn name link plus a caret button (≥ 44 px tap target) that opens a dropdown listing all active barn memberships; the current barn is marked with a checkmark, others link to their `/barn/[slug]` dashboards; the dropdown dismisses on outside click/touch or on any link click. The barn name element appears before the section links:
- manager: {Barn Name} (home), Lessons, Expenses, Horses, Leases, Boarding, Members, Finances, Manage Barn, Guide — 9 section links
- trainer: {Barn Name} (home), Lessons, Horses, Members, Guide — 4 section links
- rider: {Barn Name} (home), Lessons, Horses, Guide — 3 section links

At `md` and up, section links render inline in the bar via a `DesktopNavLinks` Client Component. Below `md`, they're hidden from the bar and collapse behind a `NavDrawer` Client Component: a hamburger (☰, ≥ 44 px tap target) that opens a left slide-out drawer (`role="dialog"`, `aria-modal`) listing the same server-built `navLinks` array vertically — no client-side role logic, the layout remains the sole source of truth for role→links. The drawer closes on link tap, scrim tap, Escape, or a `usePathname()` change (since the `(protected)` layout persists across client-side route changes, this closes the drawer if it was left open after a browser back/forward navigation), and moves focus into itself on open. Drawer links use `BlockingLink` so the unsaved-changes guard still fires.

Both `DesktopNavLinks` and `NavDrawer` highlight the link matching the current page via the shared `isNavLinkActive(currentPath, href)` helper (`nav-active.ts`): a link is active when `usePathname()` equals its `href`'s pathname or is nested under it (so `/lessons/[id]` highlights "Lessons"), and — only for links whose `href` carries a query string, i.e. Leases (`?kind=lease`) vs. Boarding (`?kind=board`), which otherwise share the `/agreements` pathname — the current `useSearchParams()` value must match too. The active link gets `aria-current="page"` plus bolder/darker styling; inactive links are de-emphasized in grey.

A `UserMenu` Client Component and a `NotificationBell` Client Component sit on the right side of the nav bar in a flex container — avatar then bell at `md` and up (unchanged), reordered to bell then avatar below `md` via CSS `order` utilities to match the mobile bar's `☰ · barn name · spacer · bell · avatar` layout. `UserMenu` shows the user's initials (first letter of `first_name` + first letter of `last_name` from `profiles`; falls back to first character of email, then `?`). Clicking it opens a dropdown with: full name + email (non-clickable header), a "Profile" link to `/profile?barn=<slug>`, a "Switch Barn" link to `/barns` (only when the user has >1 active barn membership), a "User Guide" link to `/barn/[slug]/guide`, and a Sign Out button. The dropdown closes on outside click or touch. `NotificationBell` shows a bell icon with an unread-count badge (hidden when zero); clicking opens a dropdown listing recent notifications (title, body, timestamp) with links via the `link` field and a "Mark all read" button that calls `markAllNotificationsReadAction` and refreshes. Notification data is fetched server-side in the layout via `getNotifications`; interactivity is client-side.

"Horses" → `/barn/[slug]/horses` (all roles)
"Leases" → `/barn/[slug]/agreements?kind=lease` (manager only)
"Boarding" → `/barn/[slug]/agreements?kind=board` (manager only)
"Manage Barn" → `/barn/[slug]/settings` (manager only)

| Route | Roles | Notes |
|---|---|---|
| `/` | All | Unauthenticated users are redirected to `/login`; authenticated users are redirected server-side using barn membership logic: single active → `/barn/[slug]`, multiple active → `/barns`, pending-only single → `/barn/[slug]/pending`, pending-only multiple → `/barns`, no memberships → `/login?no_barns=true` |
| `/barns` | Authenticated users | Barn selector: one card per membership; active shows role + link to `/barn/[slug]`; pending shows badge + link to `/barn/[slug]/pending`; no memberships redirects to `/login?no_barns=true` |
| `/barn/[slug]` | All active members | All roles see an upcoming-lessons preview (next 7 days; manager/trainer filtered by `instructor_id`, rider by enrollment), split client-side (`UpcomingLessonsSections`, local timezone via `isSameLocalDay`) into a "Today" section and a "This Week" section (remaining 6 days); rider cards show a "Cancelled" badge when their own `lesson_riders.cancelled_at` is set (whole lesson not already cancelled); manager also sees pending-requests badge (links to `/settings`; hidden when zero) and a **Document Reminders** section — every barn document (horse/trainer/rider) with `reminder_date <= today` via `getDueDocuments` (server-fetched with a UTC-day cutoff), re-filtered client-side (`DocumentRemindersSection`, local timezone, same tradeoff as `isSameLocalDay`) against the viewer's local day so a document doesn't flash in a day early; same amber visual language as the pending-requests badge, hidden entirely when empty, each entry linking to the owning horse's or member's detail page; no cron involved — this is Option A from #530, surfaced passively on login rather than via a scheduled job; trainer/rider dashboards don't call `getDueDocuments` at all (manager-only in this cut). For managers only, the lessons section is titled "Barn Schedule" and interleaves upcoming *scheduled* expenses (`getUpcomingScheduledExpenses` — future `expense_date`+`expense_time`, `amount IS NULL`; a date-only expense is excluded, treated as completed/spontaneous) alongside lessons within each Today/This Week bucket, sorted by datetime (`UpcomingExpenseCard`, links to the expense detail page); trainer/rider are unaffected (no heading, no expenses, even if the prop were passed — enforced by role inside the component, not just by the caller). The empty state (`EmptyState`, "You're all clear") is shown, not hidden, when nothing is scheduled; nav links rendered by layout |
| `/barn/[slug]/lessons` | All active members | Lessons split at 7-day cutoff: recent shown immediately, older behind `OlderLessonsToggle`; manager/trainer see a destructive "Cancel" link on lessons that are eligible (`lesson_at > now()` OR `payment_type IS NULL`, and not already cancelled — trainer limited to lessons they instruct) linking to `/barn/[slug]/lessons/[id]/cancel`; cancelled lessons show a "Cancelled" badge; rider sees a self-service "Cancel" link on their own row (eligible + own `lesson_riders.cancelled_at IS NULL`, whole lesson not already cancelled) linking to `/barn/[slug]/lessons/[id]/cancel-rider/[own membership id]`, and a "Cancelled" badge when their own participation is cancelled; role-aware filter bar above the list — trainer sees a horizontally scrollable pill row (`All \| [Rider A] \| ...`) to filter by rider; manager sees a two-level pill row (`All \| By Trainer \| By Rider \| By Horse` then a second row of specific people); rider sees no filter controls; active filter persists in URL params (`?filter=trainer\|rider\|horse&id=<uuid>`) |
| `/barn/[slug]/lessons/new` | manager, trainer | A "Recurring (weekly)" checkbox atomically creates a `lesson_series` template row alongside the first lesson (via `createLessonSeries`) instead of a plain one-off lesson; no UI yet to view/stop a series (foundation for a follow-up feature) |
| `/barn/[slug]/lessons/[id]` | All active members | `notFound()` if the lesson doesn't exist, is in a different barn, or (rider only) the caller has no `lesson_riders` entry on it — defense-in-depth on top of the enrollment-scoped RLS; Edit link visible to managers and trainers; trainer/manager see `horse_notes` inline under each horse entry and `rider_notes`+`private_notes` (visually distinguished) inline under each rider entry (all read-only); rider sees own `rider_notes` read-only; manager and instructing trainer see a per-rider "Cancel" link (when the whole lesson isn't already cancelled and that rider's participation isn't cancelled) linking to `/barn/[slug]/lessons/[id]/cancel-rider/[rider membership id]`, else a "Cancelled" badge next to that rider; rider sees the same Cancel link/badge for their own row only |
| `/barn/[slug]/lessons/[id]/edit` | manager, trainer | Pre-filled edit form; instructor field read-only for trainers (DB enforces via RLS); group→normal downgrade shows warning and requires manager to select one rider/horse to keep; updates are atomic via `update_lesson_with_participants` RPC; only managers can add new horses or riders inline; notes editing is embedded inside `LessonForm` via `initialNotes` prop and saved by `updateLessonAction` via the single Save button; navigating away with unsaved notes shows a confirmation prompt |
| `/barn/[slug]/lessons/[id]/cancel` | manager, trainer (own lesson only) | Confirmation form: optional cancellation-notes textarea + destructive "Confirm Cancellation" button; `notFound()` if the lesson doesn't exist, the trainer doesn't own it, or it's ineligible (already cancelled, or past-and-paid); on submit, `cancelLessonAction` sets `cancelled_at=now()`, `fee=0`, `payment_type=NULL`, saves notes, fires `lesson_cancelled` notifications (trainer cancels → barn manager(s) + enrolled riders; manager cancels → instructor + enrolled riders), then redirects to `/barn/[slug]/lessons` |
| `/barn/[slug]/lessons/[id]/cancel-rider/[riderId]` | manager, trainer (own lesson only), rider (self only) | Cancels a single `lesson_riders` row rather than the whole lesson; entry point is always the per-rider "Cancel" link on `/barn/[slug]/lessons` or `/barn/[slug]/lessons/[id]` — the rider is fixed by the URL, so there is no in-form rider selector even for group lessons. `notFound()` if: the lesson doesn't exist, the whole lesson is already cancelled, the trainer doesn't instruct it, `riderId` isn't a rider on the lesson, a rider requests someone else's `riderId`, or the target participation is already cancelled or ineligible (`lesson_at > now()` OR `payment_type IS NULL`). Manager/trainer see a type selector — "Cancelled by Rider" (default) / "Cancelled by Instructor" — that overrides the fee policy rather than just labeling who acted: "Cancelled by Instructor" always waives the fee (`isLate=false`); "Cancelled by Rider" applies the literal 24-hour policy (`isLate = lesson_at − now() ≤ 24h`, i.e. late/no-notice cancellations keep the fee, earlier ones zero it). A self-cancelling rider always gets the "Cancelled by Rider" policy with no selector shown, and can't spoof the instructor waiver via form data — enforced server-side by the RPC itself, not just the form, since the RPC re-derives `isLate` from the lesson's real `lesson_at` whenever the caller is cancelling their own participation, ignoring whatever `p_is_late` value the client sent. On submit, `cancelRiderParticipationAction` calls `cancelRiderParticipation`, then fires `rider_participation_cancelled` notifications (a distinct type from whole-lesson `lesson_cancelled`, so the two flows don't collide on the same recipient's notification row) keyed on **actor role** (not the type selected): rider self-cancels → instructor + all active managers; manager/trainer cancels on behalf of a rider → the affected rider (skipped if unclaimed/managed with null `user_id`) + all active managers only when the actor is a trainer; redirects to `/barn/[slug]/lessons/[id]` |
| `/barn/[slug]/expenses` | manager | Expenses split at 7-day cutoff (same pattern as lessons): recent shown immediately, older behind `OlderExpensesToggle`; each row shows date, time (or `—`), recipient, expense type, horse(s) or "Entire Barn" (or `—` if none), and amount (or `—` if not yet entered); "Add Expense" button links to `/barn/[slug]/expenses/new`; row-level "Edit" link goes to `/barn/[slug]/expenses/[id]`, "Delete" link goes to `/barn/[slug]/expenses/[id]/delete` |
| `/barn/[slug]/expenses/new` | manager | `ExpenseForm`: recipient (required, free-text via native `<input list>`/`<datalist>` seeded from `getRecentRecipients`, most-recent-first), expense type (optional, same autocomplete pattern seeded from `getRecentExpenseTypes`; blank/whitespace normalized to `"Unspecified"` server-side), date (required), time (optional — presence signals a planned/scheduled expense), amount (optional — blank for planned expenses), an "Entire Barn" toggle that disables the horse checkbox list and sets `applies_to_all_horses=true` (native `disabled` semantics exclude `horse_id` from the submitted `FormData`, no extra JS needed), and notes; on recipient blur, `getMostCommonExpenseTypeAction` auto-fills expense type from `getMostCommonTypeForRecipient` and flashes it (`ring-2 ring-blue-400`, same pattern as jumping→exertion flash in `LessonForm`); `createExpenseAction` validates and calls `createExpense`, then redirects to `/barn/[slug]/expenses` |
| `/barn/[slug]/expenses/[id]` | manager | Edit page reusing `ExpenseForm` pre-filled from `getExpenseById` (`notFound()` if missing); same fields as the new-expense form, "Entire Barn"/individual-horse checkbox state restored from `applies_to_all_horses`/`horse_ids`, submit label "Save Changes"; `updateExpenseAction` validates (shared `parseExpenseFormData` helper used by both `createExpenseAction` and `updateExpenseAction`) and calls `updateExpense`, then redirects to `/barn/[slug]/expenses`; a "Delete" button links to `/barn/[slug]/expenses/[id]/delete` |
| `/barn/[slug]/expenses/[id]/delete` | manager | Confirmation page mirroring the lessons cancel pattern: summary of recipient/date/amount + destructive "Confirm Delete" button; `notFound()` if the expense doesn't exist; on submit, `deleteExpenseAction` deletes the expense (cascades to `expense_horses`) and redirects to `/barn/[slug]/expenses` |
| `/barn/[slug]/horses` | All active members | Three categorized sections: **Available** (`is_active=true`, `is_available=true`) sorted by total exertion ascending (7d) — card shows name and an `ExhaustionBar` (progress bar, thresholds resolved via `resolveExhaustionThresholds`, anchored to today with no ghost segment; tap to expand the ±3-day lesson breakdown from `getHorseProjectedExhaustion`); **Unavailable** (`is_active=true`, `is_available=false`) — card shows name, unavailability reason, and the same `ExhaustionBar`; **Inactive** (`is_active=false`) — manager only, card shows name only, no bar; empty sections hidden; manager sees inline Add Horse form (name input + button) in the page header; all cards are full-card links to `/barn/[slug]/horses/[id]`; trainer and rider see Available and Unavailable only |
| `/barn/[slug]/horses/[id]` | All active members | Horse detail page; shows horse name and current availability status; manager sees `HorseManagerForm` — a unified form with name input, three-state pill status control (Active / Unavailable / Inactive), conditional reason textarea (shown only when Unavailable is selected), and a single Save that updates name, `is_active`, `is_available`, and `unavailability_reason` in one call via `updateHorseDetails`; trainer/rider see reason as read-only text when horse is unavailable; manager and trainer see a Documents section listing all uploaded files by `record_type` with signed-URL links; manager can upload and delete documents, trainer can upload only; upload form has an optional "Expiration reminder date" field; manager sees an inline editable Reminder Date cell per document (blur-to-save, mirrors `ChargesTable`'s pattern), trainer/rider see it read-only; every role sees a role-agnostic "Reminder Due" badge (`ReminderDueBadge`) next to that cell once `reminder_date <= today`; rider sees no documents section; manager sees an **Exhaustion Thresholds** section — a "Use barn defaults" checkbox (checked when both `horses.exhaustion_threshold_moderate`/`exhaustion_threshold_high` are `null`) that disables the `moderate`/`high` number inputs when checked (native `disabled`, excluded from `FormData`, same pattern as the expenses "Entire Barn" toggle); inputs pre-fill from the horse's own override when set, else the barn default; unchecking enables custom entry, validated server-side (`moderate < high`) via `updateHorseExhaustionThresholdsAction`; not rendered for trainer or rider |
| `/barn/[slug]/agreements` | manager | Kind-scoped list (`?kind=lease\|board`, defaults to `lease` when missing/invalid) of agreements of that kind — rider, horse, fee, status (Active/Ended); **Add Lease**/**Add Boarding** button top-right; `EmptyState` when the list is empty; row **View** link to `/barn/[slug]/agreements/[id]` and **Edit** link to `/barn/[slug]/agreements/[id]/edit` |
| `/barn/[slug]/agreements/new` | manager | Add form (`?kind=` from the list's Add button): rider select, horse select, fee, start date (default today); lease shows a cadence select (`one_time`\|`monthly`); board hides cadence (forced `monthly`) and pre-fills fee from `getBarnDefaultBoardFee` |
| `/barn/[slug]/agreements/[id]` | manager | Agreement detail page: rider, horse, fee, cadence, and status (resolved via `resolveMemberNames`/`resolveHorseNames`), plus every charge from `getChargesForAgreement`; each charge row has an inline **Payment Type** dropdown (`ChargesTable` client component, mirrors `OutstandingTable`'s mark-paid pattern — calls `updateChargePaymentTypeAction`, then `router.refresh()`) and an editable **Fee** input (calls `updateChargeFeeAction` on blur, only when changed) for pro-rating a single occurrence; **Edit** button top-right links to `[id]/edit` |
| `/barn/[slug]/agreements/[id]/edit` | manager | Edit form: only fee is editable; rider, horse, start date, and cadence are shown read-only (resolved via `resolveMemberNames`/`resolveHorseNames`) — to change any of those, end the agreement and create a new one; **End Agreement** button (confirm dialog) sets `is_active=false`, hidden once already ended |
| `/barn/[slug]/members` | All active members | Members roster; "You" card (links to detail page) shown for all roles; manager sees Managers (own entry excluded) + Trainers + Riders sections; trainer sees Riders section only; rider sees "You" card only; data fetched via `barn_memberships` join `profiles` (via `profile_id`); nav link visible to manager and trainer only. Manager Trainers and Riders sections each have an inline Add form (first + last name) that calls `createManagedMemberAction` bound to that section's role (`'trainer'`/`'rider'`, not a form field); managed rows in either section render via the shared `ManagedMemberRow` component — an "Unlinked" badge, Copy invite button, and Revoke button (`revokeInviteTokenAction`) instead of a link to the detail page |
| `/barn/[slug]/members/[membership_id]` | All active members | Member detail page; shows documents for the target member and an upload form when the caller can manage docs; manager target: uses `trainer_documents` stored at `{barn_id}/managers/{user_id}/` (same document types as trainer); trainer target: `trainer_documents` at `trainers/`; rider target: `rider_documents` at `riders/`; manager can upload/delete any member's docs (including other managers' and own); trainer manages own docs, views rider docs read-only; rider manages own docs only; trainer viewing another trainer gets `notFound()`; rider viewing anyone else gets `notFound()`; if target has no linked account, shows "No account linked" message; a rider target additionally shows a **Boarding** status line (fee + link to the active agreement, or an **Add boarding** link into `/agreements?kind=board` when none) resolved via `getActiveAgreementForRider` — rendered regardless of whether the target has a linked account, since `agreements.rider_id` is a `barn_memberships.id`, not an auth user id, but only when the caller is a manager or the rider viewing their own page, since `agreements` RLS grants SELECT to neither trainers nor other riders — a trainer viewing a rider's page would otherwise see a false "no boarding" status; accepted file types: PDF, JPG, PNG, DOCX (max 5 MB); documents open via signed URL (300 s TTL); upload form has an optional "Expiration reminder date" field; manager sees an inline editable Reminder Date cell per document (blur-to-save, mirrors `ChargesTable`'s pattern) regardless of who uploaded it; trainer/rider see it read-only; every role sees a role-agnostic "Reminder Due" badge (`ReminderDueBadge`) next to that cell once `reminder_date <= today` |
| `/barn/[slug]/riders` | All (redirect) | Redirects to `/barn/[slug]/members` |
| `/barn/[slug]/finances` | manager | **Outstanding** section (all-time past unpaid lessons with non-zero fee, merged with unpaid agreement charges whose `period` is before the first of the current month, via `mergeOutstandingItems` — inline payment-type dropdown via `OutstandingTable` Client Component with a Type column (Lesson/Lease/Boarding) routing to `updatePaymentTypeAction` or `updateChargePaymentTypeAction`, plus a "View all outstanding" link to `/barn/[slug]/finances/outstanding`) appears above the month selector and is hidden entirely when there are no outstanding items. Below it: `←`/`→` month navigation; `?month=YYYY-MM` selects month (defaults to current, clamped to barn creation date); summary rows in order **Collected income** (unsuffixed — the month picker above already contextualizes it), **Total Expenses** (barn-wide, from `getExpenseFinancialSummary`), **Net** (Collected income − Total Expenses), then **Pending income** (only shown for the current month) — **Collected income** and **Pending income** (`payment_type IS NOT NULL`/`IS NULL`) fold in agreement charges for the period alongside lessons and are net of the per-lesson instructor cut (agreement charges are never cut — they're not lessons; expenses are unrelated to the cut and are simply subtracted in full); pill-style tab switcher (`?tab=horse\|tier\|rider\|trainer`, defaults to `horse`) with four views: **By Horse** (Horse/Income/Expenses/Net columns, via `<Th>`/`<Td>` — row set is the *union* of horses with lesson/agreement income that month and horses with expenses that month, via `getExpenseFinancialSummary`'s per-horse `breakdown`; a horse present on only one side shows `$0.00` for the other, so an expense-only horse (e.g. injured, vet bills only) still surfaces with a negative Net; sorted by income descending, ties broken alphabetically), **By Tier** (tier name, price or `—` for Custom, lesson count, an **Instructor Cut** column showing `(cut × lessons)` accounting-style or `—` when zero, net Subtotal — plus a synthetic **"Non-lesson income"** row with a tap-to-toggle ⓘ when agreement charges were collected that month, its Instructor Cut always `—`), **By Rider** (rider name, collected income net of cut — same charge fold-in), **By Trainer** (trainer full name, collected income net of cut — plus the same synthetic **"Non-lesson income"** row, since agreements have no instructor); negative net figures (e.g. a comped lesson after the cut, or a horse's negative Net) render accounting-style via the shared `formatCurrency` helper (`src/lib/format-currency.ts`) rather than a leading minus sign; the horse/rider drill-down pages (`finances/horses/[id]`, `finances/riders/[id]`) show the same net-of-cut fee and split amounts, also via `formatCurrency`; the rider drill-down is unaffected by this issue (no expense breakdown there) |
| `/barn/[slug]/finances/outstanding` | All active members | Read-only list of outstanding items for the calling user's role (lessons merged with agreement charges via `mergeOutstandingItems`, with a Type column); manager sees all barn outstanding, trainer sees own lessons only (instructor_id filter; not a party to agreements), rider sees enrolled lessons plus their own outstanding leases/boarding; each lesson row links to the lesson detail page, charge rows are plain text (no detail page) |
| `/barn/[slug]/finances/horses/[id]` | manager | Per-horse drill-down for a given month (`?month=YYYY-MM`, defaults to current); one combined table of the horse's paid lessons (full fee, horse count, split amount, linking to the lesson), paid agreement charges (full amount, no split, linking to the agreement), and expenses (amount and split in accounting-parens format, linking to the expense), each row tagged with a **Type** column (Lesson/Lease/Boarding/Expense) and all rows ordered by date ascending; bottom **Net** row = lesson splits + charge fees − expense splits, matching this horse's Net on the "By Horse" tab; linked from the By Horse tab with month param preserved |
| `/barn/[slug]/finances/riders/[id]` | manager | Per-rider income drill-down for a given month (`?month=YYYY-MM`, defaults to current); lists each paid lesson the rider participated in (full fee, rider count, split amount) plus a separate "Leases & Boarding" table of the rider's paid agreement charges for the month (full amount, no split; links to the agreement detail page); page total matches the "By Rider" summary on the finances page; linked from the By Rider tab with month param preserved |
| `/barn/[slug]/settings` | manager | **Manage Barn** page: Invite Link, Pending Requests (approve/reject), Active Members (remove), Instructor Cut (single number field + Save, with a static warning that changing it affects all historical income figures), **Horse Exhaustion Thresholds** (Moderate/High number fields pre-filled from the barn row, server-validated `moderate < high` with a field error otherwise), Lesson Tiers list with Edit links and Add Tier link, editable **Default Board Fee** (with non-retroactive helper text — only seeds new boarding agreements) |
| `/barn/[slug]/settings/tiers/new` | manager | New Tier page; shared `TierForm` component; on save redirects to settings |
| `/barn/[slug]/settings/tiers/[id]` | manager | Edit Tier page; shared `TierForm` component; Activate/Deactivate, Set Default, and Save actions all redirect to settings |
| `/barn/[slug]/guide` | All active members | Role-specific user guide rendered from `USER_GUIDE_{ROLE}.md` at repo root using `react-markdown` with Tailwind `prose` styling |
| `/profile` | Authenticated | Edit form for first name, last name, phone, emergency contact name, emergency contact phone; name changes prompt user to notify their barn manager; linked from avatar dropdown. Accepts optional `?barn=<slug>` — when present and the user has an active membership in that barn, the full barn nav bar is rendered instead of the back-button fallback; post-save redirect also returns to `/barn/<slug>` rather than the default membership-based redirect |
| `/profile/complete` | Authenticated | Same form with "Complete your profile" heading; post-login destination when any contact field (phone, emergency_contact_name, emergency_contact_phone) is null; redirects to `/` after save |
| `/login` | All | Sign-in page; displays Supabase connection status dot (green = `NEXT_PUBLIC_SUPABASE_URL` set, yellow = not set); shows no-barn guidance when `?no_barns=true` and user is authenticated; "Keep me logged in" checkbox (checked by default, pre-filled from `remember_me_pref` cookie) — on submit the sign-in actions set `remember_me` (5-min TTL, read by the auth callback) and `remember_me_pref` (1-year TTL) cookies to `1`/`0` before the OAuth redirect; `/barn/[slug]/login` has the same checkbox. The auth callback consumes `remember_me` (cleared on every exit path): `1` → `barn_session_*` and Supabase `sb-*` cookies get `maxAge` 30 days; `0` → session-only (`maxAge` stripped; note `@supabase/ssr` defaults `sb-*` to 400 days). Because `sb-*` cookies are rewritten on token refresh (by `src/lib/supabase/server.ts` and `src/proxy.ts` cookie adapters), both adapters re-apply the lifetime via `applyRememberMe` (`src/lib/supabase/cookie-options.ts`), reading `remember_me` falling back to the durable `remember_me_pref` |
| `/barn/[slug]/register` | unauthenticated | Membership sign-up flow |

## Data access layer

`src/lib/db/` — one file per domain. Never query Supabase directly from components or actions; always go through these modules.

| File | Domain |
|---|---|
| `auth.ts` | Auth session; `getAuthenticatedUser()` — wraps `supabase.auth.getUser()`, returns `User \| null` |
| `agreements.ts` | Lease/board agreement + charge CRUD: `createAgreement(params, client?)` (calls `create_agreement_with_first_charge` RPC), `getAgreementsByBarn(barnId, kind?, client?)`, `getAgreementById(agreementId, barnId, client?)`, `updateAgreement(agreementId, barnId, updates: { fee? })`, `endAgreement(agreementId, barnId)` (sets `is_active=false`); charge fns `getChargesForAgreement(agreementId, barnId)`, `updateCharge(chargeId, barnId, fee)`, `updateChargePaymentType(chargeId, barnId, paymentType)`, `generateChargeForMonth(agreementId, barnId, period, client?)` (calls `generate_agreement_charge` RPC; atomic and barn-scoped, so it never overwrites an already-paid charge and never crosses barns); `getBarnDefaultBoardFee(barnId, client?)`; `getActiveAgreementForRider(barnId, riderId, kind, client?)` (query-scoped lookup of a rider's active agreement of a given kind, most-recently-created first — a rider can have more than one simultaneously-active agreement of the same kind, e.g. boarding two horses, so this can't assume exactly one row exists — used by the member detail page's boarding status shortcut); finance fold-in fns consumed by `lesson-finances.ts` — `getChargesForSummary(barnId, startDate, endDate, client?)` (raw `{period, fee, payment_type}[]` in the period range, for collected/pending totals), `getPaidCharges(barnId, startDate, endDate, client?)` (paid charges in range joined to their agreement's `kind`/`rider_id`/`horse_id` via a manual second query against `agreements`, rather than a PostgREST nested embed — embedding across this table's composite `(barn_id, agreement_id)` FK risks the same schema-cache resolution failure a prior nested embed hit in production — for By Horse/By Rider folding and the drilldown pages), `getOutstandingCharges(barnId, userId?, role?, client?)` (unpaid charges with `period` before the first of the current month, resolved to `riderName` via the same manual agreement join; `role==='trainer'` short-circuits to `[]` since trainers aren't parties to agreements; `role==='rider'` first resolves the caller's own agreement IDs, then filters charges to those) |
| `expenses.ts` | Horse expense CRUD + reporting: `getExpensesByBarn(barnId)` (all expenses with resolved horse names, newest first); `getExpenseById(expenseId, barnId)` (returns `null` if not found); `createExpense(barnId, data, client?)` (calls `create_expense_with_horses` RPC) / `updateExpense(expenseId, barnId, updates, client?)` (calls `update_expense_with_horses` RPC) — both atomically write the `horse_expenses` row and replace `expense_horses` junction rows in one transaction, skipped when `applies_to_all_horses`; `deleteExpense(expenseId, barnId, client?)`; `getUpcomingExpenses(barnId, from, to)` (planned expenses — `amount IS NULL` — due within the window, combining `expense_date`+`expense_time` client-side since Postgrest can't filter on that arithmetic; a null `expense_time` is treated as end-of-day); `getUpcomingScheduledExpenses(barnId, from, to)` (Barn Schedule dashboard widget — same `amount IS NULL` + window filtering as `getUpcomingExpenses`, but excludes a null `expense_time` instead of defaulting it to end-of-day, since a date-only expense isn't "on the schedule"; resolves horse names via the `expense_horses` junction, mirroring `getExpensesByBarn`); `getExpenseFinancialSummary(barnId, startDate, endDate)` / `getHorseExpenseDetail(barnId, horseId, startDate, endDate)` (barn-wide expenses split equally across horses active on `expense_date`, using `horses.created_at`/`deactivated_at`; non-barn-wide expenses split across their `expense_horses` rows); `getRecentRecipients(barnId)` (ranked by recent-6-months frequency, then all-time frequency, then alphabetical — every recipient stays listed, not just recent ones); `getRecentExpenseTypes(barnId)` (all-time frequency); `getMostCommonTypeForRecipient(barnId, recipient)` (returns `null` if the recipient has no expenses) |
| `barns.ts` | Barn lookups; `updateBarnDefaultBoardFee(barnId, defaultBoardFee, client?)` — manager-only write, backed by an RLS policy scoped by `auth_is_barn_manager(id)`; `setInstructorCut(barnId, value, client?)` — calls `set_instructor_cut` RPC; `updateExhaustionThresholds(barnId, { moderate, high }, client?)` — direct table update of `exhaustion_threshold_moderate`/`exhaustion_threshold_high`, backed by the same `manager_update_barns` RLS policy as `updateBarnDefaultBoardFee`; used by the Manage Barn settings page (`/barn/[slug]/settings`) |
| `barn-memberships.ts` | Membership reads and writes; cross-barn user lookup (`getBarnMembershipsForUser`); `getUserMembership(userId, barnId)` — single-membership lookup for a user in a specific barn, returns `null` if not found; `getActiveMembersWithProfiles(barnId, role)` — returns active members with names joined from `profiles` via `profile_id`; returns `{ membershipId, userId, name, isManaged, inviteToken }`; `getInstructorsByBarn(barnId)` — returns `{ membershipId, userId, name }[]` for any active, `can_instruct=true` membership, including stub trainers (`user_id IS NULL`) — used to populate the lesson form's instructor select and to validate a manager-submitted `instructor_id`; `resolveMemberNames(membershipIds, barnId, client?)` — resolves membership IDs to a `Map<membershipId, "First Last">` via `profile_id` FK join, falling back to raw membership ID; `createManagedMember(barnId, firstName, lastName, role, client?)` — calls `create_managed_member` RPC to insert a stub profile (`is_managed=true`, null email) + active membership of the given role with a random `invite_token`, returns `{ membershipId }`; `claimManagedMember(token, userId, email, client?)` — calls `claim_managed_member` RPC to atomically link a real auth user to a stub; `revokeInviteToken(membershipId, barnId, client?)` — regenerates the `invite_token` and returns the new UUID |
| `horses.ts` | Horse registry; per-horse exertion summary (`getHorseExertionSummary`); `getHorsesByBarn` filters to active only; `getHorseById(horseId, barnId)` — single horse lookup, returns `null` if not found; `updateHorseDetails(horseId, barnId, updates)` — atomic update (via `update_horse_details` RPC) for `name?`, `is_active`, `is_available`, `unavailability_reason` (used by manager save form); the RPC derives `deactivated_at` from the row's own prior `is_active` value in the same statement — set to `now()` on true→false, cleared to `NULL` on false→true, left untouched otherwise — no separate read step; `setHorseActive`, `setHorseAvailability` — individual field setters (still exported; used internally); `resolveHorseNames(horseIds, barnId, client?)` — resolves a list of horse IDs to a `Map<horseId, name>` scoped to `barnId`; callers fall back to raw horse ID when ID is absent from the map; `getHorseProjectedExhaustion(horseId, barnId, targetDate, excludeLessonId?)` — calls the `get_horse_projected_exhaustion` RPC, returns `{ lessonAt: string; exertionLevel: number }[]`; `resolveExhaustionThresholds(horse, barn)` — pure function, resolves `{ high, moderate }` per-field, preferring the horse's own `exhaustion_threshold_high`/`exhaustion_threshold_moderate` override and falling back to the barn's default for whichever field is `null`; `getExhaustionBand(total, thresholds)` — pure function, returns `'low'` (`total <= moderate`), `'moderate'` (`moderate < total <= high`), or `'high'` (`total > high`); defined in `src/lib/exhaustion-band.ts` and re-exported here for backward compatibility — pulled out of this file (which imports `@/lib/supabase/server`, a server-only module) so `ExhaustionBar.tsx`, a Client Component, can import it without bundling server-only code; `updateHorseExhaustionThresholds(horseId, barnId, thresholds)` — direct table update of `exhaustion_threshold_moderate`/`exhaustion_threshold_high`, backed by the existing `horses_manager_update` RLS policy (no RPC); `thresholds` is `{ moderate, high } | null` — passing `null` nulls both columns, reverting the horse to barn defaults; used by the horse detail page's per-horse threshold override section |
| `lessons.ts` | Lesson CRUD: `createLesson`, `getLessonsByBarn(barnId, userId, role)` — manager: all barn lessons; trainer: resolves the caller's own `barn_memberships.id` via `getUserMembership` and filters lessons where `instructor_id` equals that membership id (returns `[]` if the caller has no membership); rider: lessons via `lesson_riders` enrollment (resolved through `barn_memberships`, no `cancelled_at` filter — a rider's own cancelled participation still appears in their list); returns `LessonWithDetails[]` (includes `horse_names`, `horse_ids`, `rider_names`, `rider_ids`, `rider_cancelled_ats`, `instructor_name` resolved via `resolveMemberNames` alongside rider names — parallel arrays built together so indexes always correspond); `getLessonById(lessonId, barnId, role, userId?)` — `role` is required (no default); rider role omits `private_notes` from the select but includes `cancelled_at` for all roles; resolves `instructor_name` and `instructor_user_id` via a `barn_memberships` → `profiles` lookup on `instructor_id` (mirrors rider name resolution, so a managed/stub instructor with `user_id IS NULL` still resolves a name); `cancelLesson(lessonId, barnId, notes?)` — soft-cancels a lesson: sets `cancelled_at=now()`, zeroes `fee`, nulls `payment_type`, saves `cancellation_notes`; `updateLesson`, `getUpcomingLessons(barnId, from, to, userId, role)` — manager/trainer: resolves the caller's own membership id the same way as `getLessonsByBarn` and filters by `instructor_id`; rider: resolves via `barn_memberships → lesson_riders → lessons` |
| `lesson-participants.ts` | Participant management: `createLessonWithParticipants`, `updateLessonWithParticipants`, `addHorseToLesson`, `addRiderToLesson`, `updateLessonRiderNotes`, `updateLessonHorseNotes`; `getRiderEnrolledLessonIds(barnId, userId, client?)` — resolves active rider membership then returns enrolled lesson IDs; `cancelRiderParticipation(lessonId, barnId, riderId, notes?, isLate)` — cancels a single rider's participation via the `cancel_rider_participation` RPC; sets `lesson_riders.cancelled_at=now()` and overwrites `rider_notes`; zeroes `lessons.fee`/nulls `payment_type` only when `isLate` is `false` |
| `lesson-series.ts` | `createLessonSeries(params, client?)` — same param/return shape as `createLessonWithParticipants`; calls `create_lesson_series_with_participants` to atomically create the `lesson_series` template row and its first `lessons` row (tagged with `series_id`) in one transaction; used by `submitLesson` when the "Recurring (weekly)" checkbox is checked, in place of `createLessonWithParticipants`. `getSeriesById(seriesId, barnId, client?)` — barn-scoped lookup, returns `LessonSeries \| null`; not yet wired into any route — foundation for the follow-up stop-series and nightly-generation features |
| `lesson-finance-queries.ts` | Raw-row query layer for `lesson-finances.ts` — one function per underlying fetch (e.g. `getOutstandingLessonRows` — trainer-role filtering resolves the caller's own membership id via `getUserMembership` before filtering `instructor_id`, mirroring `lessons.ts`, `getPaidLessonFees`, `getLessonHorsesForLessons`, `getLessonRidersForLessons`), each taking an explicit, required `client: SupabaseClient` param — no internal `createClient()` fallback, unlike the optional-`client?`-with-fallback convention used elsewhere in this layer (e.g. `resolveMemberNames`, `resolveHorseNames`). The required param is deliberate here: it lets `lesson-finance-queries.test.ts` drive every function with plain raw-row fixtures and no `@/lib/supabase/server` mock. No name resolution or aggregation — `lesson-finances.ts` is a pure projection layer over these raw rows. |
| `lesson-finances.ts` | Financial reporting, net of the per-lesson instructor cut: `getFinancialSummary(barnId, startDate, endDate, instructorCut?)` (returns `collectedIncome`, `pendingIncome`, `breakdown` grouped by `tier_name` with `{ tierName, price, lessonCount, subtotal, instructorCut }[]` — `subtotal` is net, `instructorCut` is `cut × lessonCount` for that row's display column; folds in `agreements.ts:getChargesForSummary` — paid charges add to `collectedIncome` at their full gross amount (charges are never cut — they're not lessons), and unpaid charges add to `pendingIncome` only when their `period` is the current month or later (mirroring the lesson-side `lesson_at > now` future-only rule, so a past-due unpaid charge is never counted in both `pendingIncome` for a past month and the all-time Outstanding total), and a synthetic `{ tierName: NON_LESSON_INCOME_LABEL, price: null, lessonCount: <charge count>, subtotal: <charges collected>, instructorCut: 0 }` row is appended to `breakdown` when any charges were collected); `getOutstandingLessons(barnId, userId?, role?, client?)` (returns `OutstandingLesson[]` with past unpaid lessons, fee ≠ 0, **gross** — Outstanding is a collections list, not an income statement, so the cut is never applied here; role-filtered: manager=all barn, trainer=own lessons by `instructor_id` (resolved to the caller's own membership id), rider=enrolled lessons via `lesson_riders`; `instructor_name` resolved via `resolveMemberNames` alongside rider names; **lesson-only** — does not itself know about agreement charges); `getOutstandingCharges` (from `agreements.ts`) and `mergeOutstandingItems(lessons, charges)` (pure function, exported from this file, sorts the combined `OutstandingItem[]` by date) are composed together at the page layer (`finances/page.tsx`, `finances/outstanding/page.tsx`) rather than inside `getOutstandingLessons`, keeping that function's existing tests untouched; `getHorseIncomeSummary`/`getRiderIncomeSummary(barnId, startDate, endDate, instructorCut?)` (collected lesson-split income — the cut is subtracted from the lesson fee once per lesson *before* splitting across participants, so a group lesson is cut exactly once regardless of rider/horse count — folded with the full, unsplit, uncut amount of each collected agreement charge for that horse/rider, via `agreements.ts:getPaidCharges`; a paid lesson with zero horse/rider participants — defensive-only on both sides, since the lesson form and the deferred `enforce_lesson_participant_counts` trigger both require ≥1 horse and ≥1 rider for every lesson type; only reachable via legacy data predating that trigger — folds its net fee into a synthetic `NO_HORSE_LABEL`/`NO_RIDER_LABEL` row, unsplit, appended after the sorted real rows, so `collectedIncome` always reconciles with the sum of the breakdown); `getTrainerIncomeSummary(barnId, startDate, endDate, instructorCut?)` (collected lessons grouped by `instructor_id`, cut subtracted per lesson before summing, name resolved via `resolveMemberNames`, `trainerId` is a `barn_memberships.id`; a paid lesson whose `instructor_id` is `null` — happens via `ON DELETE SET NULL` when a trainer's membership is removed — folds its net fee into a synthetic `NO_INSTRUCTOR_LABEL` row instead of being dropped, for the same reconciliation reason; plus the same synthetic `NON_LESSON_INCOME_LABEL` row keyed by `trainerId: NON_LESSON_INCOME_LABEL` when charges were collected, uncut); `getHorseIncomeDetail(barnId, horseId, startDate, endDate, instructorCut?)` (per-horse drill-down: returns `{ horseName, rows: HorseIncomeDetailRow[], chargeRows: HorseChargeDetailRow[], total }` where each lesson row has `{ lessonId, lessonAt, fee, horseCount, splitAmount }` — `fee` is the net (post-cut) lesson fee, not the raw stored value, and `splitAmount` divides that net fee across `horseCount` — and each charge row has `{ chargeId, agreementId, period, kind, fee }` at its full uncut amount; `total` sums both); `getRiderIncomeDetail(barnId, riderId, startDate, endDate, instructorCut?)` (per-rider drill-down: returns `{ riderName, rows: RiderIncomeDetailRow[], chargeRows: RiderChargeDetailRow[], total }`, same net-fee/split shape as the horse detail). All six `instructorCut` params default to `0` (kept for test convenience — every real call site passes the barn's actual `instructor_cut`); negative net values (e.g. a comped lesson after the cut) are allowed and not clamped to zero — they display accounting-style via `src/lib/format-currency.ts`'s `formatCurrency`. `NON_LESSON_INCOME_LABEL` (`'Non-lesson income'`), `NO_INSTRUCTOR_LABEL` (`'No instructor'`), `NO_HORSE_LABEL` (`'No horse'`), and `NO_RIDER_LABEL` (`'No rider'`) are exported so the DAL-constructed rows and the page's popover-trigger/unlinked-row checks use the identical strings — the page renders all four synthetic rows as plain, unlinked text with an explanatory `InfoPopover` rather than a drill-down link. |
| `lesson-tiers.ts` | Tier CRUD: `getTiersByBarn`, `createTier` (accepts optional `defaultExertionLevel`/`defaultJumping`), `updateTier` (accepts optional `default_exertion_level`/`default_jumping` in updates), `deactivateTier`, `reactivateTier`, `setDefaultTier`, `getAllTiersByBarn` (incl. inactive), `getTierById` |
| `profiles.ts` | User profiles; `upsertProfile` (called at registration); `getProfileByUserId` (single-user lookup by auth user ID); `getProfilesByUserIds(userIds)` — bulk lookup by auth user ID array, returns empty array when list is empty; `updateProfile` (updates all five editable fields: first_name, last_name, phone, emergency_contact_name, emergency_contact_phone); `updateContactInfo` (updates phone/emergency contact fields; RLS enforces own-row for users, managed/stub profiles only for managers) |
| `notifications.ts` | Notification CRUD: `createNotification` (upserts on `user_id,barn_id,type`; resets `read_at` on conflict); `deleteNotificationByType(userId, barnId, type, client?)` (deletes a notification by user+barn+type; no-op if not found); `markNotificationRead` (sets `read_at = now()` by id); `markAllNotificationsRead` (sets `read_at = now()` for all unread for a user in a barn); `getNotifications(userId, barnId, limit=20)` (recent notifications ordered by `created_at DESC`) |
| `document-storage.ts` | Shared Supabase Storage helpers: `uploadFile(storagePath, file, contentType)`, `removeFile(storagePath)`, `getSignedUrl(storagePath)` (300 s TTL), `validateFile(file)` (throws on invalid type/extension/size; returns lowercased extension on success); exports `ALLOWED_MIME_TYPES`, `ALLOWED_EXTENSIONS`, `MAX_FILE_SIZE` |
| `documents.ts` | Unified horse/trainer/rider document CRUD, replacing the three former per-entity modules: `getDocuments(entity, entityId, barnId)`, `createDocument(entity, barnId, entityId, recordType, storagePath, fileName, fileSize, notes, reminderDate)`, `deleteDocument(entity, id, entityId, barnId)`, `updateDocumentReminderDate(entity, id, entityId, barnId, reminderDate)`, where `entity` is `'horse' \| 'rider' \| 'trainer'`. Each function has one TS overload per entity so callers get `HorseDocument[]`/`RiderDocument[]`/`TrainerDocument[]` back with no casting. `entityId` is always filtered on delete (previously only the horse variant checked it; rider/trainer did not). `reminderDate` (`YYYY-MM-DD` or `null`) is set at upload and can be pushed forward later via `updateDocumentReminderDate` — no new RLS needed, since the existing uploader INSERT policies and manager `FOR ALL` policies aren't column-restricted. `getDueDocuments(barnId, today)` — no-cron reminder surfacing: queries all three document tables for `reminder_date <= today` (`today` passed in by the caller rather than computed internally, for testability) and returns a flat `DueDocument[]` sorted by `reminderDate` ascending. Horse-doc owner names resolve via `resolveHorseNames` (from `horses.ts`, passed this function's own client); trainer/rider-doc owners are stored as an auth user id, not a membership id, so their `barn_memberships` row and `profiles` name are resolved with two inline queries scoped to the barn — `ownerId` on those rows is the resolved membership id (for linking to `/barn/[slug]/members/[membership_id]`), falling back to the raw user id / `'Unknown Member'` if no active membership or profile is found. |
| `types.ts` | Shared TypeScript types |

## Server actions pattern

No API routes. All mutations go through Next.js Server Actions.

- **Global actions:** `src/app/actions/` — auth (`auth.ts`), lesson submission, payment-type update, and cancellation (`lessons.ts`), notification create and mark-read (`notifications.ts`), expense create, delete, and recipient-type lookup (`expenses.ts`)
- **Feature-scoped actions:** co-located `actions.ts` files inside route directories (`profile/`, `barn/[slug]/horses/`, `barn/[slug]/horses/[id]/`, `barn/[slug]/register/`, `barn/[slug]/riders/`, `barn/[slug]/settings/`, `barn/[slug]/(protected)/approvals/`, `barn/[slug]/(protected)/members/[membership_id]/`, `barn/[slug]/(protected)/agreements/`)

## Auth guard

`src/lib/auth/guard.ts` exports `requireMembership(barnSlug: string, allowedRoles: Role[]): Promise<{ user, barn, membership }>`. All server actions that enforce role-based access call this function. It redirects to `/barn/[slug]/login` on any auth or role failure, eliminating the 6-line auth block that was previously duplicated across every action. `register/actions.ts` does not use it — its guard is a different pattern (membership existence check, not role authorization).

## Supabase RPC

`create_lesson_with_participants(p_barn_id, p_instructor_id, p_lesson_at, p_fee, p_horse_ids[], p_exertion_levels[], p_rider_ids[], p_lesson_type, p_jumping, p_tier_name, p_payment_type)` — atomically inserts a lesson, its horse assignments (`lesson_horses`), and one or more riders (`lesson_riders`) in one transaction. Validates participant counts inline: normal lessons require exactly 1 horse and exactly 1 rider; group lessons require ≥ 2 riders. `p_jumping` defaults to `false`; `p_tier_name` defaults to `'Custom'`; `p_payment_type` defaults to `NULL`. Used by lesson submission to avoid partial writes.

`create_lesson_series_with_participants(p_barn_id, p_instructor_id, p_lesson_at, p_fee, p_horse_ids[], p_exertion_levels[], p_rider_ids[], p_lesson_type, p_jumping, p_tier_name, p_payment_type)` — same signature and inline participant-count validation as `create_lesson_with_participants` (duplicated rather than shared, so the existing function stays untouched), but first inserts a `lesson_series` template row (arrays as given), then inserts the lesson/`lesson_horses`/`lesson_riders` rows exactly as `create_lesson_with_participants` does, setting the new lesson's `series_id` to the series row's id. `SECURITY INVOKER`; `EXECUTE` granted to `authenticated`. Used by `createLessonSeries` in `lesson-series.ts`, called from lesson submission when "Recurring (weekly)" is checked.

`set_default_tier(p_tier_id, p_barn_id)` — atomically clears `is_default` on all barn tiers then sets `is_default=true` on the target tier in one transaction. Used by `setDefaultTier` in `lesson-tiers.ts`.

`update_horse_details(p_horse_id, p_barn_id, p_name, p_is_active, p_is_available, p_unavailability_reason)` — atomically updates a horse's `name`, `is_active`, `is_available`, `unavailability_reason`, deriving `deactivated_at` from the row's own pre-update `is_active` value in the same statement (unchanged transition leaves it untouched, false→true clears it to `NULL`, true→false sets it to `now()`) — avoids a separate read-then-write. `p_name` of `NULL` leaves the name unchanged. `SECURITY INVOKER`; relies on the existing `horses_manager_update` RLS policy for authorization. Used by `updateHorseDetails` in `horses.ts`.

`update_lesson_with_participants(p_lesson_id, p_barn_id, p_lesson_at, p_instructor_id, p_fee, p_lesson_type, p_jumping, p_payment_type, p_tier_name, p_horse_ids[], p_exertion_levels[], p_rider_ids[])` — atomically updates the `lessons` row and replaces `lesson_horses` + `lesson_riders` (delete then insert) in one transaction. The deferred `enforce_lesson_participant_counts` trigger sees the final state at commit. Used by the lesson edit page.

`create_managed_member(p_barn_id UUID, p_first_name TEXT, p_last_name TEXT, p_role TEXT)` — atomically creates a managed-member stub: inserts an `is_managed=true` profile and an active `barn_memberships` row with the given role and a random `invite_token`, returning the membership ID. Sets `can_instruct = true` when `p_role = 'trainer'`. Rejects (`not_authorized`) unless the caller is a manager of `p_barn_id`; role validity enforced by the `role → roles` FK. `SECURITY DEFINER`; `EXECUTE` revoked from `PUBLIC` and granted to `authenticated`. Called by `createManagedMember` in `barn-memberships.ts`.

`claim_managed_member(p_token UUID, p_user_id UUID, p_email TEXT)` — atomically links a real auth user to a managed-stub membership. Finds the membership by `invite_token`, rejects if not found (`token_not_found`) or if `user_id` already has a profile (`user_already_claimed`), then updates `profiles` (`user_id`, `email`, `is_managed=false`) and `barn_memberships` (`user_id`, `invite_token=NULL`) in one transaction. `SECURITY DEFINER`; `EXECUTE` revoked from `PUBLIC` and granted to `authenticated`.

`set_can_instruct(p_membership_id uuid, p_barn_id uuid, p_value boolean)` — sets `can_instruct` on a single `barn_memberships` row. `SECURITY DEFINER`; verifies the caller is a manager of `p_barn_id` then updates only the `can_instruct` column. `EXECUTE` revoked from `PUBLIC` and granted to `authenticated`.

`set_instructor_cut(p_barn_id uuid, p_value numeric)` — sets `instructor_cut` on a single `barns` row. `SECURITY DEFINER`, mirroring `set_can_instruct`, since there's no broad `UPDATE` RLS policy on `barns`; verifies the caller is a manager of `p_barn_id` (`auth_is_barn_manager`) then updates only the `instructor_cut` column. `EXECUTE` revoked from `PUBLIC` and granted to `authenticated`. Used by `setInstructorCut` in `barns.ts`, called from the Manage Barn settings page.

`cancel_rider_participation(p_lesson_id uuid, p_barn_id uuid, p_rider_id uuid, p_notes text, p_is_late boolean)` — cancels a single rider's participation in a lesson: sets `lesson_riders.cancelled_at=now()` and overwrites `rider_notes` with `p_notes`, and (when the effective `isLate` is `false`) zeroes the parent lesson's `fee` and nulls its `payment_type`. `EXECUTE` is granted to `authenticated`, so the RPC is reachable directly from a client, not only through the Next.js server action — to prevent a self-cancelling rider from spoofing the 24-hour fee policy via a direct call, `p_is_late` is trusted as-is only when the caller is acting on someone else's behalf (manager or the instructing trainer); when the caller is cancelling their own participation, the RPC ignores `p_is_late` and recomputes it itself from the lesson's actual `lesson_at`. `SECURITY DEFINER`, since `lesson_riders` RLS grants riders only `SELECT`/`INSERT` (no `UPDATE`); authorizes the caller inline as the barn manager (`auth_is_barn_manager`), the trainer instructing this lesson (`auth_is_barn_trainer` + a `barn_memberships` row matching `id = lessons.instructor_id` and `user_id = auth.uid()`, since `instructor_id` is itself a membership id), or the rider themself (`barn_memberships.user_id = auth.uid()` for the row `p_rider_id` — `rider_id` is a membership id, not an auth user id). Rejects if the lesson is not found, the whole lesson is already cancelled, the caller is unauthorized, or the target participation row doesn't exist or is already cancelled. `EXECUTE` revoked from `PUBLIC` and granted to `authenticated`. Called by `cancelRiderParticipation` in `lesson-participants.ts`.

`teardown_dev_barn_lessons(p_barn_id uuid)` — dev-only helper that deletes all `lesson_riders`, `lesson_horses`, and `lessons` rows for a barn in a single transaction, so the deferred participant-count triggers see the lesson rows gone at commit and skip enforcement. `SECURITY DEFINER`; `EXECUTE` revoked from `PUBLIC` and granted to `service_role` only. Called by `teardownBarnData` in `scripts/script-utils.ts`.

`teardown_all_lesson_data()` — dev-only helper that deletes all `lesson_riders`, `lesson_horses`, and `lessons` rows across all barns in a single transaction, satisfying the deferred participant-count triggers at commit. `SECURITY DEFINER`; `EXECUTE` revoked from `PUBLIC` and granted to `service_role` only. Called by `teardownAllData` in `scripts/script-utils.ts`.

`get_horse_exertion_summary(p_barn_id uuid, p_since timestamptz)` — returns one row per horse in the barn with `is_available` and aggregated `lesson_count`, `total_exertion`, and `jumping_count` for lessons on or after `p_since`, excluding cancelled lessons (`cancelled_at IS NULL`). Uses a subquery JOIN + GROUP BY so horses with zero in-window lessons appear with zero counts. `SECURITY INVOKER`; `EXECUTE` granted to `authenticated`. Used by `getHorseExertionSummary` in `horses.ts`.

`get_horse_projected_exhaustion(p_horse_id uuid, p_barn_id uuid, p_target_date timestamptz, p_exclude_lesson_id uuid DEFAULT NULL)` — returns `(lesson_at, exertion_level)` rows for a horse's non-cancelled lessons within `[p_target_date - 3 days, p_target_date + 3 days]`, optionally excluding `p_exclude_lesson_id` (used by the lesson edit form so the lesson being edited doesn't count against its own horses' exhaustion). Returns only these two columns — no rider, fee, or instructor data — so a trainer can read exhaustion contributed by lessons they don't instruct without gaining broader access to those lessons' details. `SECURITY DEFINER`; the function body itself checks the caller is an active member of `p_barn_id` (`RAISE EXCEPTION 'not_authorized'` otherwise, mirroring `create_managed_member`'s inline check) since `EXECUTE` is granted broadly to `authenticated` and there's no RLS to fall back on. `EXECUTE` revoked from `PUBLIC` and granted to `authenticated`. Used by `getHorseProjectedExhaustion` in `horses.ts`.

`create_agreement_with_first_charge(p_barn_id, p_rider_id, p_horse_id, p_fee, p_kind, p_cadence, p_start_date)` — atomically inserts an `agreements` row and its first `agreement_charges` row in one transaction. `p_start_date` defaults to `current_date`. The first charge's `period` is the current month when `cadence = 'monthly'`, or the agreement's `start_date`'s month when `cadence = 'one_time'`. Works for both `kind` values (`lease`, `board`); the `kind`/`cadence` invariant ("board is always monthly") is enforced by a table `CHECK` constraint, not by this function. `SECURITY INVOKER`; the caller's own manager RLS policy authorizes the insert; `EXECUTE` granted to `authenticated`.

`generate_agreement_charge(p_agreement_id, p_barn_id, p_period)` — atomically generates a recurring `agreement_charges` row via a single `INSERT ... SELECT ... ON CONFLICT (agreement_id, period) DO NOTHING`, so the charge's `fee` is read from `agreements` and written in one statement (no read-then-write race with a concurrent `updateAgreement`) and it never overwrites an already-existing (possibly paid) charge for that period. Both the insert and the select-back filter on `p_barn_id`, so a mismatched barn raises `agreement not found` instead of returning another barn's data. `SECURITY INVOKER`; `EXECUTE` granted to `authenticated`. Used by `generateChargeForMonth` in `agreements.ts`. Called nightly for every active `cadence='monthly'` agreement (both kinds) by `scripts/generate-agreement-charges.ts`/`.sh` (`.github/workflows/generate-agreement-charges.yml`) with `period` = today — since the RPC normalizes to the 1st of that month and no-ops on conflict, running nightly (rather than only on the 1st) makes the job self-healing against missed runs with no extra date-range logic; `one_time` agreements are excluded by the `cadence` filter alone.

`create_expense_with_horses(p_barn_id, p_expense_date, p_recipient, p_applies_to_all_horses, p_expense_time, p_amount, p_expense_type, p_notes, p_horse_ids[])` — atomically inserts a `horse_expenses` row and its `expense_horses` junction rows in one transaction, avoiding an orphaned expense row if the junction insert fails. The junction insert is skipped when `p_applies_to_all_horses` is true. `SECURITY INVOKER`; relies on the caller's own manager RLS policies; `EXECUTE` granted to `authenticated`. Used by `createExpense` in `expenses.ts`.

`update_expense_with_horses(p_expense_id, p_barn_id, p_expense_date, p_recipient, p_applies_to_all_horses, p_expense_time, p_amount, p_expense_type, p_notes, p_horse_ids[])` — atomically updates the `horse_expenses` row and replaces its `expense_horses` junction rows (delete then insert) in one transaction, avoiding the partial-write window where a concurrent read (or a failed insert) would see zero horse assignments. `SECURITY INVOKER`; `EXECUTE` granted to `authenticated`. Used by `updateExpense` in `expenses.ts`.

`create_or_update_notification(p_user_id, p_barn_id, p_type, p_title, p_body, p_link)` — atomically upserts a `notifications` row via `INSERT ... ON CONFLICT (user_id, barn_id, type) DO UPDATE`. `SECURITY DEFINER`, since Postgres requires the `DO UPDATE` clause to satisfy the table's `notifications_update_own` (self-only) UPDATE policy even when no conflict actually occurs — this made every cross-user notification (`lesson_cancelled`, `pending_approval`, etc.) fail RLS outright when called directly against the table. Since `EXECUTE` is granted to `authenticated` (reachable directly, not only through the Next.js server action), the function requires the caller to be an active member of `p_barn_id` — this checks the caller's own membership, not the recipient's, so it doesn't reintroduce a "recipient not guaranteed active" gap; beyond that it matches `notifications_insert_authenticated`'s existing permissive-by-design trust model (the calling server action has already authorized the recipient, so no further per-type or per-recipient check is added). `EXECUTE` revoked from `PUBLIC` and granted to `authenticated`. Used by `createNotification` in `notifications.ts`. The membership check reads `auth.uid()`, which is `NULL` under a service-role client (no user JWT) — so a script calling `createNotification` always gets `not_authorized`; scripts writing notifications upsert directly against the table instead (see `generate-outstanding-notifications.ts`), matching `scripts/CLAUDE.md`'s guidance for RPCs with auth checks that block service-role callers.

## Feature anatomy

Canonical file-touch sequence for any new feature:

1. Schema migration (`supabase/migrations/`)
2. RLS migration (separate file in `supabase/migrations/`)
3. `src/lib/db/<domain>.ts` — data access function(s)
4. Action (`src/app/actions/` or co-located `actions.ts`)
5. Component / page
6. Tests (written first — TDD)

## Coverage + null safety

CI enforces 100% branch coverage via `scripts/check-coverage.sh`. Handle all branches during implementation; do not leave gaps for the coverage script to catch. Null-check at all runtime boundaries (user input, Supabase responses, external API results).
