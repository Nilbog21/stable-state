# `profiles.ts`

User profiles; `upsertProfile` (called at registration); `getProfileByUserId(userId, client?)`
(single-user lookup by auth user ID; `client?` added #505 so `/demo`'s `createOrResumeDemoBarn` can run this check against the request-scoped client before any service-role work happens); `getProfileById(profileId, client?)`
(single-profile lookup by `profiles.id` — used where the caller only has a `barn_memberships.profile_id`, e.g. the member detail page, so a managed/stub member with `user_id IS NULL` still resolves); `getProfilesByUserIds(userIds)` — bulk lookup by auth user ID array, returns empty array when list is empty; `updateProfile` (updates all five editable fields: first_name, last_name, phone, emergency_contact_name, emergency_contact_phone); `updateContactInfo` (updates phone/emergency contact fields; RLS enforces own-row for users, managed/stub profiles only for managers); `updateProfilePhotoPath(profileId, photoPath: string | null, client?)`
(#1004) — plain `profiles` column update, not an RPC, mirroring `horses.ts`'s `updateHorsePhotoPath`; `replaceProfilePhoto(profileId, barnId, file, ext, client?)`
(#1004, `client?` added #1038 so `seed-barn.ts` can seed a profile photo outside a request scope) — same re-read-current/upload/update-with-rollback/best-effort-cleanup-old-object shape as `replaceHorsePhoto`; `barnId` here is only used to build the `{barn_id}/profile-photos/{profile_id}/...` storage path (profiles have no `barn_id` column of their own) — the caller passes whatever barn context the upload happened under, since a profile's photo is visible across all the member's barns regardless of which one it was uploaded from; `removeProfilePhoto(profileId, client?)`
(#1004) — same fresh-read/no-op-if-absent/null-and-remove shape as `removeHorsePhoto`
