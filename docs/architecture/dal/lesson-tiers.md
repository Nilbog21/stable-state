# `lesson-tiers.ts`

Tier CRUD: `getTiersByBarn`, `createTier` (accepts optional `defaultExertionLevel`/`defaultJumping`/`instructorCut` — the last, added in #776, defaults to `0`), `updateTier` (accepts optional `default_exertion_level`/`default_jumping`/`instructor_cut` in updates), `deactivateTier`, `reactivateTier`, `setDefaultTier`, `getAllTiersByBarn` (incl. inactive), `getTierById`
