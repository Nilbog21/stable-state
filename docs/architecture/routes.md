# Routes

Protected barn routes (dashboard, lessons, horses, riders, finances, settings) live in a `(protected)` route group under `src/app/barn/[slug]/(protected)/`. The group layout (`layout.tsx`) centralises auth: absent or non-active membership redirects to `/barn/[slug]/login`. Public routes (login, register) stay outside the group and are unaffected.

The app enforces a ≥44px minimum tap target on every primary interactive control, with no size exception — `Button`'s `md` and `sm` sizes (`min-h-11` on both), `Pill`, `UserMenu`'s avatar, `NotificationBell`'s bell trigger, `BarnSwitcher`'s caret, and `NavDrawer`'s hamburger all carry this floor.

The `(protected)` layout renders a persistent role-aware nav bar above `{children}` on every barn page, responsive at the `md` breakpoint (768px, Tailwind default). The barn name is rendered via a `BarnSwitcher` Client Component (`font-semibold`). For single-barn members it renders as a plain `BlockingLink` home link. For multi-barn members it renders the barn name link plus a caret button (≥ 44 px tap target) that opens a dropdown listing all active barn memberships; the current barn is marked with a checkmark, others link to their `/barn/[slug]` dashboards; the dropdown dismisses on outside click/touch (via the shared `useOutsideDismiss` hook, `src/components/useOutsideDismiss.ts`, which owns the open-state/ref/listener trio for all three nav dropdowns and `ExhaustionBar`'s tap-to-expand popover) or on any link click. The barn name element appears before the section links:
- manager: {Barn Name} (home), Lessons, Expenses, Horses, Leases, Boarding, Members, Finances, Manage Barn, Guide — 9 section links
- trainer: {Barn Name} (home), Lessons, Horses, Members, Guide — 4 section links
- rider: {Barn Name} (home), Lessons, Horses, Members, Guide — 4 section links

At `md` and up, section links render inline in the bar via a `DesktopNavLinks` Client Component. Below `md`, they're hidden from the bar and collapse behind a `NavDrawer` Client Component: a hamburger (☰, ≥ 44 px tap target) that opens a left slide-out drawer (`role="dialog"`, `aria-modal`) listing the same server-built `navLinks` array vertically — no client-side role logic, the layout remains the sole source of truth for role→links. The drawer closes on link tap, scrim tap, Escape, or a `usePathname()` change (since the `(protected)` layout persists across client-side route changes, this closes the drawer if it was left open after a browser back/forward navigation), and moves focus into itself on open. Drawer links use `BlockingLink` so the unsaved-changes guard still fires.

Both `DesktopNavLinks` and `NavDrawer` highlight the link matching the current page via the shared `isNavLinkActive(currentPath, href)` helper (`nav-active.ts`): a link is active when `usePathname()` equals its `href`'s pathname or is nested under it (so `/lessons/[id]` highlights "Lessons"), and — only for links whose `href` carries a query string, i.e. Leases (`?kind=lease`) vs. Boarding (`?kind=board`), which otherwise share the `/agreements` pathname — the current `useSearchParams()` value must match too. The active link gets `aria-current="page"` plus bolder/darker styling; inactive links are de-emphasized in grey.

A `UserMenu` Client Component and a `NotificationBell` Client Component sit on the right side of the nav bar in a flex container — avatar then bell at `md` and up (unchanged), reordered to bell then avatar below `md` via CSS `order` utilities to match the mobile bar's `☰ · barn name · spacer · bell · avatar` layout. `UserMenu` shows the user's initials (first letter of `first_name` + first letter of `last_name` from `profiles`; falls back to first character of email, then `?`). Clicking it opens a dropdown with: full name + email (non-clickable header), a "Profile" link to `/profile?barn=<slug>`, a "Switch Barn" link to `/barns` (only when the user has >1 active barn membership), a "User Guide" link to `/barn/[slug]/guide`, and a Sign Out button. The dropdown closes on outside click or touch (same shared `useOutsideDismiss` hook). `NotificationBell` shows a bell icon with an unread-count badge (hidden when zero); clicking opens a dropdown listing recent notifications (title, body, timestamp) with links via the `link` field and a "Mark all read" button that calls `markAllNotificationsReadAction` and refreshes. Notification data is fetched server-side in the layout via `getNotifications`; interactivity is client-side.

"Horses" → `/barn/[slug]/horses` (all roles)
"Leases" → `/barn/[slug]/agreements?kind=lease` (manager only)
"Boarding" → `/barn/[slug]/agreements?kind=board` (manager only)
"Manage Barn" → `/barn/[slug]/settings` (manager only)

| Route | Roles | Notes |
|---|---|---|
| `/` | All | Unauthenticated users are redirected to `/login`; authenticated users are redirected server-side using barn membership logic: single active → `/barn/[slug]`, multiple active → `/barns`, no memberships → `/login?no_barns=true` |
| `/barns` | Authenticated users | Barn selector: one card per membership, showing barn name + role; no memberships redirects to `/login?no_barns=true` |
| `/barn/[slug]` | All active members | (#1015) All roles see a single-day calendar — the **Day view** — built on `getScheduleForRange` (#1013/#1014), replacing the old client-split Today/This-Week lesson list. The selected date is a `?date=YYYY-MM-DD` search param (validated server-side via `isValidDateString`; missing or malformed falls back to "today" in the barn's own zone); Prev/Next/Today are plain SSR `<Link>`s, no client JS, mirroring the mobile-first/no-hover-only convention. "Today" is computed from `barns.timezone` (`instantToLocalWallClock`, same convention `getOutstandingExpenses` uses), not the viewer's own zone; the selected date's UTC day-boundary instants are derived via `wallClockToInstant` (`src/lib/barn-timezone.ts`, the reverse of `instantToLocalWallClock`) before calling `getScheduleForRange`. Role scoping: manager sees the full barn; rider sees only enrolled lessons and role-visible `barn_events` (both via RLS, same as `getScheduleForRange` itself); trainer sees only lessons they instruct — `getScheduleForRange` has no manual role dispatch (RLS alone would let a trainer see the whole barn's lessons, since `lessons_select_staff` grants trainer the same barn-wide SELECT as manager), so the page applies `schedule.ts:scopeScheduleItemsForRole` on top, reproducing the "own scope" restriction the old `getUpcomingLessons` used to apply via its `instructor_id` filter. Expenses shown are "planned" only (`amount IS NULL`, matching the pre-#1015 dashboard, not every timed expense `getScheduleForRange` itself returns) and are manager-only (RLS has no trainer/rider grant on `horse_expenses`); `barn_events` are shown filtered by `visible_to_roles` (RLS). The shared `CalendarDayView` component (`src/components/calendar/`, day-cell rendering — also the reusable piece #1016's Week view builds on) renders each day's items via `CalendarLessonCard`/`CalendarExpenseCard`/`CalendarEventCard` (moved+renamed from the old dashboard-local `UpcomingLessonCard`/`UpcomingExpenseCard`, plus a new event card — no event detail page exists, so it renders as a plain non-link `Card`); rider cards show a "Cancelled" badge when their own `lesson_riders.cancelled_at` is set (whole lesson not already cancelled). A cancelled lesson (`cancelled_at IS NOT NULL`) does not appear at all — `getScheduleForRange` excludes it at the query level, a deliberate behavior change from the pre-#1015 dashboard (which showed it with a "Cancelled" badge), accepted since the shared function's own design already made that call for #1013/#1014. (#1016) A pill-style Day/Week switcher (`Pill`, `src/components/ui/Pill.tsx`) sits above the Prev/Next/heading row; a `?view=week` search param (default `day`, any other value falls back to `day`) selects the **Week view**, a rolling 7-day window running `selectedDate` through `selectedDate+6` — not a calendar-aligned Sun–Sat week, matching the pre-#1015 dashboard's old "This Week" behavior (`now` → `now+7d`). Prev/Next step by 7 days instead of 1 in week mode (both carry `view=week`), and the heading shows the range's start/end dates with a "· Today" suffix whenever today falls anywhere inside the visible week, not just on an exact match. The Week view reuses every Day view data path unchanged — the same `getScheduleForRange`/`scopeScheduleItemsForRole` call, just widened from a 1-day to a 7-day instant range, and the same batch `getLessonsByIds`/`getExpensesByIds`/`getEventsByIds` hydration — then buckets the result into one 7-day-wide `dayScheduleItems.ts:groupScheduleItemsByDay` call (`src/components/calendar/dayScheduleItems.ts`, wraps `mergeDayScheduleDisplayItems` once per day, bucketing on `ScheduleItem.start`'s date prefix — already barn-local, no extra zone conversion needed) instead of `mergeDayScheduleDisplayItems` directly. The new `CalendarWeekView` component (`src/components/calendar/`) renders a vertical stack of 7 day sections (mobile-first — a side-by-side 7-column grid doesn't fit a ~400px screen), each reusing `CalendarDayView` for a non-empty day and a compact one-line "Nothing scheduled for this day." for an empty one (a full illustrated empty state repeated 7× would be too heavy); the single full `EmptyState` only appears when every day in the week has zero items. All roles (#773) also see a single **Reminders** section header wrapping whichever of the following apply to them, hidden entirely when none do: one card per due document (manager only, no separate sub-heading — rendered inline in the same list as the other Reminders cards) — every barn document (horse/trainer/rider) with `reminder_date <= today` via `getDueDocuments` (server-fetched with a UTC-day cutoff), re-filtered client-side (`DocumentRemindersSection`, local timezone, same tradeoff as `isSameLocalDay`) against the viewer's local day so a document doesn't flash in a day early — same amber `Button` `warning` variant as the other Reminders cards, each card a single line reading "{owner} — {record type} — {date}" (no split/justified layout), hidden entirely when empty, each entry linking to the owning horse's or member's detail page; no cron involved — this is Option A from #530, surfaced passively on login rather than via a scheduled job; trainer/rider dashboards don't call `getDueDocuments` at all (manager-only in this cut) — and two role-scoped unpaid-income cards, "N unpaid lessons" and "N unpaid leases/boarding" (each hidden individually when its own count is zero), both linking to `/barn/[slug]/finances/outstanding` and backed by the same `getOutstandingLessons`/`getOutstandingCharges` calls (`outstanding.ts`/`agreement-finances.ts`) the outstanding page itself uses, scoped by the viewer's own role — this is trainer's and rider's only nav path to that page, since neither role has a Finances nav link. The "N unpaid lessons" card's count also folds in `getOutstandingCancellationFees` (#938 — a rider/trainer with only an outstanding late-cancellation fee and zero unpaid lesson fees still gets a dashboard reminder), per the same decision as #831's Outstanding page: a cancellation fee is lesson-adjacent debt, disambiguated by the Type column once clicked through rather than a separate card. The Reminders section's own visibility is computed from server-known counts (including the raw, pre-client-filter due-documents count), accepting the same small local-timezone-boundary tradeoff already inherent in `DocumentRemindersSection`'s own re-filter. The empty state (`EmptyState`, "You're all clear") is shown, not hidden, when nothing is scheduled for the selected day; nav links rendered by layout |
| `/barn/[slug]/lessons` | All active members | Lessons split at 7-day cutoff: recent shown immediately, older behind `OlderLessonsToggle`; each lesson renders as a full-width `Card` link to its detail page (`LessonListItem`, matching the Agreements list pattern) — no Cancel affordance lives on this list (#825, consolidated onto the lesson detail page's header, see below); `LessonListItem` no longer takes a `currentMembershipId` prop, only `viewerMembershipId` (still used to resolve the viewing rider's own row for the badge below); cancelled lessons show a "Cancelled" badge, and a rider whose own participation is cancelled (but the lesson itself isn't) sees the same badge on their own row; a normal lesson's rider name is shown to manager/trainer only — hidden for the rider role, since on their own self-scoped list it's always their own name (#767); role-aware filter bar above the list, mobile-wrapping (`flex-wrap`, no horizontal scroll) — manager/trainer see `My Lessons \| All \| By Instructor \| By Rider \| By Horse \| By Tier` (`My Lessons`, filtering to `instructor_id === own membership id`, is the default for trainer only when no `?filter=` param is present — manager isn't guaranteed to be an instructor, so manager defaults to `All`, #1025); rider sees `All \| By Instructor \| By Horse \| By Tier` (no `My Lessons` or `By Rider` — their list is already self-scoped), defaulting to `All`; selecting `By Instructor`/`By Horse`/`By Tier` (and, for manager/trainer, `By Rider`) reveals a second row of specific people/horses/tier names — trainer sees all barn instructors, not just themselves; `By Tier` (#948) filters to lessons matching the picked `tier_name` exactly, options built from the distinct tier names present across the caller's own already-role-scoped lesson list (not a barn-wide tier fetch), so a tier with zero lessons visible to this caller never appears as a filter option; active filter persists in URL params (`?filter=mine\|all\|trainer\|rider\|horse\|tier&id=<uuid>`, unchanged despite the "By Instructor" label — a tier filter's `id` is the URL-encoded tier name, not a UUID); the `rider` filter's application (not just its pill) stays gated to manager/trainer server-side, matching every other role-restricted pill; the filter bar itself is hidden entirely when the barn has zero lessons at all (nothing to filter), but stays visible whenever the *current filter* yields zero results despite the barn having lessons elsewhere — that case shows "No lessons match this filter" instead of the barn-empty "No lessons yet" copy |
| `/barn/[slug]/lessons/new` | manager, trainer | A "Recurring (weekly)" checkbox atomically creates a `lesson_series` template row alongside the first lesson (via `createLessonSeries`) instead of a plain one-off lesson. `LessonForm` also snapshots `instructor_cut` into a hidden field (#776) for display/back-compat, but the value that actually lands in the DB is re-derived server-side by the RPC from the selected tier's rate, or `barns.default_instructor_cut` for a Custom lesson — not user-editable here, only on the tier itself, and not trusted from the client even via a direct RPC call. `LessonForm`'s horse picker (shared with the edit form below) re-fetches projected exhaustion for exactly the horse IDs it's rendering (its `horses` prop, not a barn-wide fetch) via `getProjectedExhaustionForBarn` whenever the lesson date/hour changes, and shows an `ExhaustionBar` under each horse with a live ghost segment reflecting that horse's currently selected exertion level — no bar shown until the data fetched for the currently-selected date has resolved (so changing the date doesn't briefly display the previous date's numbers), no bar shown for an inactive or unavailable horse the lesson isn't currently assigned to (#766 — the fetch still requests exhaustion for those horses, only the bar is suppressed; a horse that's still checked on the lesson being edited keeps its bar regardless of active/available status, preserving #426's original fix), and no bar shown once the lesson's date/hour is in the past (#766, compared against the current hour rather than the exact minute, so "now" never itself counts as past); the picker list itself is sorted checked horses first, then available, then inactive/unavailable last, each bucket ordered least-to-most exhausted by that horse's own fetched exhaustion total (#766); the edit form binds the same action with `p_exclude_lesson_id` set to the lesson being edited so it doesn't count against its own horses, and passes the horse IDs of `horsesForForm` (including any horse deactivated after being assigned to this lesson) — that horse stays checked by default (from `initialLesson.lesson_horses`), so it's still fetched, sorted first, and keeps its bar; only sorts into the inactive/unavailable bucket (and loses its bar) if manually unchecked; the horse-picker fieldset legend reads "Horse" for a **normal** lesson (exactly one required) vs. "Horses (select at least one)" for a **group** lesson; the Payment Type field is hidden entirely whenever the fee is `0` — `payment_type` is simply omitted from the submission (parses to `null`/unpaid), which is sufficient to read as "automatically collected" since `getOutstandingLessons`'s `fee ≠ 0` filter already excludes `$0` lessons from Outstanding regardless of `payment_type` |
| `/barn/[slug]/lessons/[id]` | All active members | `notFound()` if the lesson doesn't exist, is in a different barn, or (rider only) the caller has no `lesson_riders` entry on it — defense-in-depth on top of the enrollment-scoped RLS; Edit link visible to managers and to the instructing trainer only (a trainer viewing a lesson they don't instruct doesn't see it, since trainers can now see every barn lesson on the list page, not just their own); the page is now purely read-only for every note field (#797) — Horse Notes, Rider Notes, Private Notes, a rider's own "Your Notes", and (whole-lesson and per-rider) Cancellation Notes are all hidden entirely (label + text) whenever the underlying value is null/empty, for every role, rather than rendering an empty label or a "—" placeholder; trainer/manager see non-empty `horse_notes` inline under each horse entry and non-empty `rider_notes`+`private_notes` (visually distinguished) inline under each rider entry; each horse entry's exertion rating is manager/trainer-only (#937, closing a gap `getLessonById`'s `lesson_horses` select left open — it no longer selects `exertion_level` directly for any role, not just hiding it in the UI for a rider caller); `exertion_level` has no column-level GRANT restriction on `lesson_horses` for `authenticated`, so a per-role select string alone can't stop a rider's own session from reading it directly via PostgREST (a #937 review follow-up caught this — the initial fix only closed the app's own code path); `getLessonById` instead resolves it via `get_lesson_horse_exertion_levels` (see [`rpc.md`](rpc.md)), a `SECURITY DEFINER` RPC gated the same way `get_horse_exertion_summary`/`get_horse_projected_exhaustion` already are, called only for a manager/trainer caller and merged onto each `lesson_horses` row by `horse_id`; rider sees own non-empty `rider_notes` read-only; a red "Cancelled" badge appears next to the lesson type/Jumping badges whenever `lesson.cancelled_at` is set, independent of whether `lesson.cancellation_notes` has content — `lesson.cancelled_at` can be set either directly (whole-lesson cancel) or by cascade from `cancel_rider_participation` (see `cancel_rider_participation` in [`rpc.md`](rpc.md)) when the last active rider is cancelled; the "Cancellation Notes" row itself is keyed off `lesson.cancellation_notes` being non-empty (decoupled from `cancelled_at`) and is always read-only now — editing moved to the Edit Lesson form's Notes section (see below); each rider row shows only a read-only "Cancelled" badge when that rider's participation is cancelled (#825 — the per-row "Cancel" link that used to sit next to a non-cancelled rider's name, linking to `/barn/[slug]/lessons/[id]/cancel-rider/[rider membership id]`, was removed from this list entirely) plus that rider's `cancellation_notes` when non-empty, read-only text visible to every role regardless of who entered it or whose row it's on (unlike `rider_notes`/`private_notes`, which stay own-row/staff-only masked — a cancellation reason is administrative context for the whole lesson, not private) — the DAL never nulls `lesson_riders.cancellation_notes` for the rider role (#741 follow-up); rider sees the same badge for their own row only, which stays visible even when that rider has no notes to show (only the notes label/text hides, never the badge); in place of every removed per-row link, the header now shows a single "Cancel" button (#825) next to Edit/Delete: visible to the manager or instructing trainer (same eligibility as the whole-lesson cancel below) linking to `/barn/[slug]/lessons/[id]/cancel`, or to the enrolled rider for their own still-active participation (same eligibility, plus their own `lesson_riders.cancelled_at IS NULL`) linking to `/barn/[slug]/lessons/[id]/cancel-rider/[own membership id]` — manager also sees a Delete entry point next to Cancel/Edit, reachable regardless of lesson state (past/future, paid/unpaid, already cancelled) — hard-deletes the lesson via `deleteLessonAction`/`deleteLesson`, unlike Cancel this leaves no record (beyond `transactions` rows, see below) and fires no notifications; when the lesson is ledger-uncollected (`lesson.fee !== 0 && lesson.payment_type === null` — a $0 fee is always ledger-collected regardless of `payment_type`, per `sync_lesson_transactions`'s own `v_collected` formula, corrected in a #827 review follow-up from checking `payment_type === null` alone), this is still the bare `DeleteLessonButton` (`window.confirm`-gated, mirrors `StopSeriesButton`) straight to `deleteLessonAction`; when ledger-collected, it's instead a link to `/barn/[slug]/lessons/[id]/delete` (#827) — a `DeleteLessonButton` confirm dialog can't show the collected-fee tradeoff a `transactions`-aware delete now has; above the card, a `HorseStatusBanner` (#848, shared with the edit page below) renders whenever `getHorseAttentionReasons` (`src/lib/lesson-authorization.ts`) returns a non-empty list — the same future/non-cancelled/inactive-or-unavailable condition #847's badge uses, evaluated from this page's own already-fetched `lesson.lesson_horses[].horses.is_active`/`is_available`/`unavailability_reason` (no extra query) — naming each affected horse and why ("Buttercup is inactive" / "Rocky is unavailable: lame — resting per vet"); purely informational, visible to every role, never blocks Edit/Cancel/Delete |
| `/barn/[slug]/lessons/[id]/delete` | manager | Delete confirmation page (#827, mirrors `/barn/[slug]/expenses/[id]/delete`), reached only from the lesson detail page's Delete entry point when the lesson is ledger-collected (`lesson.fee === 0 \|\| lesson.payment_type !== null`); `notFound()` if the lesson doesn't exist; shows the collected `$fee` amount and an unchecked-by-default "Also delete the collected $fee record from Finances" checkbox (`alsoDeleteTransactions`) — unchecked (the default) keeps the `lesson_fee`/`instructor_payout` `transactions` rows after the lesson is gone (they survive via the table's own `ON DELETE SET NULL (lesson_id)` FK), checked opts into deleting them too; submits to `deleteLessonAction`, which reads the checkbox and forwards it to `deleteLesson`'s `deleteCollectedTransactions` param |
| `/barn/[slug]/lessons/[id]/edit` | manager, trainer | Pre-filled edit form; instructor field hidden entirely for trainers (value still submitted/locked server-side, DB enforces via RLS) instead of shown read-only; group→normal downgrade shows warning and requires manager to select one rider/horse to keep; updates are atomic via `update_lesson_with_participants` RPC; only managers can add new horses or riders inline; notes editing is embedded inside `LessonForm` via `initialNotes` prop and saved by `updateLessonAction` via the single Save button; when the lesson being edited is already cancelled (`initialLesson.cancelled_at !== null`), the Notes section also gains a Cancellation Notes textarea (#797, replacing the old inline-editable field on the detail page), pre-filled from `initialLesson.cancellation_notes` and persisted by `updateLessonAction` alongside the horse/rider notes writes, under the same manager/instructing-trainer authorization already enforced by this page — not shown at all for a non-cancelled lesson; navigating away with unsaved notes shows a confirmation prompt; when `lesson.series_id` is set and the linked `lesson_series.is_active` is `true`, shows a "This is part of a recurring series" indicator plus a destructive **Stop Recurring Lessons** button (`StopSeriesButton`, confirm dialog, mirrors the agreements page's `EndAgreementButton`) — for a trainer, the button is only rendered when `lesson_series.instructor_id` matches their own membership id, since a lesson's current instructor (gating page access) and its series' original instructor (gating the stop write) can diverge after a manager reassigns the lesson; on submit, `stopLessonSeriesAction` sets `lesson_series.is_active=false` (manager: any series; trainer: only a series where `instructor_id` is their own membership, matching the RLS `lesson_series_update_trainer` policy) without touching any `lessons` rows, then redirects back to this page; below the heading (and above the recurring-series indicator), the same `HorseStatusBanner`/`getHorseAttentionReasons` pair as the detail page above (#848) renders under the identical condition — informational only, doesn't affect Save; `LessonForm` also receives `hasHorseIssue` (#849, `attentionReasons.length > 0`, reusing the same computed value) and OR's it into its existing nav-blocker `shouldWarn` check alongside unpaid/notes/field dirtiness — leaving the page (nav link, browser back, tab close) prompts a confirm dialog even when no form field was touched, and stops prompting once the flagged horse is swapped out and saved, since `attentionReasons` is recomputed fresh from the DB on the next page load |
| `/barn/[slug]/lessons/[id]/cancel` | manager, trainer (own lesson only) | Sole entry point is the single "Cancel" button on the Lesson Detail header (#825, replacing the former per-row Cancel link on the Lessons list). Confirmation form: optional cancellation-notes textarea + destructive "Confirm Cancellation" button; `notFound()` if the lesson doesn't exist, the trainer doesn't own it, or it's ineligible (already cancelled, or past-and-paid); the "Cancelled by Rider" / "Cancelled by Instructor" type radio (`CancelLessonFields`, a Client Component) now always renders, for both **normal** and **group** lessons alike (#825 — previously group lessons showed no radio at all) — for a **normal** lesson (1 rider), cancelling the lesson and cancelling that rider's participation are the same event, so the identical late-fee policy applies (`isLate = cancel_type==='rider' && lesson_at − now() ≤ 24h`); for a **group** lesson, selecting "Cancelled by Instructor" keeps the pre-#825 whole-lesson behavior (always fee-waived, confirmation text lists the count and names of enrolled, not-yet-individually-cancelled riders so the blast radius is visible before confirming), while selecting "Cancelled by Rider" instead reveals a single-select "Rider" fieldset (one radio per still-active enrolled rider, each marked `required`) and swaps the description to a short "select a rider" prompt; when "Cancelled by Rider" is selected on a lesson booked less than 24 hours out (#944, `CancelLessonFields.tsx`, the late-fee window recomputed live client-side against the lesson's actual `lesson_at` rather than snapshotted at page load), an amber note appears — on a normal lesson, warning the rider will be due a late cancellation fee; on a group lesson, warning instead that no late fee is currently charged for group lessons at all, since `sync_rider_cancellation_fee` no-ops for `lesson_type <> 'normal'` (see [`rpc.md`](rpc.md)) — surfacing that known gap before the manager/trainer confirms rather than leaving it silent; on submit, `cancelLessonAction` reads `cancel_type`/`rider_id` from the form and, only for a **group** lesson with `cancel_type==='rider'` and a non-empty `rider_id`, delegates directly to `cancelRiderParticipationAction` (see below) instead of running its own whole-lesson cancel logic — reusing that action's own eligibility check, RPC call, and notifications rather than duplicating them; otherwise (normal lesson, or group + "Cancelled by Instructor") it proceeds as before: sets `cancelled_at=now()`, saves notes, zeroes `fee`/nulls `payment_type` unless `isLate`, fires `lesson_cancelled` notifications (trainer cancels → barn manager(s) + enrolled riders; manager cancels → instructor + enrolled riders), then redirects to `/barn/[slug]/lessons/[id]` (#825 — changed from the lessons list, matching the new header entry point and `cancelRiderParticipationAction`'s own redirect target) |
| `/barn/[slug]/lessons/[id]/cancel-rider/[riderId]` | manager, trainer (own lesson only), rider (self only) | Cancels a single `lesson_riders` row rather than the whole lesson; reachable in the UI only via the rider's own "Cancel" button on the Lesson Detail header (#825 — the manager/instructing-trainer per-row "Cancel" link that used to also point here was removed from the Riders list; a manager/trainer's group-lesson per-rider cancel now goes through the rider picker on `/cancel` above instead) — the route itself, its auth checks, and its own in-page type selector are otherwise unchanged, so it remains directly reachable (and still fully authorized) for a manager or instructing trainer who navigates to it directly. The rider is fixed by the URL, so there is no in-form rider selector even for group lessons. `notFound()` if: the lesson doesn't exist, the whole lesson is already cancelled, the trainer doesn't instruct it, `riderId` isn't a rider on the lesson, a rider requests someone else's `riderId`, or the target participation is already cancelled or ineligible (`lesson_at > now()` OR `payment_type IS NULL`). Manager/trainer see a type selector — "Cancelled by Rider" / "Cancelled by Instructor" — that overrides the fee policy rather than just labeling who acted: the radio defaults to "Cancelled by Instructor" when the acting manager/trainer is the lesson's own instructor (`isInstructorOfLesson`, `src/lib/lesson-authorization.ts`), else "Cancelled by Rider" (matching prior behavior — this is also what a manager sees when they don't instruct the lesson); "Cancelled by Instructor" always waives the fee (`isLate=false`); "Cancelled by Rider" applies the literal 24-hour policy (`isLate = lesson_at − now() ≤ 24h`, i.e. late/no-notice cancellations keep the fee, earlier ones zero it). A self-cancelling rider always gets the "Cancelled by Rider" policy with no selector shown, and can't spoof the instructor waiver via form data — enforced server-side by the RPC itself, not just the form, since the RPC re-derives `isLate` from the lesson's real `lesson_at` whenever the caller is cancelling their own participation, ignoring whatever `p_is_late` value the client sent. On submit, `cancelRiderParticipationAction` calls `cancelRiderParticipation`, then fires `rider_participation_cancelled` notifications (a distinct type from whole-lesson `lesson_cancelled`, so the two flows don't collide on the same recipient's notification row) keyed on **actor role** (not the type selected): rider self-cancels → instructor + all active managers; manager/trainer cancels on behalf of a rider → the affected rider (skipped if unclaimed/managed with null `user_id`) + all active managers only when the actor is a trainer; when `cancelRiderParticipation` reports a cascade (this was the last active rider — see `cancel_rider_participation` in [`rpc.md`](rpc.md)), also fires `lesson_cancelled` notifications using the same recipient rule as `cancelLessonAction` (trainer cancels → managers + riders; manager/rider cancels → instructor + riders); redirects to `/barn/[slug]/lessons/[id]` |
| `/barn/[slug]/expenses` | manager | Expenses split at 7-day cutoff (same pattern as lessons): recent shown immediately, older behind `OlderExpensesToggle`; each expense renders as a full-card link (`ExpenseCard`, #947 — replacing the former table, matching the Horses/Agreements/Members/Lessons card pattern) to `/barn/[slug]/expenses/[id]`, the shared edit page — there's no separate read-only detail view; card shows date, time (or `—`), recipient, expense type, horse(s) or "Entire Barn" (or `—` if none), and amount (or `—` if not yet entered); "Add Expense" button links to `/barn/[slug]/expenses/new`; no row-level Delete affordance on the list — Delete remains reachable one click further via the edit page's own Delete button; card fields render via the shared expense formatters in `src/lib/format-expense.ts` (`formatExpenseDate`/`formatExpenseTime`/`formatExpenseAmount`/`formatExpenseHorses`), also used by the delete-confirmation page and the dashboard's `UpcomingExpenseCard` (whose `formatExpenseDateTime` now delegates its time segment to `formatExpenseTime`) |
| `/barn/[slug]/expenses/new` | manager | `ExpenseForm`: recipient (required, free-text via native `<input list>`/`<datalist>` seeded from `getRecentRecipients`, most-recent-first), expense type (optional, same autocomplete pattern seeded from `getRecentExpenseTypes`; blank/whitespace normalized to `"Unspecified"` server-side), date (required), time (optional — presence signals a planned/scheduled expense), amount (optional — blank for planned expenses), an "Entire Barn" toggle that disables the horse checkbox list and sets `applies_to_all_horses=true` (native `disabled` semantics exclude `horse_id` from the submitted `FormData`, no extra JS needed), and notes; on recipient blur, `getMostCommonExpenseTypeAction` auto-fills expense type from `getMostCommonTypeForRecipient` and flashes it (`ring-2 ring-blue-400`, same pattern as jumping→exertion flash in `LessonForm`); `createExpenseAction` validates and calls `createExpense`, then redirects to `/barn/[slug]/expenses` |
| `/barn/[slug]/expenses/[id]` | manager | Edit page reusing `ExpenseForm` pre-filled from `getExpenseById` (`notFound()` if missing); same fields as the new-expense form, "Entire Barn"/individual-horse checkbox state restored from `applies_to_all_horses`/`horse_ids`, submit label "Save Changes"; `updateExpenseAction` validates (shared `parseExpenseFormData` helper used by both `createExpenseAction` and `updateExpenseAction`) and calls `updateExpense`, then redirects to `/barn/[slug]/expenses`; a "Delete" button links to `/barn/[slug]/expenses/[id]/delete` |
| `/barn/[slug]/expenses/[id]/delete` | manager | Confirmation page mirroring the lessons cancel pattern: summary of recipient/date/amount + destructive "Confirm Delete" button; `notFound()` if the expense doesn't exist; #941 added an unchecked-by-default "Also delete the collected record from Finances" checkbox (`alsoDeleteTransactions`), shown only when `expense.amount !== null` (a `transactions` row exists whenever the amount is known, written by `sync_expense_transaction`, independent of `payment_type`/collected status — unlike the lesson case, so this checks `amount`, not a paid/unpaid flag) — an expense with no amount set stays a bare confirm with no checkbox; on submit, `deleteExpenseAction` reads the checkbox and forwards it to `deleteExpense`'s `deleteCollectedTransactions` param, which deletes the expense (cascades to `expense_horses`) and redirects to `/barn/[slug]/expenses` |
| `/barn/[slug]/horses` | All active members | Manager/trainer: three categorized sections fed by `getHorseExertionSummary` — **Available** (`is_active=true`, `is_available=true`) sorted by total exertion ascending (±3 days, #936) — card shows name and an `ExhaustionBar` (progress bar, thresholds resolved via `resolveExhaustionThresholds`, anchored to today with no ghost segment; tap to expand the ±3-day lesson breakdown from `getHorseProjectedExhaustion`); **Unavailable** (`is_active=true`, `is_available=false`) — card shows name, unavailability reason, and the same `ExhaustionBar`; **Inactive** (`is_active=false`) — manager only, card shows name only, no bar; manager sees inline Add Horse form (name input + button) in the page header; empty sections hidden. Rider (#765): neither exertion RPC is called at all — Available/Unavailable are built from a plain `getHorsesByBarn` list instead, so cards show name (plus unavailability reason) with no `ExhaustionBar`, matching the RPC-level restriction on `get_horse_exertion_summary`/`get_horse_projected_exhaustion`; no Inactive section (rider never sees inactive horses). All cards are full-card links to `/barn/[slug]/horses/[id]` (`HorseCard`'s `linkable` prop, defaults to `true`). Rider cards were `linkable={false}` from #946 (riders couldn't act on anything a horse detail page showed, so the card was a plain non-interactive block) until #1002 added a barn-wide-visible photo to that page, giving riders a reason to open it — `linkable={false}` is no longer used by any caller, but stays as `HorseCard`'s own tested, supported prop for a future non-navigable variant |
| `/barn/[slug]/horses/[id]` | All active members | Horse detail page; shows horse name, then a **Photo** section (#1002) with a section heading and its controls top-right (mirrors the Documents section's heading/button placement below) — every role sees the horse's single identification photo (signed URL from `horses.photo_path` via `document-storage.ts`'s `getSignedUrl`), scaled to a fixed height without cropping (`h-48 w-auto`, so non-square photos keep their aspect ratio rather than being center-cropped to a square) when set, or the shared `EmptyState` component ("No photo yet") when not; manager-only controls, both in the header row next to the "Photo" title, are plain links (not an inline upload form) to the shared `/barn/[slug]/documents/new?entity=horse&id=<horseId>&type=photo` page (see that route below) — "Set Photo" (empty) or "Replace Photo" + a "Remove" button (present, submits `deleteHorsePhotoAction` directly on this page); `deleteHorsePhotoAction` is thin — `requireMembership` then delegates to `src/lib/db/horses.ts`'s `removeHorsePhoto`, which re-reads the horse's current `photo_path` itself rather than trusting a value the page bound at render time; trainer/rider have no upload/remove controls since photo write stays manager-only (matches the base `horses` permissions matrix — owner write is the separate, blocked #1003 follow-up). Below the photo: current availability status; manager sees `HorseManagerForm` — a single form and single Save button (#759 — previously two independent forms/Save buttons) covering name input, three-state pill status control (Active / Unavailable / Inactive), conditional reason textarea (shown only when Unavailable is selected), and an **Exhaustion Thresholds** section: a "Use barn defaults" checkbox (checked when both `horses.exhaustion_threshold_moderate`/`exhaustion_threshold_high` are `null`) that disables the `moderate`/`high` number inputs when checked (native `disabled`, excluded from `FormData`, same pattern as the expenses "Entire Barn" toggle) — inputs pre-fill from the horse's own override when set, else the barn default; one submit calls `updateHorseAction`, which validates status and (when not using barn defaults) `moderate < high` server-side before writing, then makes a single atomic call to `updateHorseDetails` with both the status/name fields and the resolved thresholds — one `update_horse_details` RPC round-trip, not two separate writes, so a mid-save failure can't leave the name/status persisted while thresholds silently fail; trainer/rider see reason as read-only text when horse is unavailable, and don't see the Exhaustion Thresholds section at all; manager and trainer see a Documents section listing all uploaded files by `record_type` with signed-URL links, and an **Add Document** button (top-right of the section header) linking to the shared `/barn/[slug]/documents/new?entity=horse&id=<horseId>` upload page (see that route below); manager can delete documents, trainer cannot; manager sees an inline editable Reminder Date cell per document (blur-to-save, mirrors `ChargesTable`'s pattern), trainer/rider see it read-only; every role sees a role-agnostic "Reminder Due" badge (`ReminderDueBadge`) next to that cell once `reminder_date <= today`; rider sees no documents section |
| `/barn/[slug]/agreements` | manager | Kind-scoped list (`?kind=lease\|board`, defaults to `lease` when missing/invalid) of agreements of that kind, rendered as full-card links (`Card`, matching the Horses page pattern) to `/barn/[slug]/agreements/[id]` — each card shows rider, horse, fee, and status via the shared `getAgreementStatusLabel` helper (`src/lib/db/agreements.ts`: `is_active=false` → "Ended"; `is_active=true` + `cadence='one_time'` → "Complete", since a one-time agreement's single charge is created at creation and never bills again; `is_active=true` + `cadence='monthly'` → "Active"); no separate row-level Edit link — Edit remains reachable from the detail page; **Add Lease**/**Add Boarding** button top-right; `EmptyState` when the list is empty |
| `/barn/[slug]/agreements/new` | manager | Add form (`?kind=` from the list's Add button): rider select, horse select, fee, start date (default today); lease shows a cadence select (`one_time`\|`monthly`); board hides cadence (forced `monthly`) and pre-fills fee from `getBarnDefaultBoardFee` |
| `/barn/[slug]/agreements/[id]` | manager | Agreement detail page: rider and horse (resolved via `resolveMemberNames`/`resolveHorseNames`), fee, cadence, and status via the shared `getAgreementStatusLabel` helper (see the list route above), plus every charge from `getChargesForAgreement`; each charge row has an inline **Payment Type** dropdown (`ChargesTable` client component, mirrors `OutstandingTable`'s mark-paid pattern — calls `updateChargePaymentTypeAction`, then `router.refresh()`) and an editable **Fee** input (calls `updateChargeFeeAction` on blur, only when changed) for pro-rating a single occurrence; **Edit** button top-right links to `[id]/edit` |
| `/barn/[slug]/agreements/[id]/edit` | manager | Edit form: only fee is editable; rider, horse, start date, and cadence are shown read-only (resolved via `resolveMemberNames`/`resolveHorseNames`) — to change any of those, end the agreement and create a new one; **End Agreement** button (confirm dialog) sets `is_active=false`, hidden once already ended |
| `/barn/[slug]/members` | All active members | Members roster; "You" card (links to detail page) plus Managers + Trainers + Riders sections (self excluded from own section) shown uniformly for every role (#779 — previously trainer saw Riders only and rider saw the "You" card only); data fetched via `getActiveMembersWithProfiles` for all three roles regardless of caller role. Trainers and Riders sections each have an inline Add form (first + last name, manager-only — `{membership.role === 'manager' && ...}`) that calls `createManagedMemberAction` bound to that section's role (`'trainer'`/`'rider'`, not a form field); every row (managed/unclaimed or claimed) renders as one unconditional `Card` link to the member detail page — no branching on `isManaged`/`inviteToken`/caller role for which component to render (#824 — previously a managed/unclaimed row rendered via a separate non-clickable `ManagedMemberRow` component with its own Copy Invite/Revoke controls, now deleted); a manager viewing an `isManaged` row additionally sees an inline amber `Badge` reading "Unlinked" next to the name — gated on `isManaged && membership.role === 'manager'` alone, not on `inviteToken` being non-null (a deliberate simplification, since a manager never actually sees an `isManaged=true` row with a null `invite_token` in practice: that state only arises on the RPC-sourced "gap" rows #779 adds for a caller's out-of-scope reads, e.g. a rider viewing the Trainers/Managers sections, which are already excluded by the `role === 'manager'` half of the condition); trainers/riders never see the badge, matching pre-#824 behavior. Copy Invite/Revoke no longer appear anywhere on this page — see the member detail page's **Manage member** section below |
| `/barn/[slug]/members/[membership_id]` | All active members | Member detail page; any active barn member can open any other member's page (#779 — previously a trainer viewing another trainer/manager or a rider viewing anyone else got `notFound()`; `targetMembership` is now resolved via `getMembershipByIdForBarn`, which falls back to the `get_active_barn_member_summaries` RPC for rows the caller's narrow direct-query access doesn't cover); the `<h1>` name heading sits in a header row alongside a manager-only **Remove** button (`RemoveMemberButton`, #943 — moved here from the Settings page's now-removed "Active Members" section, same `window.confirm` copy) — hidden when the caller is viewing their own page (`targetMembership.user_id !== user.id`, mirroring the old Settings-page self-exclusion), and re-checked server-side by `removeMemberAction` (`notFound()` on a missing/cross-barn/self target) since a Server Action is directly callable regardless of button visibility; on confirm it hard-deletes the membership and redirects to `/barn/[slug]/members`, since the detail page's own target row no longer exists afterward; immediately after that header row, a manager-only **Manage member** section (`ManageMemberSection`, #824) renders whenever the target is unclaimed (`targetProfile.is_managed === true && targetMembership.invite_token !== null`) — an amber notice ("This is an unlinked member...") plus **Copy Invite** and **Revoke** buttons side by side, moved here from the old members-list `ManagedMemberRow` component (now deleted); Copy Invite writes `${origin}/barn/${slug}/register?token=${inviteToken}` to the clipboard client-side (#942 — repointed from `/login`, which has no already-authenticated short-circuit, to `/register`, which does) and shows a transient "Copied!" label (~2s, plain `useState`/`setTimeout`); Revoke submits `revokeInviteTokenAction` (moved from the list page's `actions.ts` to this page's own co-located `actions.ts`, since it's now only invoked from here) unchanged — one click, no confirm dialog — which now revalidates both `/barn/[slug]/members` and this detail page's own path so the rotated token reflects immediately; both Copy Invite and Revoke are disabled from the moment Revoke is clicked until the revalidated page delivers the new `inviteToken` prop (#939, `ManageMemberSection.tsx` — `pending` alone isn't enough, since Next resolves the action's promise before the revalidated props land, so a `tokenBeforeRevoke` snapshot gates both controls until `inviteToken` actually changes) — closing both a stale-token-copy race and a double-Revoke race where a second click mid-flight would re-pin the stale-token check to itself; after that, a **Photo** section (#1004, mirrors the horse detail page's own Photo section) — every active barn member sees the target's single profile picture (signed URL from `profiles.photo_path` via `getSignedUrl`), scaled the same way as horse photos (`h-48 w-auto`, no cropping), or the shared `EmptyState` ("No photo yet") when unset; edit controls (Set/Replace/Remove, same layout as horses') show for the target themself (`isOwnPage`) or a manager viewing a still-managed/stub profile (`canEditContactInfo`'s same condition) — Set/Replace link to the shared `/barn/[slug]/documents/new?entity=profile&id=<membershipId>&type=photo` page, Remove submits `deleteProfilePhotoAction` directly; unlike horse photos, self-upload is reachable here too (a member managing their own page), not only a manager-of-managed-profile, and the write-lock is `is_managed` itself rather than a separate lock column — see `profiles.photo_path`'s note in `schema.md`; profile is resolved via `targetMembership.profile_id` (not `user_id`), so a managed/stub member (`user_id IS NULL`) still shows a name and a **Contact Info** section (Phone, Emergency Contact Name, Emergency Contact Phone; missing fields render `—`) — visible to any active barn member viewing any other active barn member (#863, broadened from self/manager-any-target/trainer-viewing-rider), no new RLS or access beyond the existing gating; when the caller is a manager and the target profile is a stub (`is_managed=true`), Contact Info renders as an inline edit form (`ContactInfoForm`, mirrors `ProfileForm`'s controlled-input pattern) instead of the read-only block — Save calls `updateContactInfoAction`, which parses/validates the contact fields via the shared `parseContactFields` helper (`src/lib/contact-info.ts`, also used by `/profile`'s `updateProfileAction`) and writes via the existing `updateContactInfo` DAL function and relies on the `profiles_manager_update` RLS policy (restricted to `is_managed=true` rows, #526) to reject a write against a real/claimed account if one is ever attempted directly; every other caller (including a manager viewing a real account) sees the read-only block; the **Documents** section is visible only to a manager or the member themself (#779 — narrowed from the prior self/manager/trainer-viewing-rider rule; a trainer no longer sees a rider's documents even read-only), hidden entirely with no placeholder for any other viewer, gated by `canViewDocuments` (`canUpload` pre-#864) — this is the section's *only* view gate: it no longer hard-blocks on `targetMembership.user_id` presence (#738 — previously a managed/unclaimed target showed a "No account linked — documents unavailable" line instead, even for a manager who could otherwise fully manage the page; documents are fetched via `targetMembership.id`, not `.user_id`, now that `rider_documents.rider_id`/`staff_documents.trainer_id` are `barn_memberships.id`s); when visible, shows documents for the target member and, when `canManageDocuments` (manager, or the instructing trainer on their own page — rider-self excluded since #864, per the barn manager's #780 decision: a rider can view but no longer upload or delete their own documents, enforced at RLS too, not just this gate), an **Add Document** button (linking to the shared `/barn/[slug]/documents/new?entity=trainer|rider&id=<membershipId>` upload page, see that route below); manager target: uses `staff_documents` stored at `{barn_id}/managers/{user_id}/` (same document types as trainer); trainer target: `staff_documents` at `trainers/`; rider target: `rider_documents` at `riders/`; manager can upload/delete any member's docs, including a managed/unclaimed member's (#738); trainer manages own docs, rider is read-only on own docs (#864); a rider target additionally shows an **Active Agreements** section (#772) — one-line `Card` per active agreement (any kind) reading `{kind} · {horse name} · {fee}{/month if monthly}`, horse names resolved via `resolveHorseNames`; empty state is plain "No active agreements" text with no CTA link; backed by `getActiveAgreementsForRider` — rendered regardless of whether the target has a linked account, since `agreements.rider_id` is a `barn_memberships.id`, not an auth user id, but only when the caller is a manager or the rider viewing their own page, since `agreements` RLS grants SELECT to neither trainers nor other riders — a trainer viewing a rider's page would otherwise see a false "no agreements" status; the card only links to the agreement detail page (`/barn/[slug]/agreements/[id]`) when the caller is a manager (`linkable` prop on `ActiveAgreements`) — that route is manager-only (`requireMembership(slug, ['manager'])`), so a rider viewing their own agreements sees the same card content rendered as a plain non-interactive `Card` (no `href`, so it renders as a bordered `div` rather than a linked, hoverable block) instead of a dead link; when the caller is a manager and the target's role is `manager` or `trainer`, an **Instructor Access** section shows a Grant/Revoke toggle button (`InstructorAccess`, a client component since #943, calls `setCanInstructAction` → `setCanInstruct` DAL → `set_can_instruct` RPC) — rendered regardless of whether the target has a linked account, since the RPC only needs the membership id; not shown for a rider target, since `can_instruct` is instructor-specific; revoking (not granting) shows a `window.confirm` dialog naming the target and warning they'll no longer be assignable to future lessons (#943, matching every other destructive action in the app); `setCanInstructAction` itself calls `notFound()` for an invalid/cross-barn/rider target, mirroring the page's own gating rather than the sibling document/contact-info actions' `{ error }` return pattern, since this form has no client-side error display and the RPC's own `invalid_target_role` check already guards the same case server-side; accepted file types: PDF, JPG, PNG, DOCX (max 4.5 MB); documents open via signed URL (300 s TTL); upload form has an optional "Expiration reminder date" field; manager sees an inline editable Reminder Date cell per document (blur-to-save, mirrors `ChargesTable`'s pattern) regardless of who uploaded it; trainer/rider see it read-only; every role sees a role-agnostic "Reminder Due" badge (`ReminderDueBadge`) next to that cell once `reminder_date <= today` |
| `/barn/[slug]/documents/new` | manager, trainer, rider (per entity — see below) | Shared upload page (#763) for horse/trainer/rider documents, replacing the three near-duplicate embedded upload forms that used to live on the horse and member detail pages. `?entity=horse\|trainer\|rider&id=<entityId>` — `id` is the horse id for `entity=horse`, or the `barn_memberships.id` for `entity=trainer\|rider` (a manager target's staff documents are linked with `entity=trainer`, matching `documents.ts`'s three-value `Entity` type — there's no separate `'manager'` entity); for `entity=horse`, `requireMembership` restricts to `['manager','trainer']` (rider has no horse-doc upload access); for `entity=trainer\|rider`, restricted to `['manager','trainer','rider']` then further gated by the shared `canManage` helper (`src/lib/document-target.ts`; manager: any target, trainer: own page only, rider-self excluded since #864) computed from the fetched `barn_memberships` row — `notFound()` on any failure. For the trainer/rider branch, the `?entity=` query param only selects which page branch renders — both the page's rendered `DocumentUploadForm` and `uploadDocumentAction`'s DB write independently re-derive the real entity/table from the target's actual `barn_memberships.role` via the shared `resolveManageableTarget` helper (`src/lib/document-target.ts`, also used by `deleteDocumentAction`/`updateDocumentReminderDateAction` on the member detail page), rather than trusting the query param — closing a gap where a mismatched `?entity=` could otherwise misfile a document into the wrong table. Renders an entity-specific `DocumentUploadForm` (record-type `<select>` options per entity — options, display labels, and per-entity valid-value sets all come from `src/lib/document-record-types.ts`, the single vocabulary module also behind `uploadDocumentAction`'s record-type validation, the horse/member detail pages' Documents tables, and `DocumentRemindersSection`'s record-type rendering — file picker with 4.5 MB/type validation, notes, optional reminder date, indeterminate progress bar while pending) and a Cancel `Button` (`ghost` variant) back to the originating horse or member detail page. `uploadDocumentAction` (`actions.ts`'s three-branch merge of the former `uploadHorseDocumentAction`/`uploadDocumentAction`) validates the file (`validateFile`), uploads to Storage (`uploadFile`), inserts via `createDocument` (rolling back the Storage upload on a DB error), and on success calls `redirect()` back to the originating detail page instead of returning `{error: null}`, since this is a standalone page navigating away rather than an inline form. A fourth branch, `?entity=horse&id=<horseId>&type=photo` (#1002 revise), reuses this same page/form rather than the horse detail page's own inline control, to avoid duplicating the upload UI — restricted to `['manager']` only (stricter than the `['manager','trainer']` used for regular horse documents, since photo write stays manager-only); heading reads "Set Photo — {name}" or "Replace Photo — {name}" depending on whether `horses.photo_path` is already set; renders `DocumentUploadForm` with its `photoMode` prop, which locks the Document Type field to a static "Photo" value (no `<select>`), hides the Notes and Expiration reminder date fields, restricts the file input to jpeg/png, and — unlike every other branch, where choosing a file just populates the filename display and still requires a separate Upload click — auto-submits the form (`formRef.current?.requestSubmit()`) as soon as a valid-size file is selected, so there's no second click; the bound action is `uploadHorsePhotoAction` (from the horse detail page's `actions.ts`), not `uploadDocumentAction` — the photo is still written to `horses.photo_path` and the separate `horse-photos/` storage prefix, never to `horse_documents`, since that separation (riders can see the photo but not documents) is unchanged by this reuse; `uploadHorsePhotoAction` mirrors `uploadDocumentAction`'s own success behavior, calling `redirect()` back to the horse detail page rather than returning `{error: null}`, so a successful photo upload actually navigates the manager back to see it instead of leaving them stranded on the upload form. A fifth branch, `?entity=profile&id=<membershipId>&type=photo` (#1004), reuses this same page/form for member profile pictures — restricted to `['manager','trainer','rider']` since, unlike horse photos, the target's own self-upload is a valid caller (any active member managing their own page), not just a manager; access beyond that role check is re-derived from the target's `barn_memberships`/`profiles` rows exactly as `uploadProfilePhotoAction` does (self, or a manager targeting a still-`is_managed` profile) rather than trusting the URL; heading and `photoMode` behavior are identical to the horse-photo branch; the bound action is `uploadProfilePhotoAction` (from the member detail page's `actions.ts`), writing to `profiles.photo_path` and the `profile-photos/` storage prefix, and redirects back to the member detail page on success. |
| `/barn/[slug]/finances` | manager | **Outstanding Income** section (renamed from "Outstanding" by #940, no behavior change — all-time past unpaid lessons with non-zero fee, merged with unpaid agreement charges whose `period` is before the first of the current month and uncollected rider late-cancellation fees (#831, `getOutstandingCancellationFees`), via `mergeOutstandingItems` — inline payment-type dropdown via `OutstandingTable` Client Component with a Type column (Lesson/Lease/Boarding/Cancellation Fee) routing to `updatePaymentTypeAction`, `updateChargePaymentTypeAction`, or `updateCancellationFeePaymentTypeAction`, plus a "View all outstanding" link to `/barn/[slug]/finances/outstanding`) appears above the month selector and is hidden entirely when there are no outstanding items — decoupled from outstanding expenses (#872, #940), which get their own independent section (see below), rather than a row type squeezed into this table: an expense has no rider/instructor and no known fee yet, so it doesn't share `OutstandingItem`'s shape or its "pick a payment type, it auto-saves" interaction. A separate **Outstanding Expenses** section (renamed from "Needs an amount" and broadened by #940 — amber header, matching the amber `warning` convention, plus a bold total above the list summing known amounts only and an `InfoPopover` explaining the inclusion criteria; `getOutstandingExpenses(barn.id, barn.timezone)`, hidden when empty) lists each outstanding expense (missing an amount, missing a payment type, or both, and past its due datetime) as a single line — `"{date} — {recipient} — {expense type}"` — linking straight to `/barn/[slug]/expenses/[id]`, where the existing Amount + Payment Type fields resolve it; no dedicated resolve action or inline form. Below the Outstanding Income table: `←`/`→` month navigation; `?month=YYYY-MM` selects month (defaults to current, clamped to barn creation date) — parsing/clamping and the prev/next month URLs come from `resolveFinancesMonth` (`src/lib/finances-month.ts`, shared with the four drill-down pages, which previously imported it cross-page from the finances page module), and links re-emit the selected month via its sibling `formatMonthParam`; a single **Pending income** line (only shown for the current month; `payment_type IS NULL` lessons/charges collected this month but not yet marked paid, own `InfoPopover`) is all that remains above the pill switcher — **#971 removed** the three `SummaryStatCard`s (Gross Income / Total Expenses / Net Income) that used to sit above it, since "Gross"/"Net" meant genuinely different things in different breakdown tables below (some net-of-cut, some not) and the cards' own numbers didn't reconcile with any one table; that ambiguity is what #971 as a whole resolves. Pill-style tab switcher (`?tab=horse\|tier\|rider\|trainer\|recipient`, defaults to `horse`) with five views. **#971 unifies all five around one model**: every table shows **Gross | Expenses | Net** (`—` for a table with no concept of a column, e.g. By Paid To's Gross/Net, By Rider's Expenses), with **Net = Gross − Expenses always** (never computed independently) — both barn-wide and per-row. `Gross` = collected `transactions.amount` for positive-sign kinds (`lesson_fee`, `rider_cancellation_fee`, `lease_charge`, `board_charge`) in the month range; `Expenses` = collected `instructor_payout` rows plus every `expense`-kind row with a known amount in the range (`expense-finances.ts`'s `fetchExpenseTransactionsInRange` doesn't gate on `collected` the way the lesson/instructor-payout side does, so an expense with an amount entered but no payment type yet still counts here, matching its pre-existing appearance in the separate Outstanding Expenses section); this makes barn-wide Net a real bottom-line figure that can go deeply negative in an expense-heavy month, not "net of instructor cut only." Each table ends in a shared `<tfoot>` (`ReconciliationFoot.tsx`) with four rows, in order — **Subtotal** (this table's own attributable rows), **Unattributed** (`InfoPopover`-explained; genuine data gaps — most commonly an `expense`-kind `transactions` row whose owning `horse_expenses` record was deleted after being marked paid, per `deleteExpense`'s default `deleteCollectedTransactions=false`, so the cost survives in `getExpenseFinancialSummary`'s `totalExpenses` but drops out of every per-horse/per-recipient `breakdown` — previously silently missing from By Horse/By Paid To entirely, now surfaced here instead; rendered above Outside this view since it's a gap in *this* table's own data, a more direct concern than money that's real but structurally belongs elsewhere), **Outside this view** (`InfoPopover`-explained; derived as `barnTotal − Subtotal − Unattributed`, e.g. lease/boarding income has no lesson tier so it's By Tier's Gross-Outside; horse expenses have no instructor so they're By Instructor's Expenses-Outside), and **Total** (bold, `Subtotal + Outside + Unattributed`, identical across all five tabs for a given column) — the Expenses column's `ValueCell` passes `forceParens` (with a same-as-`ByTierTable` zero-guard rendering exact zero as `—` rather than the `-0` formatting artifact `($0.00)`), so every bucket's Expenses figure renders in accounting parens consistently, not just each table's own body rows — computed via `src/lib/finances-reconciliation.ts`'s `buildReconciliationColumn(total, subtotal, unattributed)` (deriving `outside`) and `deriveNetColumn(gross, expenses)` (subtracting field-by-field: `subtotal`, `outside`, `unattributed`, and `total` each independently). This footer is what the former synthetic body rows (`Non-lesson income`, `No horse`, `No rider`, `No instructor`) were replaced by — their totals now fold into a table's Outside-this-view or Unattributed bucket instead of appearing as an unlinked `<tbody>` row. All five tables keep #945's client-side-only sortable column headers — a shared `useSortableRows` hook (`src/app/barn/[slug]/(protected)/finances/useSortableRows.ts`) plus a single `SortableTh` header cell used for every column on every table (#971 consolidation — previously By Rider's Expenses and By Paid To's Gross/Net used ad hoc `<Th>`+`InfoPopover>` markup instead): when `sortKey`/`onSort` are supplied it wraps the shared `<Th>` with a clickable button, `aria-sort`, and a ▲/▼ directional indicator; when omitted it renders the label as plain non-interactive text with no `aria-sort` (By Rider's Expenses, By Paid To's Gross/Net); either way an optional `infoText` prop renders an `InfoPopover` as a **sibling** of the label/button — not nested inside it, so tapping the info icon never triggers a sort — both living in one `inline-flex` row so the icon can never wrap onto its own line below the label (a block-level `flex` button was the original bug) — each table is its own small Client Component (`ByHorseTable.tsx`/`ByTierTable.tsx`/`ByRiderTable.tsx`/`ByInstructorTable.tsx`/`ByPaidToTable.tsx`) rather than inline JSX in the (Server Component) page, since sort state needs a client boundary; no server round-trip, no URL param; default sort is the name column ascending for all five (the synthetic-row-derived footer is structurally outside `<tbody>` so it's never part of the sort), clicking a column sorts it ascending first, clicking the active column again flips direction. **By Horse** (Horse/Gross/Expenses/Net — row set is the *union* of horses with lesson/agreement income that month and horses with expenses that month, via `getExpenseFinancialSummary`'s per-horse `breakdown`; a horse present on only one side shows `$0.00` for the other, so an expense-only horse (e.g. injured, vet bills only) still surfaces with a negative Net; `Gross` comes from `getHorseIncomeSummary`'s now-gross `totalIncome` (#971, `splitsGrossFee`), `computeHorseNetIncome` derives `net = gross − expenses`). **By Tier** (tier name, **Gross**/**Expenses**(the tier's own summed instructor cut, `—` at zero)/**Net** — #971 dropped both the `Lessons` count column and the `Price` column; every active barn tier still appears even with zero paid lessons that month, #771, **except** the tab now gates on real activity rather than row count — `page.tsx`'s `tierHasActivity` checks whether any tier row has a non-zero `subtotal`/`instructorCut`, since #771's own per-active-tier `$0` backfill means `breakdown.length` is never 0 for a barn with any active tier, **or** whether `tierGross.outside` is non-zero (a review-follow-up fix — a boarding/lease-only month with zero paid lessons has an all-zero `tierRows`, but real charge income still exists via the `NON_LESSON_INCOME_LABEL` row `tierRows` excludes, and that money must still show up reconciled in the footer's Outside-this-view row rather than being hidden behind an `EmptyState`); a month where every tier collected nothing AND no charges were collected either renders `EmptyState` instead of a table full of `$0.00` rows). **By Rider** (rider name — underlined link — **Gross** (#971, now exposed via `RIDER_INCOME_DESCRIPTOR`'s `splitsGrossFee`, previously this tab had no Gross column at all), **Expenses** always `—` (no rider-level expense concept — rendered via `SortableTh`'s own non-sortable mode, no `sortKey`/`onSort` passed, since sorting a constant column is meaningless), **Net** reuses the Gross column's sort key since the two are always numerically equal). **By Instructor** (trainer full name — underlined link to that trainer's drill-down — **Gross** (that trainer's raw, pre-cut lesson fees), **Expenses** (renamed from "Instructor Cut", Gross minus Net, `—` when Gross is `—`), **Net**). **By Paid To** (#949 — recipient name — underlined link, since `horse_expenses.recipient` is free text with no id anywhere in the schema — **Gross**/**Net** always `—` (a recipient has no revenue concept), **Expenses** (renamed from "Expense Amount", #971) via `getRecipientExpenseSummary`). Every tab's default sort re-sorts by name/recipient ascending on load regardless of the DAL's own sort order; negative net figures (e.g. a comped lesson after the cut, or a horse's negative Net) render accounting-style via the shared `formatCurrency` helper (`src/lib/format-currency.ts`) rather than a leading minus sign — and date cells across the finances pages (both Outstanding tables, the three drill-downs) plus the dashboard's `DocumentRemindersSection` render via the shared `formatShortDate`/`formatShortDateOnly` helpers (`src/lib/format-date.ts`), consolidated from per-module private copies; the By Horse/By Rider/By Instructor name cells are underlined links (#771 — persistent, not hover-only, matching the drill-down pages' own date-link styling) to their respective drill-down pages (`finances/horses/[id]`, `finances/riders/[id]`, `finances/trainers/[id]`), which show the same net-of-cut fee and split amounts, also via `formatCurrency`, and are unaffected by #971 (out of scope — no reconciliation footer or Gross/Expenses/Net unification on the drill-down pages themselves); the By Paid To recipient name cells are likewise underlined links (#949) to `finances/expenses/[recipient]` |
| `/barn/[slug]/finances/outstanding` | All active members | Read-only list of outstanding items for the calling user's role (lessons merged with agreement charges and rider cancellation fees via `mergeOutstandingItems`, #831, with a Type column); manager sees all barn outstanding, trainer sees own lessons only (instructor_id filter; not a party to agreements) plus cancellation fees for lessons they instruct, rider sees enrolled lessons plus their own outstanding leases/boarding and own cancellation fees; each lesson row and each cancellation-fee row (linked via its parent lesson) links to the lesson detail page; each charge (lease/board) row links to its agreement detail page for a manager viewer only (#931 — `OutstandingCharge.agreementId`, resolved from `agreement_charges.agreement_id`; `/barn/[slug]/agreements/[id]` is manager-only, so a trainer/rider's own charge row renders as plain unlinked text, mirroring the member detail page's `ActiveAgreements` `linkable` gating, #772) |
| `/barn/[slug]/finances/horses/[id]` | manager | Per-horse drill-down for a given month (`?month=YYYY-MM`, defaults to current); one combined table of the horse's paid lessons (full fee, horse count, split amount, linking to the lesson), paid agreement charges (full amount, no split, linking to the agreement), and expenses (amount and split in accounting-parens format, linking to the expense), each row tagged with a **Type** column (Lesson/Lease/Boarding/Expense) and all rows ordered by date ascending; bottom **Net** row = lesson splits + charge fees − expense splits, matching this horse's Net on the "By Horse" tab; linked from the By Horse tab with month param preserved |
| `/barn/[slug]/finances/riders/[id]` | manager | Per-rider income drill-down for a given month (`?month=YYYY-MM`, defaults to current); reworked in #771 to match the By-Horse combined-table pattern above — one table of the rider's paid lessons (full fee, rider count, split amount, linking to the lesson) and paid agreement charges (full amount, no split, linking to the agreement), each row tagged with a **Type** column (Lesson/Lease/Boarding) and ordered by date ascending, replacing the prior two-table "Leases & Boarding" layout; bottom **Total** row matches the "By Rider" summary on the finances page; linked from the By Rider tab with month param preserved |
| `/barn/[slug]/finances/trainers/[id]` | manager | Per-trainer income drill-down for a given month (`?month=YYYY-MM`, defaults to current), added in #771 following the same combined-table/Type-column pattern as the horse/rider drill-downs above; one table of the trainer's paid lessons (lesson date linking to the lesson detail, Type always "Lesson", fee net of that lesson's own snapshotted instructor cut — no split, since `lessons.instructor_id` is a single column rather than a junction table like `lesson_riders`/`lesson_horses`), ordered by date ascending; no charge rows — agreements have no instructor; bottom **Total** row matches the "By Instructor" summary's Income column on the finances page; linked from the By Instructor tab with month param preserved |
| `/barn/[slug]/finances/expenses/[recipient]` | manager | Per-recipient expense drill-down for a given month (`?month=YYYY-MM`, defaults to current), added in #949; `[recipient]` is the raw recipient string itself (URL-encoded by the linking By Paid To tab, decoded automatically by Next's route-param handling), not a resolved entity id, since `horse_expenses.recipient` is free text with no id anywhere in the schema; one table of that recipient's expenses that month (Date — linking to the expense's edit page, Type — the expense's `expense_type`, Amount), ordered by date ascending, mirroring the By Trainer drill-down's flat (unsplit) shape rather than the By Horse/Rider drill-downs' split-across-participants shape, since a recipient is 1:1 with each of its expenses; bottom **Total** row matches the "By Paid To" summary on the finances page; linked from the By Paid To tab with month param preserved |
| `/barn/[slug]/settings` | manager | **Manage Barn** page (#778 — restructured into collapsible accordion sections, each built on a native `<details>`/`<summary>` `AccordionSection` component rather than a client-side disclosure pattern, so the page stays a pure Server Component, all collapsed by default; a section's own header button, e.g. Lesson Tiers' Add Tier link, renders as a sibling positioned outside `<summary>` rather than nested inside it, avoiding the nested-interactive-element nesting `<details>`/`<summary>` don't tolerate well; the self-service join-request "Pending Requests" section — approve/reject for legacy pending rows — was removed in #1037, since #777 had already closed off the generic self-registration flow that created them, and the sibling "Active Members" section that used to live here was removed in #943 — member removal now lives on the Member Detail page header instead): **Default Instructor Cut** (#776, renamed from "Instructor Cut"; single number field + Save, with helper text noting it doesn't affect past lessons — only seeds new lesson tiers and Custom lessons booked afterward), **Horse Exhaustion Thresholds** (Moderate/High number fields pre-filled from the barn row, server-validated `moderate < high` with a field error otherwise), Lesson Tiers list with Edit links and Add Tier link, **Barn Events** list (#1014, title/date/visible-to roles, Edit links, Add Event link — see `barn_events` in [`schema.md`](schema.md)), editable **Default Board Fee** (with non-retroactive helper text — only seeds new boarding agreements), editable **Barn Timezone** (#955 — a `<select>` restricted to `src/lib/barn-timezone.ts`'s short fixed list of common US IANA zones; used only by the handful of server-side comparisons with no viewer to ask instead, see `barns.timezone` in [`schema.md`](schema.md) — has no effect on how dates/times display, which always follows the viewer's own browser timezone) |
| `/barn/[slug]/settings/tiers/new` | manager | New Tier page; shared `TierForm` component, including an Instructor Cut field pre-filled from `barns.default_instructor_cut` (#776); on save redirects to settings |
| `/barn/[slug]/settings/tiers/[id]` | manager | Edit Tier page; shared `TierForm` component; Instructor Cut field pre-filled from the tier's own rate, with the same amber "won't affect past lessons" warning on change as the price field (#776); Set Default and Save actions redirect to settings; Activate/Deactivate (#752) stay on this page — they `revalidatePath` both this page and settings instead of redirecting, so the manager sees the tier's new status in place; attempting to deactivate the barn's default tier no longer silently redirects — `deactivateTierAction` returns `{ error }` (via `useActionState`, same pattern as the Save form) and `TierForm` renders it inline next to the Deactivate button, telling the manager to set another tier as default first |
| `/barn/[slug]/settings/events/new` | manager | New Event page (#1014); shared `EventForm` component — title, `DateHourPicker` (reused as-is from `lessons/DateHourPicker.tsx` via its `onChange` prop into a locally-named `event_at` hidden input), notes, and a three-checkbox `visible_to_roles` selector defaulting all-checked; on save redirects to settings |
| `/barn/[slug]/settings/events/[id]` | manager | Edit Event page (#1014); shared `EventForm` component pre-filled from the event row, checkboxes pre-checked per `visible_to_roles`; a Delete link/button navigates to the delete-confirm page rather than deactivating in place (no soft-delete concept for events, unlike Lesson Tiers) |
| `/barn/[slug]/settings/events/[id]/delete` | manager | Delete Event confirm page (#1014) — mirrors the Expenses delete-confirm pattern; hard-deletes the row, redirects to settings |
| `/barn/[slug]/guide` | All active members | Role-specific user guide rendered from `USER_GUIDE_{ROLE}.md` at repo root using `react-markdown` with Tailwind `prose` styling |
| `/profile` | Authenticated | Edit form for first name, last name, phone, emergency contact name, emergency contact phone; name changes prompt user to notify their barn manager; linked from avatar dropdown. Accepts optional `?barn=<slug>` — when present and the user has an active membership in that barn, the full barn nav bar is rendered instead of the back-button fallback; post-save redirect also returns to `/barn/<slug>` rather than the default membership-based redirect. When `?barn=<slug>` resolves to an active membership, also renders a **Calendar Feed** section (`CalendarFeedSection`, #1018) — a "Get my calendar link" button that generates (or reveals) that membership's `calendar_feed_token`-bearing `/calendar.ics?token=...` link on click, plus Copy/Regenerate controls once a link exists, with a static explainer that refresh cadence is controlled by the external calendar app, not the server; the displayed/copied link's origin is only ever read inside the Copy click handler (never at render time), so the component never touches `window` during a server render, mirroring `ManageMemberSection.tsx`'s invite-link pattern |
| `/profile/complete` | Authenticated | Same form with "Complete your profile" heading; post-login destination when any contact field (phone, emergency_contact_name, emergency_contact_phone) is null — the shared `isProfileIncomplete` predicate (`src/lib/contact-info.ts`), evaluated by the auth callback, which also drives the `incomplete_profile`/`member_incomplete_profile` notifications; redirects to `/` after save |
| `/login` | All | Sign-in page; displays Supabase connection status dot (green = `NEXT_PUBLIC_SUPABASE_URL` set, yellow = not set); shows no-barn guidance when `?no_barns=true` and user is authenticated; shows a "demo is unavailable" message when `?error=demo_unavailable` (set by `demo/actions.ts`'s `createOrResumeDemoBarn` on its failure paths — missing env vars, sign-in failure, missing profile); "Keep me logged in" checkbox (checked by default, pre-filled from `remember_me_pref` cookie) — on submit the sign-in actions set `remember_me` (5-min TTL, read by the auth callback) and `remember_me_pref` (1-year TTL) cookies to `1`/`0` before the OAuth redirect; `/barn/[slug]/login` has the same checkbox. The auth callback consumes `remember_me` (cleared on every exit path): `1` → `barn_session_*` and Supabase `sb-*` cookies get `maxAge` 30 days; `0` → session-only (`maxAge` stripped; note `@supabase/ssr` defaults `sb-*` to 400 days). Because `sb-*` cookies are rewritten on token refresh (by `src/lib/supabase/server.ts` and `src/proxy.ts` cookie adapters), both adapters re-apply the lifetime via `applyRememberMe` (`src/lib/supabase/cookie-options.ts`), reading `remember_me` falling back to the durable `remember_me_pref` |
| `/terms` | unauthenticated | Terms of Service; renders `TERMS_OF_SERVICE.md` (repo root) via `react-markdown`, no auth/barn scoping — same static-markdown-page pattern as `/barn/[slug]/guide` but public; generic "← Back" link to `/barns` above the content (#996 follow-up, same treatment as `/about`/`/changelog`); linked from `/login` |
| `/privacy` | unauthenticated | Static privacy policy, rendered from `PRIVACY_POLICY.md` at repo root using `react-markdown` (same rendering approach as `/barn/[slug]/guide`, no auth check); generic "← Back" link to `/barns` above the content (#996 follow-up, same treatment as `/about`/`/changelog`). Linked from `/login` |
| `/about` | unauthenticated | App overview (name, short description, links to `/terms`/`/privacy`/`/changelog`); the `/changelog` link's label includes the current version parsed from `CHANGELOG.md`'s top `## vX.Y.Z` heading (via `parseLatestVersion`, `src/lib/changelog.ts`) — the suffix is omitted rather than erroring if the file can't be read. A generic "← Back" link to `/barns` sits above the content (not barn-scoped like `/profile`'s nav bar, since this route carries no barn context). Linked from the avatar dropdown (`UserMenu.tsx`), between User Guide and Sign out |
| `/changelog` | unauthenticated | Renders `CHANGELOG.md` (repo root) via `react-markdown`, same static-markdown-page pattern as `/terms`/`/privacy`, no auth check; same generic "← Back" link to `/barns` as `/about`. Linked from `/about`'s Changelog bullet, and from the avatar dropdown via `/about` |
| `/calendar.ics` | Token (no session) | #1018 — Route Handler (`src/app/calendar.ics/route.ts`), not a page — one of two sanctioned exceptions to "no API routes" (see `ARCHITECTURE.md`'s Server actions pattern section; the other is `/api/cron/reset-demo` below), since an external calendar app's poller can't run a Server Action or carry a session cookie. `GET`-only; reads `?token=` from the query string, missing → `400`. The token is checked against `barn_memberships.calendar_feed_token` entirely inside the `get_calendar_feed` `SECURITY DEFINER` RPC (see [`rpc.md`](rpc.md)) — there is no `requireMembership` call here, since that helper's `redirect()`/`notFound()` calls are Server Component/Action-only and don't work inside a Route Handler; an unknown/inactive token → `404`. A valid token returns `200` with `Content-Type: text/calendar; charset=utf-8` and a hand-rolled RFC 5545 `.ics` body (`buildIcsFeed`, `src/lib/ics.ts`) built fresh from the RPC's role-filtered rows every request — no stored file, no cache, `Cache-Control: no-store`. Linked from `/profile?barn=<slug>`'s Calendar Feed section (see that route's own entry above) |
| `/demo` | unauthenticated | Entry point for the public demo experience (#505). Server Component `notFound()`s if `DEMO_USER_EMAIL` isn't set; otherwise renders `DemoLoader`, a Client Component (spinner + "Explore Stable State" heading) that calls the co-located `createOrResumeDemoBarn` Server Action on mount. The action signs an unauthenticated visitor in as the shared `DEMO_USER_EMAIL`/`DEMO_USER_PASSWORD` auth user (an already-authenticated real user keeps their own session and gets the demo barn added to their account instead), resumes an existing demo barn via the `demo_barn_slug` cookie when it and the visitor's membership on it both still exist, otherwise reaps the oldest `is_demo=true` barn once `DEMO_BARN_CAP` is reached and creates+seeds (`seedBarn()`, `scripts/seed-barn.ts`) a fresh one (slug `demo-{8 hex chars}`), adds the visitor as its manager, sets `demo_barn_slug` + the same `barn_session_{slug}` cookie `/barn/[slug]/*` routes require, and redirects into it |
| `/api/cron/reset-demo` | `CRON_SECRET` bearer token (no session) | #506 — Route Handler (`src/app/api/cron/reset-demo/route.ts`), the other sanctioned exception to "no API routes" (see `/calendar.ics` above) — Vercel Cron (`vercel.json`'s `crons` entry, hourly) calls a plain HTTP endpoint and can't invoke a Server Action. `POST`-only; requires an `Authorization: Bearer <CRON_SECRET>` header matching the `CRON_SECRET` env var, else `401` (an unset `CRON_SECRET` also denies everyone, rather than leaving the endpoint open). Reuses the same reap mechanism `/demo` already calls inline (`getOldestDemoBarn`/`countDemoBarns`/`teardownBarnData`/`deleteBarn`, all in `src/lib/db/barns.ts` + `service-role.ts`) via a service-role client, repeatedly tearing down and hard-deleting the oldest `is_demo=true` barn while it's either older than 6 hours or the total demo barn count still exceeds `DEMO_BARN_CAP` (0 = no cap enforcement here) — `/demo`'s own cap check only ever reaps reactively, on the next visitor's page load, so this also acts as a standing safety valve between visits. Returns `200` with `{ "reaped": <count> }` |
| `/barn/[slug]/register` | unauthenticated or already-authenticated | Invite-token landing page (#777 — generic self-registration closed; every member now joins via the managed-member + per-person invite-token flow) and, since #942, the entry point every generated invite link actually points at (`buildInvitePath` in `scripts/seed-account.ts`, `ManageMemberSection.tsx`'s Copy Invite) rather than `/login`, so an already-authenticated visitor gets the Accept-Invite short-circuit below instead of always being forced through Google. Requires a `?token=` query param matching an unclaimed managed-member invite; missing/invalid token, or an `?error=1` param from a failed claim, renders an "Invite invalid" message with no form. A valid token, when the visitor isn't yet signed in, redirects to `/barn/[slug]/login?token=...` to reuse the existing OAuth+claim flow (see `claim_managed_member` in [`rpc.md`](rpc.md)); when already signed in (and not already an active member), the page renders a "Join {barn}" confirmation with an **Accept Invite** button bound to `register/actions.ts`'s `acceptInvite` Server Action — claiming is deliberately not performed as a side effect of the page's own render (a bare GET), since that would let a `<Link>` prefetch or a chat-app link-preview bot burn the single-use invite token before the intended recipient acts; `acceptInvite` calls `claimManagedMember` (passing `null` for a missing OAuth email, matching `/auth/callback`'s handling), sets the `barn_session_{slug}` cookie (#1076 — this path previously left it unset, which silently passed the bare `/barn/[slug]` dashboard render but bounced the user to `/barn/[slug]/login` via `proxy.ts`'s session check on the first nested route visited, despite a valid Supabase session) and redirects to the barn home on success, or back to this page with `?error=1` on failure |
