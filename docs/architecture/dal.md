# Data access layer

`src/lib/db/` — one file per domain. Never query Supabase directly from components or actions; always go through these modules.

**`Instant` return types (#1222).** This layer is the only one that knows both a `TIMESTAMPTZ` and the barn it belongs to, so it is the only place an `Instant` (`{ at, tz }`, `types.ts`) is minted — that is what lets `format-date.ts`'s `formatBarnDateTime`/`formatBarnDate`/`formatBarnTime` take no timezone argument and be impossible to call wrong. Any reader returning an instant that gets rendered or compared therefore takes a `timezone` parameter and brands its output rather than returning a plain string:

| Module | Branded field(s) | Signature change |
|---|---|---|
| `lessons.ts` | `Lesson.lesson_at`, `LessonDetail.lesson_at` | `getLessonById`/`getLessonsByIds`/`getLessonsByBarn` take `timezone` |
| `barn-events.ts` | `BarnEvent.event_at` | `getEventsByBarn`/`getEventById`/`getEventsByIds` take `timezone`; `createEvent`/`updateEvent` return the raw row (unbranded — neither caller reads it) |
| `outstanding.ts` | `OutstandingItem.date` | `mergeOutstandingItems` takes `timezone` |
| `lesson-finances.ts` | `HorseIncomeDetailRow.lessonAt`, `RiderIncomeDetailRow.lessonAt`, `TrainerIncomeDetailRow.lessonAt` | the three `get*IncomeDetail` readers take `timezone` |
| `notifications.ts` | `Notification.created_at` | `getNotifications(userId, barnId, timezone, limit?)` |
| `horses.ts` | projected-exhaustion and upcoming-lesson `lessonAt` | `getHorseProjectedExhaustion`/`getUpcomingLessonsForHorse` take `timezone` |
| `lesson-participants.ts` | the hydrated participant's `lesson_at` | `hydrateParticipants` takes `timezone` |

Audit columns nothing displays stay plain strings — see `types.ts`'s `Instant` doc comment for the rule and its one exception. `backup.ts` is deliberately outside this scheme (see its row below): it needs zone-less wall-clock digits for an Excel serial, not a formatted string.

**`CalendarDate` return types (#1223).** The other frame. A `DATE` column names a day on a wall calendar, not a moment on the timeline, so it needs no `timezone` parameter — but while it was a bare `string` the compiler could not stop it reaching a zoned path, or an instant reaching a UTC-forced one. Every reader returning a `DATE` column now returns `CalendarDate` (`types.ts`), which `format-date.ts`'s `formatShortDateOnly`/`formatChargePeriod` require:

| Module | Branded field(s) |
|---|---|
| `documents.ts` | `HorseDocument`/`TrainerDocument`/`RiderDocument.reminder_date`, `DueDocument.reminderDate`; `getDueDocuments(barnId, today)` takes one too — the comparison is `CalendarDate` on both sides |
| `agreements.ts` | `Agreement.start_date`, `AgreementCharge.period` |
| `agreement-finances.ts` | `ChargeSummaryRow.period`, `PaidCharge.period`, `OutstandingCharge.period` |
| `lesson-finances.ts` | `HorseChargeDetailRow.period`, `RiderChargeDetailRow.period` |
| `expenses.ts` | `Appointment.expense_date`, and every shape extending it |
| `expense-finances.ts` | `HorseExpenseDetailRow.expenseDate`, `RecipientExpenseDetailRow.expenseDate` |
| `outstanding.ts` | `OutstandingItem`'s `lease`/`board` `date` (its `lesson`/`cancellation_fee` arms stay `Instant` — that union is where the two frames meet) |

Unlike `Instant`, which is structurally constructed, a branded string needs an explicit mint. There are three and no more, so `grep` answers "who produces one?": `local-day.ts`'s `calendarDate` (unchecked, for a value PostgREST hands back as `string` that the DB already types `DATE`) and `isValidDateString` (a type predicate — the validating mint for a `?date=` search param), plus `barn-timezone.ts`'s `barnDay`/`barnToday`, the crossing from the instant frame into this one.

Write inputs stay bare `string` — `ExpenseInput.expenseDate` and the agreement/document date parameters take unvalidated form text, and branding there would only move the lie earlier.

| File | Domain |
|---|---|
| `auth.ts` | Auth session; `getAuthenticatedUser()` — wraps `supabase.auth.getUser()`, returns `User \| null` |
| `transactions.ts` | Single typed reader for the `transactions` ledger (#865), replacing the hand-rolled FK-embed queries `agreements.ts`/`expenses.ts`/`lesson-finance-queries.ts` each used to write independently. `getTransactionRows(barnId, kinds, options?: {startDate?, endDate?, collected?}, client?)` selects the full base row set (`id`, `kind`, `amount`, `collected`, `paymentType`, `membershipId`, `horseId`, `lessonId`, `lessonRiderId`, `agreementChargeId`, `expenseId`, `occurredAt`, camelCase-mapped) scoped to `barn_id`/`kind IN (...)`, with `collected`/date-range filters applied only when passed — no FK embeds; a caller needing one extra joined field (tier_name, agreement_id, applies_to_all_horses) resolves it via its own small follow-up `.in('id', ids)` lookup against the joined table, the same idiom as `resolveMemberNames`/`resolveHorseNames`/`getTierPricesByNames` elsewhere in this DAL — this removes the FK-hint-pinning correctness concern (previously re-justified with near-identical comments citing #407/#665/#826/#827/#828/#776 in three separate files) rather than centralizing it, at the cost of one extra query round-trip per call site. `positiveAmount(kind, amount)` centralizes the ledger's sign convention: `instructor_payout`/`expense` are stored negative (an outflow), everything else — including `rider_cancellation_fee` (#830) — positive. `getOutstandingTransactionRows(barnId, { lessonIds?, chargeIds?, lessonRiderIds? }, client?)` (#831) — the Outstanding-side counterpart, calling the `get_outstanding_transactions` `SECURITY DEFINER` RPC instead of a direct table read (`transactions` SELECT is manager-only RLS, so this is what makes a trainer/rider-facing unpaid check possible); short-circuits to `[]` without an RPC round-trip when every id list is empty; returns a smaller row shape (`kind`, `entityId` — the resolved `lesson_id`/`agreement_charge_id`/`lesson_rider_id` depending on `kind`, `amount`, `collected`, `paymentType`) since the RPC itself only relays those columns. |
| `agreements.ts` | Lease/board agreement + charge CRUD: `getAgreementStatusLabel(agreement)` (pure — `is_active=false` → `'Ended'`; else `cadence='one_time'` → `'Complete'` (a one-time agreement's single charge is created at creation and never bills again), else `'Active'`; used by both the agreements list and detail pages); `createAgreement(params, client?)` (calls `create_agreement_with_first_charge` RPC), `getAgreementsByBarn(barnId, kind?, client?)`, `getAgreementById(agreementId, barnId, client?)`, `updateAgreement(agreementId, barnId, updates: { fee? })`, `endAgreement(agreementId, barnId)` (sets `is_active=false`); charge fns `getChargesForAgreement(agreementId, barnId)` (#885 — after the base `agreement_charges.*` select, overlays each charge's `payment_type` from a second `transactions` query, keyed on `agreement_charge_id`, `kind IN ('lease_charge','board_charge')` — `agreement_charges.payment_type` is no longer written by `mark_agreement_charge_paid`, so trusting the column directly would go stale; both queries are already manager-only RLS, so no RPC is needed, unlike the lesson-side `get_lesson_payment_info`; unlike `agreement-finances.ts`'s `getChargesForSummary`/`getPaidCharges`, this doesn't go through `transactions.ts:getTransactionRows` — that reader has no per-charge-ID filter, and this call only ever needs one agreement's charges, so a targeted query stays cheaper than a barn-wide fetch), `updateCharge(chargeId, barnId, fee)` (#828 — calls `update_agreement_charge_fee` RPC instead of a direct table update, so the linked `transactions.amount` stays in sync), `updateChargePaymentType(chargeId, barnId, paymentType)` (#828 — calls `mark_agreement_charge_paid` RPC instead of a direct table update, syncing the linked `transactions.collected`/`payment_type`; no longer dual-writes `agreement_charges.payment_type` as of #885, see that RPC's own doc entry in [`rpc.md`](rpc.md)), `generateChargeForMonth(agreementId, barnId, period, client?)` (calls `generate_agreement_charge` RPC; atomic and barn-scoped, so it never overwrites an already-paid charge and never crosses barns); `getBarnDefaultBoardFee(barnId, client?)`; `getActiveAgreementsForRider(barnId, riderId, client?)` (query-scoped lookup of all of a rider's active agreements, any kind, most-recently-created first — a rider can have more than one simultaneously-active agreement, e.g. boarding two horses, so this returns every match rather than assuming exactly one row exists — used by the member detail page's Active Agreements section, #772); `getUnpaidAgreementIds(barnId, client?)` (#831 — barn-wide, manager-only caller, so this reads `transactions` directly via `getTransactionRows` instead of going through the relay RPC; resolves each unpaid charge transaction's `agreementChargeId` to an `agreement_id` via a small follow-up `agreement_charges` lookup, same idiom as `agreement-finances.ts:getPaidCharges`) |
| `agreement-finances.ts` | Ledger-backed agreement-charge financial reporting — income fold-ins consumed by `lesson-finances.ts`, plus the Outstanding charges read — split out of `agreements.ts`, which keeps the agreement/charge record CRUD; completes the `lessons.ts`/`lesson-finances.ts` and `expenses.ts`/`expense-finances.ts` CRUD-vs-finances seam for the third revenue domain. Also exports `CHARGE_TRANSACTION_KINDS` (the `lease_charge`/`board_charge` kind pair), imported back by `agreements.ts`'s two record-surface ledger reads (`getChargesForAgreement`, `getUnpaidAgreementIds`): `getChargesForSummary(barnId, startDate, endDate, client?)` (#828, #865 — now calls `transactions.ts:getTransactionRows` with `kind IN ('lease_charge','board_charge')` instead of embedding the query directly, mapping `occurredAt`→`period`/`amount`→`fee`; same external `{period, fee, payment_type}[]` shape as before, so `lesson-finances.ts` needed no changes), `getPaidCharges(barnId, startDate, endDate, client?)` (#828, #865 — calls `getTransactionRows` with the same kind filter plus `collected: true`; `kind`/`membershipId`/`horseId` are already on the returned row, so only `agreementId` needs resolving — done via a small follow-up `agreement_charges` lookup keyed by the distinct non-null `agreementChargeId`s returned (a null id, defensive-only since no code path currently hard-deletes an `agreement_charges` row, skips the lookup and falls back to the transaction's own id), replacing the former FK-hint-pinned embed; `PaidCharge.riderId`/`horseId` are `string | null` (#865 — a rider's `membership_id` can be nulled by `ON DELETE SET NULL` when their membership is removed after a charge is already collected; `lesson-finances.ts`'s `getHorseIncomeSummary`/`getRiderIncomeSummary` fold a null into the same `NO_HORSE_LABEL`/`NO_RIDER_LABEL` bucket a lesson with no junction rows already falls into, rather than corrupting the grouping map with a literal `null` key)), `getOutstandingCharges(barnId, userId?, role?, client?)` (#831 — candidate unpaid-looking charges with `period` before the first of the current month, resolved to `kind`/`riderName` via a single-round-trip FK-hint embed as before — `getAgreementsMapForCharges`'s two-query join was deleted (#707); `role==='trainer'` short-circuits to `[]` since trainers aren't parties to agreements; `role==='rider'` first resolves the caller's own agreement IDs, then filters charges to those — but since `agreement_charges.payment_type` is gone, a candidate is only kept once `transactions.ts:getOutstandingTransactionRows` relays its `lease_charge`/`board_charge` transaction back as `collected=false`, same relay RPC `getOutstandingLessonRows` uses) |
| `expenses.ts` | Appointment CRUD — #1148 renamed `horse_expenses`/`expense_horses` to `appointments`/`appointment_horses` and moved `amount`/`payment_type` onto a manager-only `appointment_costs` row, so every read here now hydrates through a private `hydrate()` = `attachHorseNames` + `attachCosts` pair. `attachCosts(supabase, barnId, appointments)` flattens each appointment's cost row back on as `amount`/`payment_type` (a separate `.in('appointment_id', ids)` query, same idiom as `attachHorseNames` — the FK is composite, which is not the shape PostgREST embeds are reliable on), so every consumer of `HorseExpense` keeps reading the two fields where they always were. It has **no role branch, deliberately**: `appointment_costs` is manager-only RLS while its `authenticated` table grant stays, so a trainer's session gets zero rows rather than an error and both fields come out `null` — indistinguishable from a not-yet-priced appointment, which is exactly what the trainer-facing UI wants. `types.ts` splits the shape to match: `Appointment` is the bare row (what the write RPCs return), `HorseExpense extends Appointment` adds the two flattened fields, and `ScheduledExpense` became `ScheduledAppointment`. The DAL function names, the `expense_*` column names and the `HorseExpense`/`ExpenseWithHorses` type names are all unchanged on purpose — the manager's surface is still "expenses" and renaming them would churn it for no behavioral gain. `getExpensesByBarn(barnId)` (all appointments with resolved horse names and costs, newest first); `getExpenseById(expenseId, barnId)` (returns `null` if not found); `createExpense(barnId, data, client?)` (calls `create_expense_with_horses` RPC) / `updateExpense(expenseId, barnId, updates, client?)` (calls `update_expense_with_horses` RPC) — both atomically write the `appointments` row and replace `appointment_horses` junction rows in one transaction, skipped when `applies_to_all_horses`; both RPCs also sync the `appointment_costs` row *and* a matching `expense`-kind `transactions` row (#829/#1148, see `sync_expense_transaction` in [`rpc.md`](rpc.md)) — no TS-level change, since the RPC names/params are unchanged; both now return `Appointment` rather than `HorseExpense`, since the row they hand back no longer carries the money (every caller uses only `.id`, or discards it); both now also forward `data.paymentType`/`updates.paymentType` as `p_payment_type` (#872), defaulting to `null` when omitted; `deleteExpense(expenseId, barnId, deleteCollectedTransactions = false, client?)` (#941 — calls the `delete_expense_with_transactions` RPC instead of a direct table delete, mirroring `lessons.ts`'s `deleteLesson`; an uncollected `expense`-kind `transactions` row is always deleted, a collected one only when `deleteCollectedTransactions` is `true` — the default keeps it, relying on the table's own `ON DELETE SET NULL (expense_id)` FK); `getOutstandingExpenses(barnId, timezone, client?)` (#872, renamed and broadened from `getPastDueExpenses` by #940; `timezone` param added #955) — Finances dashboard's **Outstanding Expenses** section: appointments with no `appointment_costs` row at all (still planned) *or* a cost row missing a payment type (amount known but never marked paid) whose due datetime (`expense_date`+`expense_time`, or end-of-day when time is null) has already passed **in the barn's own local time** (`timezone` — `barns.timezone`), sorted ascending by that combined datetime. #1148 moved that predicate from a query-level `.or('amount.is.null,payment_type.is.null')` into JS over the attached cost — neither column is on this table any more, and the past-due bound was already evaluated in JS for the reason below. This section is manager-only UI, so `attachCosts` always resolves real costs for it; `getOutstandingExpenses` (#955) replaced a naive `new Date(\`${expense_date}T${expense_time}Z\`)` cast — which mislabeled the barn-local digits as UTC, the same root-cause bug #935 fixed for `lesson_at` — with a wall-clock **string** comparison via `src/lib/barn-timezone.ts`'s `instantToLocalWallClock(instant, timezone)`: it renders `now` as the wall-clock time it corresponds to in the barn's zone, then compares that string directly against the expense's own entered `expense_date`+`expense_time` digits — both live in the same "naive local" frame, so this sidesteps ever having to encode a wall-clock+zone pair back into a real instant. (#1015 removed `getUpcomingScheduledExpenses`, the same idiom's other former user — its only caller, the dashboard's Today/This-Week list, was replaced by the Day view, which sources planned expenses via `getScheduleForRange` + `getExpensesByIds` instead, see `schedule.ts`/`getExpensesByIds` below); `getRecentRecipients(barnId)` (ranked by recent-6-months frequency, then all-time frequency, then alphabetical — every recipient stays listed, not just recent ones); `getRecentExpenseTypes(barnId)` (all-time frequency); `getMostCommonTypeForRecipient(barnId, recipient)` (returns `null` if the recipient has no expenses); `getExpensesByIds(barnId, ids)` (#1015 — empty-array short-circuit, else scoped `.in('id', ids)` fetch through the same `hydrate` helper `getExpensesByBarn` uses; hydrates `getScheduleForRange`'s bare appointment ids into display data for the dashboard's Day view — this is the trainer dashboard's hydration path too, not just the manager's, so it goes through the same role-blind `attachCosts`) |
| `expense-finances.ts` | Ledger-backed expense financial reporting, split out of `expenses.ts` — mirrors the `lessons.ts`/`lesson-finances.ts` seam: a private `fetchExpenseTransactionsInRange(supabase, barnId, startDate, endDate, timezone)` helper (#829, #865, #949; `timezone` param added #955) shared by all four functions below — calls `transactions.ts:getTransactionRows` for the base `expense`-kind rows, then resolves `applies_to_all_horses`, `recipient`, and `expense_type` (the extra fields it needs) via a small follow-up `appointments` lookup keyed by the distinct non-null `expenseId`s, replacing the former FK-hint-pinned embed; sign-flips `amount` back to positive via `transactions.ts:positiveAmount`; each row's `expense_date` is decoded from its `occurredAt` instant via `instantToLocalWallClock(new Date(occurredAt), timezone)` (`src/lib/barn-timezone.ts`) rather than a naive `occurredAt.slice(0, 10)` (#955) — once `occurredAt` is a genuine local-aware instant (see `ExpenseForm`'s `p_occurred_at` wiring below), slicing the raw ISO string reads the wrong calendar day for any entry near the barn's local midnight boundary; `agreement-finances.ts`'s own `occurredAt.slice(0, 10)` calls are deliberately unchanged — those `occurred_at` values are always constructed at exact UTC midnight of a period's first day (no time-of-day ever enters them), so naive slicing is already correct there and barn-timezone decoding would introduce a *new* off-by-one-day bug instead of fixing one; since a `transactions` row only exists once an expense's amount is known, a planned expense is simply absent from the result — no separate null-`amount` filter needed anymore; `expenseId` is nullable via `ON DELETE SET NULL` (`deleteExpense`'s default keeps the collected `transactions` row when deleting the `appointments` row, #941, so a deleted-but-already-collected expense's row survives unless the caller opts into `deleteCollectedTransactions`) — such a row's amount still counts toward `totalExpenses`/detail totals (mirroring `lesson-finance-queries.ts`'s orphaned-`lessonId` precedent), but it falls back to the transaction's own id as its `id`, `applies_to_all_horses=false`, and `recipient`/`expense_type=null`, so it never matches a real `appointments`/`appointment_horses` row and naturally drops out of every per-horse/per-recipient breakdown instead of corrupting one; a duplicated `appointment_horses` junction-fetch block that used to live independently in each of the two per-horse functions below was extracted into a shared private `getExpenseHorseJunctionRows(supabase, barnId, expenseIds)` helper (#865); `getExpenseFinancialSummary(barnId, startDate, endDate, timezone)` / `getHorseExpenseDetail(barnId, horseId, startDate, endDate, timezone)` (#829 — read via `fetchExpenseTransactionsInRange` instead of the appointment row's own (now nonexistent) `amount` column; barn-wide expenses split equally across horses active on `expense_date`, using `horses.created_at`/`deactivated_at`; non-barn-wide expenses split across their `appointment_horses` rows — this downstream splitting logic is unchanged); `getRecipientExpenseSummary(barnId, startDate, endDate, timezone)` (#949 — groups by the raw `recipient` string, since it has no id anywhere in the schema; rows with no resolved recipient, i.e. an orphaned transaction, are skipped from the breakdown the same way an orphaned row already drops out of the horse breakdown; sorted descending by total) / `getRecipientExpenseDetail(barnId, recipient, startDate, endDate, timezone)` (#949 — filters to an exact `recipient` string match, no split since a recipient is 1:1 with each of its own expenses unlike the horse/rider breakdowns); |
| `barns.ts` | Barn lookups; `updateBarnDefaultBoardFee(barnId, defaultBoardFee, client?)` — manager-only write, backed by an RLS policy scoped by `auth_is_barn_manager(id)`; `setInstructorCut(barnId, value, client?)` — calls `set_instructor_cut` RPC, which now writes `barns.default_instructor_cut` (#776; function/wrapper names unchanged); `updateExhaustionThresholds(barnId, { moderate, high }, client?)` — direct table update of `exhaustion_threshold_moderate`/`exhaustion_threshold_high`, backed by the same `manager_update_barns` RLS policy as `updateBarnDefaultBoardFee`; `updateBarnTimezone(barnId, timezone, client?)` (#955) — direct table update of `timezone`, same RLS policy, mirroring `updateBarnDefaultBoardFee`'s shape exactly; `updateScheduleBufferMinutes(barnId, minutes, client?)` (#1017) — direct table update of `schedule_buffer_minutes`, same shape/RLS again; used by the Manage Barn settings page (`/barn/[slug]/settings`); `countDemoBarns(client)`/`getOldestDemoBarn(client)`/`deleteBarn(barnId, client)`/`createDemoBarn(slug, client)` (#505) — `is_demo=true` count/oldest-by-`created_at` lookup, a plain-id delete, and an `is_demo: true` insert, all requiring a service-role `client` (no request-scoped default — a regular authenticated user has no RLS path to any of these), used by `/demo`'s `createOrResumeDemoBarn` action for the `DEMO_BARN_CAP` reap check and new-barn creation, and by `/api/cron/reset-demo` (#506) for its hourly age/cap reap loop (`createDemoBarn` not used there — the cron route only reaps, never creates) |
| `barn-memberships.ts` | Membership CRUD and reads; cross-barn user lookup (`getBarnMembershipsForUser` — excludes `barns.is_demo=true` rows, #504; the sole source feeding `/` redirect logic, `/barns`, and `BarnSwitcher`, so demo barns are hidden from all three at once); `getUserMembership(userId, barnId, client?)` — single-membership lookup for a user in a specific barn, returns `null` if not found; `createActiveMembership(userId, profileId, barnId, role, client?)` (#505) — inserts a `status: 'active'` membership directly (vs. `createPendingMembership`'s `status: 'pending'`), used by `/demo`'s `createOrResumeDemoBarn` to grant the visitor an immediate manager membership on their new demo barn; `getActiveMembersWithProfiles(barnId, role)` and `getInstructorsByBarn(barnId)` share an exported `joinMembershipsWithProfiles(supabase, applyFilter)` helper (plus `fetchProfilesById`/`ActiveMemberSummaryRow`) — one `barn_memberships` → `profiles` two-query join, parameterized by each caller's own membership filter — rather than each independently querying and `.find()`-joining; these two are exported (not private) so `member-names.ts`'s `resolveMemberNames` (#869 — split out of this file, see below) can reuse the same join primitive instead of duplicating it; `getActiveMembersWithProfiles(barnId, role, client?)` returns active members with names joined from `profiles` via `profile_id`, `{ membershipId, userId, name, isManaged, inviteToken }` — since #779, rows the direct query's narrow RLS doesn't cover (e.g. a rider listing managers/trainers/other riders) are merged in via the `get_active_barn_member_summaries` RPC fallback, with `inviteToken: null` on those rows (never sourced from the RPC, which excludes the column entirely); the RPC fallback is skipped entirely when a caller supplies its own `client` (`seed-barn.ts`/`seed-test-barn.ts`, always service-role) — service-role already bypasses RLS on the direct query, so there are no gap rows to backfill, and the RPC call is simply never reached in that case (#930 added a `service_role` EXECUTE grant to `get_active_barn_member_summaries` for other callers, but that's orthogonal to this early return); `getMembershipByIdForBarn(membershipId, barnId, client?)` mirrors that same direct-query-then-RPC-fallback shape for a single row (falls back to `getMembershipById` first, then the RPC filtered client-side to the one row, reconstructing `invite_token: null`) — used by the member detail page, which now needs to resolve any active member's row, not just ones the narrow policies already cover; `getInstructorsByBarn` returns `{ membershipId, userId, name }[]` for any active, `can_instruct=true` membership, including stub trainers (`user_id IS NULL`) — used to populate the lesson form's instructor select and to validate a manager-submitted `instructor_id`; `getActiveManagerUserIds(barnId, client?)` — returns the `user_id`s of a barn's active managers (nulls filtered), consolidating a filter previously reimplemented at each recipient-resolution call site (`lessons.ts`, `generate-recurring-lessons.ts`, `generate-outstanding-notifications.ts`); `setCanInstruct(membershipId, barnId, value, client?)` — calls the `set_can_instruct` RPC; used by `setCanInstructAction` on the member detail page's Instructor Access toggle |
| `member-names.ts` | Cross-cutting membership name resolution, split out of `barn-memberships.ts` (#869) since four other DAL files (`agreements.ts`, `lessons.ts`, `lesson-finances.ts`, `lesson-participants.ts`) plus `documents.ts` and several pages depended on it without any signal that this is where it lives. `resolveMemberNames(membershipIds, barnId, client?)` resolves membership IDs to a `Map<membershipId, "First Last">` via `profile_id` FK join (reusing `barn-memberships.ts`'s exported `joinMembershipsWithProfiles`/`fetchProfilesById`), falling back to raw membership ID; for any membership ID the base RLS-scoped query doesn't return (e.g. an instructor's row on a lesson the caller isn't the instructor of), it calls the `get_instructor_membership_names` RPC once with the remaining IDs and merges any resolved names in, rather than granting broad row-level `barn_memberships` access via RLS (see [`rpc.md`](rpc.md) — this replaced an earlier `barn_memberships_read_as_instructor` RLS policy that over-exposed `invite_token`, #739); any membership ID still unresolved after that (e.g. a co-rider on a group lesson, not an instructor the caller can already see) gets a second-tier fallback to the broader `get_active_barn_member_summaries` RPC (#845 — same column-limiting rationale, never exposes `invite_token`), resolving the profile directly rather than re-querying `barn_memberships`; used directly by `documents.ts`'s `getDueDocuments` (#738) to resolve trainer/rider document owner names, now that `rider_documents.rider_id`/`staff_documents.trainer_id` are membership ids themselves. |
| `member-invites.ts` | Managed-member/invite workflow, split out of `barn-memberships.ts` (#869). `createManagedMember(barnId, firstName, lastName, role, client?)` — calls `create_managed_member` RPC to insert a stub profile (`is_managed=true`, null email) + active membership of the given role with a random `invite_token`, returns `{ membershipId }`; `claimManagedMember(token, userId, email, client?)` — calls `claim_managed_member` RPC to atomically link a real auth user to a stub; `revokeInviteToken(membershipId, barnId, client?)` — regenerates the `invite_token` and returns the new UUID. |
| `horses.ts` | Horse registry; per-horse exertion summary (`getHorseExertionSummary`, its `HorseExertionSummary` rows include `registered_name` (#1001), read from `get_horse_exertion_summary`'s own column since the manager/trainer Horses-list path reads this RPC rather than the raw table); `getHorsesByBarn` filters to active only; `getOwnedHorses(barnId, membershipId)` (#1000) — barn-scoped lookup of horses where `owning_member_id = membershipId`, ordered by name, `is_active` not filtered (unlike `getHorsesByBarn`) so an owner's deactivated horse still surfaces in the Horses-list page's My Horses section, badged Inactive; no RLS change needed since `horses_member_read` already grants barn-wide `SELECT` to every active member regardless of role; `getHorsesByIds(horseIds, barnId)` — bulk lookup by ID list scoped to barn, `is_active` not filtered (so a horse deactivated after being assigned to a lesson can still be resolved) — used by `getProjectedExhaustionForBarn` so exhaustion data is scoped to the exact horse set a caller is rendering, not an independent barn-wide active-only query; `getHorseById(horseId, barnId, client?)` — single horse lookup, returns `null` if not found; `createHorse(barnId, name, owningMemberId?, client?)` (#998) — `owningMemberId` defaults `owning_member_id` at creation, `null` when omitted; all 3 app call sites (manual "Add Horse", and the two inline-new-horse paths in `submitLesson`/`updateLessonAction`) are already manager-gated, so each passes the creating manager's own membership id; `updateHorseDetails(horseId, barnId, updates)` — atomic update (via `update_horse_details` RPC) for `name?`, `is_active`, `is_available`, `unavailability_reason`, `exhaustion_thresholds: { moderate, high } | null`, `feed_notes`/`medication_notes: string | null`, `registered_name: string | null`, and `owning_member_id: string | null` (#759 — folded into this single RPC call so the horse detail page's one Save button writes both status and thresholds in one transaction, rather than two independent DB round-trips that could partially fail; passing `exhaustion_thresholds: null` nulls both threshold columns, reverting the horse to barn defaults; `feed_notes`/`medication_notes` (#1005), `registered_name` (#1001), and `owning_member_id` (#998, review follow-up — an initial separate `updateHorseOwner` column update broke this same atomicity guarantee, so it was folded into this RPC instead, growing it to 12 params) are all written as-given, including `null` to clear the field, same as the thresholds); the RPC derives `deactivated_at` from the row's own prior `is_active` value in the same statement — set to `now()` on true→false, cleared to `NULL` on false→true, left untouched otherwise — no separate read step; `resolveHorseNames(horseIds, barnId, client?)` — resolves a list of horse IDs to a `Map<horseId, name>` scoped to `barnId`; callers fall back to raw horse ID when ID is absent from the map; `getHorseProjectedExhaustion(horseId, barnId, targetDate, excludeLessonId?)` — calls the `get_horse_projected_exhaustion` RPC, returns `{ lessonAt: string; exertionLevel: number }[]`; `updateHorseNotes(horseId, barnId, updates: { feed_notes: string | null; medication_notes: string | null })` (#1006) — calls the `update_horse_notes` RPC, the owner-write counterpart to `updateHorseDetails`'s manager-only write of these same two columns; throws on any RPC error (`not_authorized`/`horse_not_found`) rather than swallowing it; `updateHorsePhotoPath(horseId, barnId, photoPath: string | null, client?)` (#1002; RPC-backed since #1003) — calls the `update_horse_photo` RPC, which arbitrates owner-vs-manager write access dynamically (see `photo_uploaded_by` in `schema.md`) since `horses` has no owner-write table grant, only a manager-only RLS policy that a plain column update would incorrectly rely on; when a `client` is injected (the #1038 script path), bypasses the RPC entirely with a raw `.update()` — service-role calls have no `auth.uid()` for the RPC's internal check to key off, so this mirrors the RLS-bypass a plain update would have gotten for free before #1003 introduced the RPC; the injected-client update clears `photo_uploaded_by` to `null` only when clearing `photo_path` too (matching the RPC's delete-unlocks behavior) and leaves it untouched on a set, since a script has no acting membership to attribute the upload to; `replaceHorsePhoto(horseId, barnId, file, ext, client?)` (#1002, `client?` added #1038 so `seed-barn.ts` can seed a horse photo outside a request scope) — re-reads the horse's current `photo_path` itself (rather than trusting a value the caller captured earlier) so a concurrent replace/remove can't be clobbered by a stale caller, uploads the new object via `document-storage.ts`'s `uploadFile`, calls `updateHorsePhotoPath` with the new path (rolling back the just-uploaded object if that write fails), then best-effort removes the previously-current object via `removeFile`; `removeHorsePhoto(horseId, barnId, client?)` (#1002) — same fresh-read approach: re-fetches the current `photo_path`, no-ops if already absent, otherwise nulls the column and best-effort removes the object; both functions are the DAL-layer home for the upload/replace/remove orchestration the horse-photo actions call into, built on `document-storage.ts`'s generic helpers rather than duplicating storage calls in the action; `resolveExhaustionThresholds(horse, barn)` — pure function, accepts anything with the two `exhaustion_threshold_*` fields (`Horse` or `HorseExertionSummary` both satisfy it), resolves `{ high, moderate }` per-field, preferring the horse's own `exhaustion_threshold_high`/`exhaustion_threshold_moderate` override and falling back to the barn's default for whichever field is `null`; `getExhaustionBand(total, thresholds)` — pure function, returns `'low'` (`total <= moderate`), `'moderate'` (`moderate < total <= high`), or `'high'` (`total > high`); defined in `src/lib/exhaustion-band.ts` (kept out of this file, which imports `@/lib/supabase/server`, a server-only module, so `ExhaustionBar.tsx`, a Client Component, can import it without bundling server-only code) — the former back-compat re-export from this file was removed once nothing imported it here; `getUpcomingLessonsForHorse(horseId, barnId)` (#999) — two-step lookup (lesson ids from `lesson_horses`, then those `lessons` filtered to `cancelled_at IS NULL` and `lesson_at >= now()`, ordered ascending) rather than an embedded `lessons!inner` join, mirroring `getLessonsByBarn`'s rider branch and avoiding the FK-embed ambiguity gotcha `agreement-finances.ts` documents (#665); backs the horse detail page's Upcoming Lessons accordion — RLS already scopes visibility correctly per role (manager/trainer see everything, a privileged rider sees only lessons involving their horse), so this needs no privilege check of its own |
| `member-horse-privileges.ts` | Manager-only CRUD for `member_horse_privileges` (#998, foundation from #997) — `getHorsePrivileges(horseId, barnId)` lists a horse's grants; `grantHorsePrivilege(horseId, barnId, memberId)` inserts a row with column defaults (`document_privileges='none'`, `lesson_read_privileges=false`) — the horse detail page's Access section then flips these individually; `updateHorsePrivilegeDocumentAccess(privilegeId, barnId, value)`/`updateHorsePrivilegeLessonAccess(privilegeId, barnId, value)` — single-column updates, one per toggle (mirrors `barn-memberships.ts`'s `setCanInstruct` single-flag idiom rather than one combined upsert, since the document select and lesson toggle fire independently in the UI); `revokeHorsePrivilege(privilegeId, barnId)` calls the `revoke_horse_privilege` RPC (#998 redesign — the horse detail page's Access table now also doubles as the owner picker, so revoking a grant must atomically clear `horses.owning_member_id` if that member was the current owner; every other function here still needs no RPC, since `member_horse_privileges`' `manager_all_member_horse_privileges` RLS policy is already `FOR ALL`); `setHorseOwner(horseId, barnId, memberId)` (#1069, revised to close a non-atomic-write review follow-up) calls the `set_horse_owner` RPC — the forward-direction counterpart to `revokeHorsePrivilege`'s owner-clearing — atomically setting `horses.owning_member_id` and, when `memberId` is non-null, elevating that member's `member_horse_privileges` row (`document_privileges='write'`, `lesson_read_privileges=true`) in the same statement; a `null` `memberId` only clears ownership, no privilege row to elevate. Replaces the original two-call version (`updateHorseDetails` + a since-removed `elevateOwnerPrivileges`), which could leave a horse owned with privileges never elevated if the second call failed. **#999 additions**: `getMyHorseDocumentPrivilege(horseId, barnId)`/`getMyHorseLessonReadPrivilege(horseId, barnId)` — thin wrappers around the `auth_get_horse_document_privilege`/`auth_has_horse_lesson_read_privilege` `SECURITY DEFINER` RPCs (already `GRANT EXECUTE`d to `authenticated` for RLS's own use since #997), letting a rider caller look up their own privilege for a horse directly — `member_horse_privileges` itself has no rider-facing RLS grant, so a plain `SELECT` isn't an option the way it is for `getHorsePrivileges`' manager-only callers; used by the horse detail page to gate its Documents/Exhaustion/Upcoming-Lessons sections. |
| `lessons.ts` | Lesson CRUD: `createLesson`, `getLessonsByBarn(barnId, userId, role)` — manager and trainer both get all barn lessons (unfiltered by instructor — a trainer sees every instructor's lessons, not just their own); rider: lessons via `lesson_riders` enrollment (resolved through `barn_memberships`, no `cancelled_at` filter — a rider's own cancelled participation still appears in their list); `payment_type` on every returned lesson is overlaid post-hydration from the `get_lesson_payment_info` RPC (#885, see [`rpc.md`](rpc.md)) rather than selected directly off `lessons` — that column is no longer written by any RPC, so trusting it directly would silently go stale; returns `LessonWithDetails[]` (includes `horse_names`, `horse_ids`, `rider_names`, `rider_ids`, `rider_cancelled_ats`, `instructor_name` resolved via `resolveMemberNames` alongside rider names — parallel arrays built together so indexes always correspond; also `needs_attention` (#847) — `true` when any assigned horse is `is_active=false` or `is_available=false`, computed in `hydrateParticipants` from horse status embedded directly onto its existing `lesson_horses` query (`horses!lesson_horses_barn_id_horse_id_fkey(is_active, is_available)`) rather than a second round trip to `horses`; time-agnostic — the future/non-cancelled gating that decides whether the "Needs Attention" badge actually renders happens in `LessonListItem`/`UpcomingLessonCard`, the same pattern the existing `Unpaid` badge already uses); `getLessonById(lessonId, barnId, role)` — `role` is required (no default), though #999 left it functionally unused inside the function itself: both branches that used to key off it (the rider-role select-string trim for `private_notes`, and a `callerMembershipId`-anchored own-row masking for `rider_notes`) moved into RPCs that do that filtering server-side instead (below) — `role` stays part of the signature as a caller-facing contract rather than being dropped, since every call site already has it in hand; `payment_type` is likewise overlaid from `get_lesson_payment_info` (#885), same rationale as `getLessonsByBarn` above; the `lesson_horses.horses` embed also selects `is_active`/`is_available`/`unavailability_reason` (#847/#848) so the detail and edit pages can render per-horse status badges and the `HorseStatusBanner` (see [`routes.md`](routes.md)) without a second query; `exertion_level` is never selected directly off `lesson_horses`, and `rider_notes`/`private_notes` are never selected directly off `lesson_riders`, for any role (#937/#999 review follow-ups — see `get_lesson_horse_exertion_levels`/`get_lesson_rider_notes` in [`rpc.md`](rpc.md) for why a per-role select string alone can't restrict either) — private `fetchExertionLevels`/`fetchRiderNotes` helpers call those RPCs unconditionally (the DB does the per-role/per-row filtering now, not the caller) and merge the results onto each returned `lesson_horses`/`lesson_riders` row by `horse_id`/`rider_id`, leaving `exertion_level` `undefined` and `rider_notes`/`private_notes` `null` when the RPC returns no matching row; rider names are resolved via `resolveMemberNames` (membership ids collected from `lesson_riders.rider_id` — a plain column, not the nested `barn_memberships` embed); `instructor_user_id`/`instructor_name` are resolved separately, in one shot, via `getMembershipByIdForBarn(instructorId, barnId, client)` (rather than a nested instructor embed, #845 — PostgREST enforces `barn_memberships` RLS independently on a nested embed, so a rider caller previously got `null` back for both a co-rider's and the instructor's embed, silently blanking co-rider names and dropping the instructor from the self-cancel notification in `cancelRiderParticipationAction`) followed by a `getProfileById(profileId)` lookup for the name — kept out of the `resolveMemberNames` rider batch so a rider caller viewing a lesson taught by an instructor they can't otherwise see doesn't trigger the same `get_active_barn_member_summaries` RPC fallback twice (once inside `resolveMemberNames`, once inside `getMembershipByIdForBarn`) for the same instructor; a managed/stub instructor with `user_id IS NULL` still resolves a name this way. `cancelLesson(lessonId, barnId, notes?, isLate = false)` — thin wrapper over the `cancel_lesson_with_transactions` RPC (#830, replacing a former raw `.from('lessons').update(...)` call — same external signature, unchanged callers): soft-cancels a lesson, sets `cancelled_at=now()` and saves `cancellation_notes`; zeroes `fee`/nulls `payment_type` unless `isLate` is `true` (a normal lesson's rider-attributed, within-24h cancellation — see `cancelLessonAction` below — keeps the fee); also syncs a normal lesson's `rider_cancellation_fee`/`lesson_fee` transactions via `sync_rider_cancellation_fee` (see [`rpc.md`](rpc.md)) — needed since #825's Cancel UI consolidation left this as the still-reachable whole-lesson path for every normal-lesson cancellation; `deleteLesson(lessonId, barnId, deleteCollectedTransactions = false)` — hard-deletes a `lessons` row outright (used for lessons entered by mistake, as opposed to `cancelLesson`'s soft-cancel, which preserves history); `lesson_horses`/`lesson_riders` still cascade via their existing `ON DELETE CASCADE` FKs, but the delete itself now goes through the `delete_lesson_with_transactions` RPC (#827, see [`rpc.md`](rpc.md)) instead of a plain scoped `.delete()`, since deciding what happens to the lesson's `transactions` rows requires `SECURITY DEFINER`; `collectLessonPayment(lessonId, barnId, paymentType)` (#827) — thin wrapper over the `collect_lesson_payment` RPC (see [`rpc.md`](rpc.md)), called by `updatePaymentTypeAction`, which no longer dual-writes `lessons.payment_type` afterward (#885 — that column is read-overlaid from `transactions` now, see `getLessonsByBarn`/`getLessonById` above, so the dual-write was redundant); `updateLesson(lessonId, barnId, updates: Partial<Pick<Lesson, 'fee' | 'lesson_at' | 'jumping' | 'lesson_type' | 'tier_name' | 'cancellation_notes'>>)` (#885 — `'payment_type'` dropped from this union; `lessons.payment_type` is no longer an app-writable column) — `cancellation_notes` is editable post-cancellation via the Cancellation Notes textarea in `LessonForm`'s edit-mode Notes section (#797, manager or instructing trainer only, shown only when the lesson is already cancelled), saved by `updateLessonAction` (`src/app/actions/lessons.ts`) alongside the horse/rider notes writes rather than a standalone action, independent of the initial cancel write, no new RLS needed since trainers already have `UPDATE own (any column)` on `lessons`; `updateLessonAction` re-fetches the lesson server-side and rejects the write with `{ error: 'lesson is not cancelled' }` when a `cancellation_notes` field is submitted for a lesson whose `cancelled_at` is still `NULL` — this mirrors the guard the deleted standalone `updateCancellationNotesAction` (#741) used to enforce, since a direct Server Action call isn't limited to what `LessonForm`'s conditional rendering submits; `getUpcomingLessons(barnId, from, to, userId, role)` — manager: barn-wide, no instructor filter (#1008 fix — was previously scoped identically to trainer, so a non-instructing manager saw zero lessons on the dashboard); trainer: resolves the caller's own `barn_memberships.id` via `getUserMembership` and filters by `instructor_id`, same split `getOutstandingLessonRows` already uses; rider: resolves via `barn_memberships → lesson_riders → lessons`; `getLessonsByIds(barnId, ids)` (#1015 — empty-array short-circuit, else scoped `.in('id', ids)` fetch through the same `hydrateParticipants` helper `getUpcomingLessons`/`getLessonsByBarn` use; no role param — the id list handed in is already role-scoped by the caller via `schedule.ts:scopeScheduleItemsForRole` + `getScheduleForRange`, and RLS still applies independently to this query) |
| `lesson-participants.ts` | Participant management: `createLessonWithParticipants`, `updateLessonWithParticipants`, `updateLessonRiderNotes` (writes `lesson_riders.rider_notes`/`private_notes` via the `update_lesson_rider_notes` RPC rather than a direct table update — see [`rpc.md`](rpc.md) — since a bare `.update().select()` would trip an implicit `RETURNING *` the caller no longer has `SELECT` privilege for; returns `void`, not the updated row), `updateLessonHorseNotes` (#1082 — same defect, same reason: writes `lesson_horses.horse_notes` with a direct `.update()` and deliberately no `.select()`, since a bare `.select()` trips the implicit `RETURNING *` against `exertion_level`, which is outside the column-restricted SELECT grant; needs no RPC unlike its rider counterpart, because `UPDATE` on `lesson_horses` is still table-wide granted and all three WHERE columns sit inside the granted SELECT list; returns `void`); `getRiderEnrolledLessonIds(barnId, userId, client?)` — resolves active rider membership then returns enrolled lesson IDs; `cancelRiderParticipation(lessonId, barnId, riderId, notes?, isLate)` — cancels a single rider's participation via the `cancel_rider_participation` RPC; sets `lesson_riders.cancelled_at=now()` and `cancellation_notes` (not `rider_notes`, which cancellation no longer touches); zeroes `lessons.fee` only when `isLate` is `false` (#885 — the RPC stopped also nulling `payment_type`, that column no longer exists); returns `boolean` — whether this call cascaded the whole lesson to cancelled (see the RPC's own doc entry in [`rpc.md`](rpc.md)) — so `cancelRiderParticipationAction` knows whether to also fire a `lesson_cancelled` notification. Neither `createLessonWithParticipants` nor `updateLessonWithParticipants` takes an `instructorCut` or passes `p_instructor_cut` — #1154 deleted that chain (a #776 leftover, forwarded but ignored all the way to the RPC), since `lessons.instructor_cut` is derived server-side; the RPC signatures still carry the parameter, for the reason recorded in [`rpc.md`](rpc.md). `updateCancellationFeePaymentType(lessonRiderId, barnId, paymentType)` (#831) — manager-only mark-paid/revert for a `rider_cancellation_fee` transaction, calling the `collect_rider_cancellation_fee` RPC; used by `updateCancellationFeePaymentTypeAction` (`src/app/actions/lessons.ts`), wired into `OutstandingTable`'s payment-type dropdown for a `cancellation_fee` row |
| `lesson-series.ts` | `createLessonSeries(params, client?)` — same param/return shape as `createLessonWithParticipants`, and like it passes no `p_instructor_cut` (#1154 — see `lesson-participants.ts` above); calls `create_lesson_series_with_participants` to atomically create the `lesson_series` template row and its first `lessons` row (tagged with `series_id`) in one transaction; used by `submitLesson` when the "Recurring (weekly)" checkbox is checked, in place of `createLessonWithParticipants`. `getSeriesById(seriesId, barnId, client?)` — barn-scoped lookup, returns `LessonSeries \| null`; used by the lesson edit page and `stopLessonSeriesAction`. `stopLessonSeries(seriesId, barnId, client?)` — direct table update setting `is_active=false`, scoped by id+barn_id, relying on `lesson_series`' own RLS (manager: any; trainer: own) for authorization; used by `stopLessonSeriesAction` in `src/app/actions/lessons.ts` and by the nightly `scripts/generate-recurring-lessons.ts` when a series' rider is no longer an active member. `generateNextLessonForSeries(seriesId, barnId, lessonAt, client?)` — thin wrapper calling the `generate_lesson_for_series` RPC (unchanged signature — the RPC reads `instructor_cut` from the stored series template); used by `scripts/generate-recurring-lessons.ts` |
| `lesson-finance-queries.ts` | Raw-row query layer for `lesson-finances.ts` and `outstanding.ts` (its two outstanding-row functions, `getOutstandingLessonRows`/`getOutstandingCancellationFeeRows`, are imported only by `outstanding.ts`; `schedule.ts` also reuses `getLessonJunctionRows`, and `backup.ts` reuses both it and `getLessonFeeRows`), each function taking an optional `client?: SupabaseClient` param (last, with `createClient()` fallback) — matching the convention used elsewhere in this layer (e.g. `resolveMemberNames`, `resolveHorseNames`). `getOutstandingLessonRows` — trainer-role filtering resolves the caller's own membership id via `getUserMembership` before filtering `instructor_id`, mirroring `lessons.ts`; stays gross (the returned `OutstandingLessonRow` type picks only `id`/`barn_id`/`lesson_at`/`instructor_id`/`fee` — the candidate query itself is a `select('*')`, so the `instructor_cut` restriction is type-level, not query-level), since Outstanding never applies the cut; queries candidate lessons (past, non-zero fee, role-scoped) directly off `lessons` (`fee`/`lesson_at`/`instructor_id` are unaffected by #831's column drop), then (#831) cross-references `transactions.ts:getOutstandingTransactionRows` — the `lessons.payment_type` column is gone, so a candidate is only kept when its `lesson_fee` transaction relays back `collected=false`; a late-cancelled lesson's `lesson_fee` row no longer exists at all (see `sync_rider_cancellation_fee`), so it naturally drops out here rather than needing an explicit `cancelled_at` filter. `getOutstandingCancellationFeeRows(barnId, userId?, role?, client?)` (#831) — the counterpart for late-cancellation debt: candidate cancelled `lesson_riders` rows (`cancelled_at IS NOT NULL`) come from a plain query relying on that table's own RLS for manager/rider scoping (manager and trainer are both barn-wide via `lesson_riders_select_staff`, so a trainer caller gets an explicit app-level filter to lessons they instruct, mirroring `getOutstandingLessonRows`'s own trainer scoping; a rider's RLS-enforced enrollment already limits them to their own cancelled participation, since a normal lesson has exactly one rider) — resolved lesson ids feed a follow-up `lessons` lookup for `lesson_at`/`instructor_id`, then the candidate `lesson_rider_id`s are relayed through `getOutstandingTransactionRows` the same way, filtered to uncollected `rider_cancellation_fee` rows. `getLessonFeeRows(barnId, startDate, endDate, client?)` (#827, replacing `getLessonsForSummary` and `getPaidLessonRows`) — every `lesson_fee`/`instructor_payout`/`rider_cancellation_fee` transaction in a barn-scoped `occurred_at` range, merged one row per lesson into `LessonFeeRow { lessonId, fee, instructorCut, collected, instructorId, occurredAt, tierName }`; (#865) now calls `transactions.ts:getTransactionRows` for the base rows, then resolves `tierName` — the only field not itself on `transactions` — via a small follow-up `lessons` lookup keyed by the distinct non-null resolved lesson ids (an orphaned/deleted-lesson row, resolved lesson id `null` via `ON DELETE SET NULL`, skips the lookup and keeps the `DELETED_LESSON_LABEL` fallback), replacing the former FK-hint-pinned embed; `instructorId` comes from the `instructor_payout` row's own `membershipId` (present only once collected) rather than a second join, and its `instructorCut` is sign-flipped via `transactions.ts:positiveAmount`. A late rider cancellation (#830) replaces a normal lesson's `lesson_fee` row with a `rider_cancellation_fee` row that carries `lesson_rider_id` instead of `lesson_id` — this function resolves that back to the lesson via a small follow-up `lesson_riders` lookup so it still merges with that lesson's `instructor_payout` row rather than appearing as a spurious zero-fee/negative-net orphan. Covers both paid and pending lessons in one query — `collected` distinguishes them — replacing the former split between `getLessonsForSummary` (all lessons) and `getPaidLessonRows` (paid-only, parameterized column selection). `getLessonJunctionRows(table, participantColumn, barnId, lessonIds, client?)` merges the former `getLessonRidersForLessons`/`getLessonHorsesForLessons` into one query parameterized by junction table (`'lesson_riders' \| 'lesson_horses'`) and participant column (`'rider_id' \| 'horse_id'`), keeping the same `{ lesson_id, [participantColumn]: string }` row shape as before so callers didn't need to rename any properties. `lesson-finance-queries.test.ts` mocks `@/lib/supabase/server` via `vi.mock`, same as the rest of the DAL test suite. No name resolution or aggregation — `lesson-finances.ts` is a pure projection layer over these raw rows. |
| `lesson-finances.ts` | Financial reporting, net of each lesson's own snapshotted instructor cut (#776 — moved off a shared barn-level rate; every income function now reads the cut from each row instead of accepting an `instructorCut` parameter). All lesson-derived rows now come from `lesson-finance-queries.ts:getLessonFeeRows` (#827, reading the `transactions` ledger) rather than the `lessons` table directly — **except** `getOutstandingLessons` (moved to `outstanding.ts`), which still reads candidate lessons off `lessons` itself (unaffected by the ledger migration — only `payment_type` moved, and `fee`/`lesson_at`/`instructor_id` never did) and cross-references `transactions` via `lesson-finance-queries.ts:getOutstandingLessonRows`'s own `get_outstanding_transactions` relay instead (#831; see the `transactions` table entry in [`schema.md`](schema.md) and the `lesson-finance-queries.ts` entry above). `getHorseIncomeSummary`/`getRiderIncomeSummary`/`getTrainerIncomeSummary`/`getHorseIncomeDetail`/`getRiderIncomeDetail`/`getTrainerIncomeDetail` (#866) are now thin wrappers over one shared `getEntityIncome(descriptor: EntityIncomeDescriptor, mode: 'summary' | 'detail', barnId, startDate, endDate, targetId?)` core (exported, overloaded on `mode` for return-type narrowing) — replacing what used to be six near-identical copy-pasted fetch→group/detail→fold-charges→resolve-names pipelines. `EntityIncomeDescriptor` makes the horse/rider/trainer differences explicit data instead of implicit drift between functions: `junctionTable`/`participantColumn` (`'lesson_horses'`/`'horse_id'`, `'lesson_riders'`/`'rider_id'`, or `null` for trainer — `lessons.instructor_id` is a single column, not a many-to-many, so trainer keys directly off `LessonFeeRow.instructorId` with no junction query), `fallbackLabel` (`NO_HORSE_LABEL`/`NO_RIDER_LABEL`/`NO_INSTRUCTOR_LABEL`), `chargesApply` (`true` for horse/rider — `getPaidCharges` folds in per-entity, unsplit; `false` for trainer — `getChargesForSummary` instead folds into one synthetic `NON_LESSON_INCOME_LABEL` row, since agreements have no instructor party), `getChargeEntityId` (horse/rider only — `(c) => c.horseId`/`(c) => c.riderId`, both `string | null`: #865 — `getPaidCharges` returns `null` when a charge's rider membership or horse relation was cleared by `ON DELETE SET NULL`; the charge-folding loop in `getEntityIncomeSummary` falls back to `descriptor.fallbackLabel` in that case rather than grouping under a literal `null` key), `resolveNames`, `includeGrossIncome` (trainer-only — raw pre-cut fee sum per entity), and `splitsGrossFee` (#971, horse/rider only — see below). The three descriptors (`HORSE_INCOME_DESCRIPTOR`, `RIDER_INCOME_DESCRIPTOR`, `TRAINER_INCOME_DESCRIPTOR`) are exported constants; each of the six wrappers calls `getEntityIncome` with its descriptor + mode and maps the generic `{ id, name, totalIncome, grossIncome }` / `{ name, rows, chargeRows, total }` shape onto its own public type (field renames only — `id`→`horseId`/`riderId`/`trainerId`, generic `count`→`horseCount`/`riderCount`, dropped entirely for trainer detail rows since a trainer's fee is never split). Internally, `getEntityIncome` still routes through the same `computeGroupedIncome<T extends { fee: number; instructorCut: number }>(rows, key: (row: T) => string[], fallbackLabel)` pipeline (pure, exported, directly unit-tested) — the single call site for cut-subtraction (`splitNetFee(row.fee, row.instructorCut, ...)`, called once per row before splitting across `key(row)`'s participant keys) and for the "no participant" fallback-row accumulation (rows with zero keys fold into a `Map` entry keyed by `fallbackLabel` instead of splitting); a private `toSortedIncomeRows` helper resolves names and sorts the grouped map descending by total while always appending the fallback row last, regardless of its value (never part of the value-sort); a private `foldChargesCollected` computes the count/total behind every `NON_LESSON_INCOME_LABEL` synthetic row, and a private `computeDetailRows` helper handles the per-lesson (not grouped) detail-mode split, both junction-based (horse/rider) and single-key (trainer, via a `lessonId → instructorId` map instead of a junction fetch). `getFinancialSummary` is unaffected by #866 (out of scope — it groups by tier, not by entity) and remains its own adapter over `computeGroupedIncome`. `getFinancialSummary(barnId, startDate, endDate)` (returns `collectedIncome`, `pendingIncome`, `breakdown` grouped by `tier_name` with `{ tierName, price, lessonCount, subtotal, instructorCut }[]` — `subtotal` is net, `instructorCut` is a direct sum of that tier group's own paid lessons' snapshotted `instructorCut` values (#776 — not `rate × lessonCount`; computed via a `cutByTier` map built alongside `tierGroups` from the same `paidLessons` array, so every key is guaranteed present) for that row's display column; folds in `agreement-finances.ts:getChargesForSummary` — paid charges add to `collectedIncome` at their full gross amount (charges are never cut — they're not lessons), and unpaid charges add to `pendingIncome` only when their `period` is the current month or later (mirroring the lesson-side `lesson_at > now` future-only rule, so a past-due unpaid charge is never counted in both `pendingIncome` for a past month and the all-time Outstanding total), and a synthetic `{ tierName: NON_LESSON_INCOME_LABEL, price: null, lessonCount: <charge count>, subtotal: <charges collected>, instructorCut: 0 }` row is appended to `breakdown` when any charges were collected; also appends a `$0` row — `{ lessonCount: 0, subtotal: 0, instructorCut: 0, price: tier.price }` — for every active barn tier (via `lesson-tiers.ts:getTiersByBarn`) not already present from a paid lesson that month, #771, so a barn's full current tier list stays visible even at zero collected — before the alphabetical sort, so these interleave with the real rows rather than trailing after them); `getTrainerIncomeDetail(barnId, trainerId, startDate, endDate)` (#771, per-trainer drill-down: returns `{ trainerName, rows: TrainerIncomeDetailRow[], total }` where each row is `{ lessonId, lessonAt, fee }`, `fee` net of that lesson's own snapshotted cut — no split/junction table like the horse/rider details use, since `lessons.instructor_id` is a single column matched directly against `trainerId`); `getHorseIncomeDetail(barnId, horseId, startDate, endDate)` (per-horse drill-down: returns `{ horseName, rows: HorseIncomeDetailRow[], chargeRows: HorseChargeDetailRow[], total }` where each lesson row has `{ lessonId, lessonAt, fee, horseCount, splitAmount }` — `fee` is the net (post-cut) lesson fee, not the raw stored value, and `splitAmount` divides that net fee across `horseCount` — and each charge row has `{ chargeId, agreementId, period, kind, fee }` at its full uncut amount; `total` sums both); `getRiderIncomeDetail(barnId, riderId, startDate, endDate)` (per-rider drill-down: returns `{ riderName, rows: RiderIncomeDetailRow[], chargeRows: RiderChargeDetailRow[], total }`, same net-fee/split shape as the horse detail). Negative net values (e.g. a comped lesson after the cut) are allowed and not clamped to zero — they display accounting-style via `src/lib/format-currency.ts`'s `formatCurrency`. `NON_LESSON_INCOME_LABEL` (`'Non-lesson income'`), `NO_INSTRUCTOR_LABEL` (`'No instructor'`), `NO_HORSE_LABEL` (`'No horse'`), and `NO_RIDER_LABEL` (`'No rider'`) are exported so the DAL-constructed rows and (as of #971, see the route entry in [`routes.md`](routes.md)) the page's footer-reconciliation math use the identical strings to filter/fold synthetic rows — these four no longer render as plain body rows in any table; their totals are folded into each table's `ReconciliationFoot` instead. **#971 update**: `HORSE_INCOME_DESCRIPTOR`/`RIDER_INCOME_DESCRIPTOR` gained `splitsGrossFee: true` — before `computeGroupedIncome` runs, each lesson's `instructorCut` is zeroed (`lessons.map(l => ({ ...l, instructorCut: 0 }))`), so the existing proportional-split logic yields the gross (pre-cut) fee split instead of the net one; `getHorseIncomeSummary`/`getRiderIncomeSummary`'s returned `totalIncome` is therefore gross as of this change (previously net-of-cut), fixing a bug where the old separate `grossByEntity` loop's `getKey(l)[0]`-based key selection would silently mis-split a group lesson's gross across only its *first* horse/rider key. Detail-mode (`getHorseIncomeDetail`/`getRiderIncomeDetail`) and the trainer descriptor's own `includeGrossIncome` mechanism are untouched — out of scope for #971. `computeHorseNetIncome(horseIncome, expenseBreakdown)` (pure, exported) merges `getHorseIncomeSummary`'s output with `expense-finances.ts:getExpenseFinancialSummary`'s per-horse `breakdown` by `horseId`; its output field was renamed `income`→`gross` (#971, matching the type's now-actually-gross value) and `net` is a single subtraction (`gross - expenses`, no longer double-subtracting the cut now that `gross` itself is pre-cut) — sorted by gross descending then name ascending; used by `finances/page.tsx`'s By Horse tab so that merge has a test surface of its own instead of living inline in the page. |
| `outstanding.ts` | Outstanding collections read layer, split out of `lesson-finances.ts` — Outstanding is gross, never net of the instructor cut, unlike every income function there. `getOutstandingLessons(barnId, userId?, role?, client?)` (returns `OutstandingLesson[]` with past unpaid lessons, fee ≠ 0, **gross** — Outstanding is a collections list, not an income statement, so the cut is never applied here; role-filtered: manager=all barn, trainer=own lessons by `instructor_id` (resolved to the caller's own membership id), rider=enrolled lessons via `lesson_riders`; `instructor_name` resolved via `resolveMemberNames` alongside rider names; **lesson-only** — does not itself know about agreement charges); `getOutstandingCancellationFees(barnId, userId?, role?, client?)` (#831 — the `rider_cancellation_fee` counterpart, same name-resolution shape over `lesson-finance-queries.ts:getOutstandingCancellationFeeRows`; returns `OutstandingCancellationFee[]` — `id` is the `lesson_rider_id` (used for the mark-paid action), `lessonId` the parent lesson (used for the Outstanding table's link), `riderName` resolved with the same rider-id-or-fallback pattern `getOutstandingCharges` uses, `instructorName` `null` when the row has no `instructorId` or it doesn't resolve); `getOutstandingCharges` (from `agreement-finances.ts`) and `mergeOutstandingItems(lessons, charges, cancellationFees = [])` (pure function, exported from this file, sorts the combined `OutstandingItem[]` by date — a `cancellation_fee` item's `linkId` carries its `lessonId` separately from `id`, since `id` is the `lesson_rider_id` the payment action needs) are composed together at the page layer (`finances/page.tsx`, `finances/outstanding/page.tsx`) rather than inside `getOutstandingLessons`, keeping that function's existing tests untouched. |
| `schedule.ts` | #1013/#1014: merged, duration-aware read of `lessons` + `appointments` + `barn_events` for availability/conflict checking — foundation for the scheduling/calendar work (day/week calendar views, instructor-proximity notification, `.ics` export, conflict picker). A UNION across indexed tables isn't a real bottleneck at barn scale — mirrors the `agreement_charges` vs. lessons precedent in Finances. `ScheduleItem { id, itemType: 'lesson' \| 'expense' \| 'event', start, durationMinutes, instructorId, horseIds, riderIds, exertionByHorseId, appliesToAllHorses, label }` (`types.ts`) — `start` is a **barn-local wall-clock string** (`"YYYY-MM-DDTHH:mm:ss"`), not a UTC instant: `lessons.lesson_at`/`barn_events.event_at` are true instants but `appointments.expense_date`/`expense_time` are barn-local digits (#935/#955), so lessons/events convert *down* into that frame via `instantToLocalWallClock` (`src/lib/barn-timezone.ts`) rather than inventing a wall-clock→instant conversion; never pipe `start` through `new Date(x)` or a barn formatter for display. `getScheduleForRange(barnId, from, to, timezone, client?)` — `from`/`to` are real instants (matches `lessons.ts:getUpcomingLessons`'s convention); excludes cancelled lessons (`cancelled_at IS NULL`, the same filter `getNearbyInstructorMembershipIds` below applies — not `getOutstandingLessonRows`, which has no `cancelled_at` filter and needs none: a normally-cancelled lesson's fee is zeroed and a late-cancelled lesson's `lesson_fee` transaction no longer exists, so both already drop out, see the `lesson-finance-queries.ts` row above) and expenses with a null `expense_time` (same coarse/precise wall-clock filtering idiom as `expenses.ts:getOutstandingExpenses`, the DB-level `getUpcomingScheduledExpenses` idiom's other former user before #1015 removed it); `barn_events` is queried by the same real-instant `event_at >= from AND event_at < to` bound as lessons, no wall-clock coarse/precise split needed; horse ids come from `lesson-finance-queries.ts:getLessonJunctionRows` (#1019 — once the exertion column moved out to its own RPC, the remaining `lesson_id`/`horse_id` select is exactly that helper's shape, same as `backup.ts`/`outstanding.ts` already use), rider ids from a direct `lesson_riders` query (that one can't use the helper — it also needs `cancelled_at`), and appointment horse ids from a small `appointment_horses` junction query (no name resolution, unlike `expenses.ts:attachHorseNames` — this is a raw-row foundation layer); events always carry `horseIds: []`/`riderIds: []`/`instructorId: null` — no horse, rider, or instructor association on `barn_events`. The three #1019 fields: `riderIds` is non-cancelled `lesson_riders.rider_id` only (`cancelled_at` is selected and filtered in JS rather than at the query level, matching what `lesson-participants.ts` already treats as still-enrolled — a cancelled rider is not a scheduling conflict), `exertionByHorseId` maps `horse_id → exertion_level` (a `NULL` level defaults to `0`) and is built from the `get_lesson_horse_exertion_levels_batch` RPC (see [`rpc.md`](rpc.md)) rather than from the `lesson_horses` select alongside it — `exertion_level` has no `SELECT` grant to `authenticated` at all (#937), so selecting it directly makes Postgres deny the *whole* `lesson_horses` query with `42501`; the two reads stay split because the RPC's filter (manager/trainer, or a rider holding `lesson_read_privileges` on the horse) is narrower than `lesson_horses` row RLS, so folding the horse ids into the RPC would drop rider-visible ids from the dashboard's Day view. The RPC is called unconditionally for every role, same as `lessons.ts:fetchExertionLevels` — the DB does the per-role filtering. `label` is display text for expense (`"{expense_type} — {recipient}"`) and event (`title`) items, `null` for lessons — the lesson form that consumes it already holds the horse and rider names its ids resolve to, so a server-built lesson label would be redundant. All three are inert for the dashboard Day/Week views, which merge by id against their own separately hydrated rows. `appliesToAllHorses` (#1147) carries `appointments.applies_to_all_horses` through on expense items (`false` for lessons/events): `create_expense_with_horses`/`update_expense_with_horses` skip the `appointment_horses` insert entirely for a barn-wide appointment, so it arrives with `horseIds: []` and the flag is the only signal that it concerns every horse — `month-calendar.ts`'s conflict-dot predicate fans that out at read time (`i.appliesToAllHorses || i.horseIds.some(...)`) rather than the junction rows being materialized, which would need a backfill *and* would silently exclude any horse added to the barn after the appointment was booked. It contributes no exertion: `windowTotal` sums lesson items only, so a vet/farrier day shades nothing. RLS: `lessons` SELECT already covers manager/trainer/enrolled-rider; `appointments`/`appointment_horses` SELECT is manager + trainer (#1019 added the trainer half so the lesson form's month conflict calendar could mark vet/farrier days for a trainer, who uses the same form; #1148 renamed both and moved the money the grant used to expose onto manager-only `appointment_costs`), so only a *rider* caller gets lesson items back with an empty expense set, silently (zero rows, not an error); `barn_events` SELECT (#1014) is role-filtered via `visible_to_roles` rather than manager-only, so a trainer/rider caller gets back whatever events include their role, also via RLS with no DAL-level check. `mergeScheduleItems(lessons, expenses, events = [])` (pure, sorts by `start` then `id` as a deterministic tiebreaker for identical timestamps — same class of fix #523 applied to `getUpcomingScheduledExpenses`) and `intervalsOverlap(a, b)` (pure — half-open interval `[start, start + durationMinutes)`; `LESSON_DURATION_MINUTES = 60` is a hardcoded constant, no `duration_minutes` column exists yet; an item ending exactly when another starts does not count as overlapping) are both independently exported/unit-tested. `isLessonNearby(aLessonAt, bLessonAt, bufferMinutes)` (#1017, pure) — two lessons are "nearby" iff `\|aStart - bStart\| < LESSON_DURATION_MINUTES + bufferMinutes`, which reduces to "the gap between them is less than the buffer" for non-overlapping lessons and always flags a true overlap regardless of buffer size (unlike a raw start-to-start distance check, which would miss overlaps whenever the gap is large relative to the buffer). `getNearbyInstructorMembershipIds(barnId, excludeLessonId, lessonAt, excludeInstructorId, bufferMinutes, client?)` (#1017) — queries `lessons` (barn-scoped, `cancelled_at IS NULL`, `instructor_id IS NOT NULL`, excludes the given lesson id) bounded by the same threshold on `lesson_at`, applies `isLessonNearby` in JS as defense-in-depth over that bound, dedupes to distinct `instructor_id`s, and excludes `excludeInstructorId` (no self-notification) — backs the instructor-proximity notification; returns barn-membership ids, leaving user-id resolution to each call site (`submitLesson`, `generate-recurring-lessons.ts`). `scopeScheduleItemsForRole(items, role, membershipId)` (#1015, pure) — `getScheduleForRange` has no manual role dispatch, which is correct for manager/rider/`barn_events` (all RLS-scoped already) but not for trainer + lessons: `lessons_select_staff` grants trainer the same barn-wide SELECT as manager, so without this filter a trainer would see every lesson in the barn on the dashboard's Day view instead of just their own; keeps `itemType !== 'lesson' \|\| instructorId === membershipId` for `role === 'trainer'`, passes everything through unchanged otherwise — the same "own scope" restriction `getUpcomingLessons` applies via its app-level `instructor_id` filter, reproduced here since `getScheduleForRange` itself doesn't. |
| `barn-events.ts` | #1014: `barn_events` CRUD — ad-hoc calendar entries that aren't a lesson or expense (e.g. a costume party), manager-owned but readable by any role listed in `visible_to_roles`. `getEventsByBarn(barnId)` (all events, `event_at` ascending), `getEventById(eventId, barnId)`, `createEvent(barnId, input: BarnEventInput)`, `updateEvent(eventId, barnId, input: BarnEventInput)`, `deleteEvent(eventId, barnId)` — hard delete, no soft-deactivate concept (unlike `lesson-tiers.ts`) since there's no financial history tied to an event. `BarnEventInput { title, eventAt, notes?, visibleToRoles: Role[] }` (`types.ts`) — `visibleToRoles` can be empty (a manager-only private note is a valid state, since the manager RLS policy grants SELECT unconditionally regardless of `visible_to_roles`). `getEventsByIds(barnId, ids)` (#1015 — empty-array short-circuit, else scoped `.in('id', ids)` fetch, no hydration needed since `BarnEvent` has no derived display fields; hydrates `getScheduleForRange`'s bare event ids into display data for the dashboard's Day view, same idiom as `lessons.ts:getLessonsByIds`/`expenses.ts:getExpensesByIds`) |
| `calendar-feed.ts` | #1018: per-membership `.ics` calendar feed token + role-filtered feed data, the DAL layer behind the token-authenticated `src/app/calendar.ics/route.ts` Route Handler (no session, so `requireMembership`/RLS don't apply there — see `get_calendar_feed` in [`rpc.md`](rpc.md)). `getOrCreateCalendarFeedToken(membershipId, barnId, client?)` — returns the existing `calendar_feed_token` if the row already has one, else delegates to `regenerateCalendarFeedToken`; `regenerateCalendarFeedToken(membershipId, barnId, client?)` — `crypto.randomUUID()` + `.update(...).select().single()`, exact mirror of `member-invites.ts`'s `revokeInviteToken` (old token is simply overwritten, no separate revoke step). `getCalendarFeedData(token, client?)` — calls the `get_calendar_feed` RPC and maps its `snake_case` row to `CalendarFeedData { valid, barnName, items: CalendarFeedItem[] }` (`types.ts`); `valid: false` means an unknown/inactive token, distinct from a valid token with an empty `items` array. The actual `.ics` text templating (VEVENT construction, RFC 5545 escaping/line-folding) is a separate pure-function module, `src/lib/ics.ts` (`buildIcsFeed`) — outside `src/lib/db/` since it has no Supabase dependency. |
| `lesson-tiers.ts` | Tier CRUD: `getTiersByBarn`, `createTier` (accepts optional `defaultExertionLevel`/`defaultJumping`/`instructorCut` — the last, added in #776, defaults to `0`), `updateTier` (accepts optional `default_exertion_level`/`default_jumping`/`instructor_cut` in updates), `deactivateTier`, `reactivateTier`, `setDefaultTier`, `getAllTiersByBarn` (incl. inactive), `getTierById` |
| `profiles.ts` | User profiles; `upsertProfile` (called at registration); `getProfileByUserId(userId, client?)` (single-user lookup by auth user ID; `client?` added #505 so `/demo`'s `createOrResumeDemoBarn` can run this check against the request-scoped client before any service-role work happens); `getProfileById(profileId, client?)` (single-profile lookup by `profiles.id` — used where the caller only has a `barn_memberships.profile_id`, e.g. the member detail page, so a managed/stub member with `user_id IS NULL` still resolves); `getProfilesByUserIds(userIds)` — bulk lookup by auth user ID array, returns empty array when list is empty; `updateProfile` (updates all five editable fields: first_name, last_name, phone, emergency_contact_name, emergency_contact_phone); `updateContactInfo` (updates phone/emergency contact fields; RLS enforces own-row for users, managed/stub profiles only for managers); `updateProfilePhotoPath(profileId, photoPath: string | null, client?)` (#1004) — plain `profiles` column update, not an RPC, mirroring `horses.ts`'s `updateHorsePhotoPath`; `replaceProfilePhoto(profileId, barnId, file, ext, client?)` (#1004, `client?` added #1038 so `seed-barn.ts` can seed a profile photo outside a request scope) — same re-read-current/upload/update-with-rollback/best-effort-cleanup-old-object shape as `replaceHorsePhoto`; `barnId` here is only used to build the `{barn_id}/profile-photos/{profile_id}/...` storage path (profiles have no `barn_id` column of their own) — the caller passes whatever barn context the upload happened under, since a profile's photo is visible across all the member's barns regardless of which one it was uploaded from; `removeProfilePhoto(profileId, client?)` (#1004) — same fresh-read/no-op-if-absent/null-and-remove shape as `removeHorsePhoto` |
| `notifications.ts` | Notification CRUD: `createNotification` (upserts on `user_id,barn_id,type`; resets `read_at` on conflict); `upsertNotification(client: SupabaseClient, params)` (required-client, no-fallback variant — needed because service-role callers have no `auth.uid()` to satisfy `create_or_update_notification`'s RLS-gated RPC — upserts directly against the table on the same `user_id,barn_id,type` conflict target, bypassing `create_or_update_notification`'s RPC for service-role callers whose `auth.uid()` is null; shared by `generate-recurring-lessons.ts` and `generate-outstanding-notifications.ts`); `deleteNotificationByType(userId, barnId, type, client?)` (deletes a notification by user+barn+type; no-op if not found); `upsertNotificationsForRecipients<T>(client, recipients, formatter, type, linkForBarn, send?)` (consolidates the "aggregate recipients into a Map, then send each with an isolated try/catch" pattern shared by the nightly notification scripts and the cancellation actions — iterates `recipients: Map<string, {userId, barnId, payload: T}>`, sends one notification per entry sequentially via `send` (defaults to `upsertNotification`'s raw table upsert, for service-role script callers), and returns the count of per-recipient failures rather than throwing; used by `generate-recurring-lessons.ts` (2 calls, `payload: number`) and `generate-outstanding-notifications.ts` (1 call, `payload: {count, total}`) with the default `send`; `cancelLessonAction`/`cancelRiderParticipationAction` (`src/app/actions/lesson-cancellation.ts`) pass a `send` override backed by `createNotification`'s RPC, since their session-authenticated client can't satisfy the raw upsert's RLS-gated `ON CONFLICT DO UPDATE` for another user's row); `resolveCancellationRecipients(params)` (pure — encodes "who gets notified for a cancellation," shared by `cancelLessonAction`/`cancelRiderParticipationAction`: whole-lesson cancel by trainer → managers + riders, by manager → instructor + riders; single-rider cancel by the rider themself → instructor + managers, by trainer on the rider's behalf → affected rider + managers, by manager on the rider's behalf → affected rider only); `markAllNotificationsRead` (sets `read_at = now()` for all unread for a user in a barn); `getNotifications(userId, barnId, limit=20)` (recent notifications ordered by `created_at DESC`); `formatNearbyInstructorNotification(count)` (pure — `instructor_lesson_nearby` title/body, singular/plural by count; shared by `submitLesson` and `generate-recurring-lessons.ts`'s batched aggregation, so both produce identical copy); `getUnreadNotificationCount(client, userId, barnId, type)` (#1017 review follow-up — parses the leading integer off an existing unread row's title, or `0` if none exists/unread; `instructor_lesson_nearby` is written by both `submitLesson` (always adds 1) and `generate-recurring-lessons.ts` (adds its own per-run aggregate) to the same `user_id,barn_id,type` upsert target, so each producer reads this first and adds to it instead of blindly overwriting the other's count — the same upsert-collision class #535 fixed for `lesson_cancelled` by splitting notification types instead, not viable here since both producers legitimately share one type) |
| `document-storage.ts` | Shared Supabase Storage helpers: `uploadFile(storagePath, file, contentType, client?, upsert = false)` (`upsert` added #995 so a Data Backup export can overwrite a barn's previous archive at a fixed `backup-archive/` path — both of `settings/actions.ts`'s download actions pass `true`, the #995 documents zip (`downloadAllDocumentsAction`) and the #994 data xlsx (`downloadBarnDataAction`); every other caller omits it and keeps the prior create-only behavior), `downloadFile(storagePath, client?)` (#995 — returns the object's `Blob`, used by `document-backup.ts` to read each document's bytes into the zip), `removeFile(storagePath, client?)` (`client?` added #1038 so `seed-barn.ts`'s photo seeding can inject a service-role client outside a request scope), `getSignedUrl(storagePath)` (300 s TTL), `validateFile(file, allowedMimeTypes?, allowedExtensions?)` (throws on invalid type/extension/size; returns lowercased extension on success; the two allow-list params default to `ALLOWED_MIME_TYPES`/`ALLOWED_EXTENSIONS` — #1002 added them so horse-photo uploads can pass the narrower `PHOTO_MIME_TYPES`/`PHOTO_EXTENSIONS` instead of duplicating the function); exports `ALLOWED_MIME_TYPES`, `ALLOWED_EXTENSIONS`, `MAX_FILE_SIZE`, `PHOTO_MIME_TYPES` (jpeg/png only), `PHOTO_EXTENSIONS` (jpg/jpeg/png only) |
| `documents.ts` | Unified horse/trainer/rider document CRUD, replacing the three former per-entity modules: `getDocumentsWithUrls(entity, entityId, barnId)`, `createDocument(entity, barnId, entityId, recordType, storagePath, fileName, fileSize, notes, reminderDate)`, `deleteDocument(entity, id, entityId, barnId)`, `updateDocumentReminderDate(entity, id, entityId, barnId, reminderDate)`, where `entity` is `'horse' \| 'rider' \| 'trainer'`. Each function has one TS overload per entity so callers get `HorseDocument[]`/`RiderDocument[]`/`TrainerDocument[]` back with no casting, with `getDocumentsWithUrls` returning `{ doc, signedUrl }[]` pairs (each document paired with a 300s signed URL via `document-storage.ts`'s `getSignedUrl`) — consolidating the fetch-then-sign motion the horse and member detail pages previously each hand-rolled — while the raw row query (`getDocuments`) is now module-private with no exported adapter. `entityId` is always filtered on delete (previously only the horse variant checked it; rider/trainer did not). `reminderDate` (`YYYY-MM-DD` or `null`) is set at upload and can be pushed forward later via `updateDocumentReminderDate` — no new RLS needed, since the existing uploader INSERT policies and manager `FOR ALL` policies aren't column-restricted. `getDueDocuments(barnId, today)` — no-cron reminder surfacing: queries all three document tables for `reminder_date <= today` (`today` passed in by the caller rather than computed internally, for testability) and returns a flat `DueDocument[]` sorted by `reminderDate` ascending. Horse-doc owner names resolve via `resolveHorseNames` (from `horses.ts`, passed this function's own client); trainer/rider-doc `ownerId` is simply `trainer_id`/`rider_id` as stored (a `barn_memberships.id` since #738), with the owner name resolved via a single `resolveMemberNames` call (`member-names.ts`) — `'Unknown Member'` is the fallback for any id it can't resolve. This dropped a "membership removed but profile persists" fallback path the prior auth-user-id-keyed version needed: `rider_documents`/`staff_documents` now `ON DELETE CASCADE` from `barn_memberships`, so a document can no longer outlive its owning membership. |
| `document-backup.ts` | Barn-wide documents-as-zip export (#995), companion to #994's spreadsheet export. `getAllBarnDocuments(barnId, client?)` — like `documents.ts`'s `getDueDocuments` but barn-scoped only (no `reminder_date` filter), returns `{ horse, trainer, rider }` raw rows from all three document tables. `buildBackupZipEntries(docs, horseNames, memberNames)` — pure function, no I/O: plans each document's path in the archive as `horse/<horse name>/<file>` or `member/<member name>/<file>` (trainer + rider documents share the `member/` namespace, keyed by the shared `barn_memberships.id` via `resolveMemberNames`, since a person's role can change but their membership id doesn't); filename is `<original base>-<record_type>-<created_at date>.<ext>`, deduped with a numeric suffix on collision within the same folder; folder names are sanitized (`/`/`\` stripped) since horse/member names are free text. `buildDocumentsBackupZip(barnId, client?)` — orchestrates `getAllBarnDocuments` + `resolveHorseNames`/`resolveMemberNames` + `buildBackupZipEntries`, downloads each document's bytes via `document-storage.ts`'s `downloadFile`, and assembles an in-memory `jszip` archive; returns `null` when the barn has zero documents (server-side enforcement of "nothing to download," independent of the UI's disabled button). |
| `backup.ts` | Barn-wide data spreadsheet export (#994), companion to #995's documents zip. `getBarnBackupData(barnId, timezone, client?)` fetches and joins every covered table into 8 flat, human-readable row shapes — **Horses**, **Lessons** (+ `lesson_horses`/`lesson_riders`/`lesson_tiers`/`lesson_series` folded in as columns rather than shipped as their own sheets — junction/lookup data is denormalized onto each lesson row: joined horse/rider/instructor names via `lesson-finance-queries.ts:getLessonJunctionRows` + `member-names.ts`/`horses.ts` resolvers, a `Recurring` flag from `series_id IS NOT NULL`, and `Collected`/`Instructor Payout` sourced from `lesson-finance-queries.ts:getLessonFeeRows` called with wide sentinel date bounds instead of a reporting window so it returns full history), **Agreements**, **Agreement Charges** (rider/horse name and `kind` resolved via the agreement each charge belongs to; `Collected`/`Payment Type` overlaid from `transactions.ts:getTransactionRows(barnId, CHARGE_TRANSACTION_KINDS)` keyed by `agreement_charge_id`, since `agreement_charges` itself has carried no `payment_type` column since #831 — same overlay idiom `agreement-finances.ts:getPaidCharges` already uses), **Horse Expenses** (reuses `expenses.ts:getExpensesByBarn` directly — already resolves horse names and carries its own `payment_type` column, no ledger lookup needed), **Members** (a fresh `barn_memberships` + `profiles` two-query join scoped to the barn, all statuses — `barn-memberships.ts`'s `joinMembershipsWithProfiles`/`MembershipRow`/`ProfileRow` are deliberately narrow, built only for name resolution, so this is its own query rather than a forced reuse), **Documents** (reuses `document-backup.ts:getAllBarnDocuments` directly, flattened to one sheet with an Owner Type Horse/Member column), and **All Transactions** (`transactions.ts:getTransactionRows(barnId, ALL_TRANSACTION_KINDS)`, all 6 kinds, no date filter — the barn's full money ledger in one place, kept *in addition to* the collected/payment-status columns folded into Lessons/Agreement Charges, not instead of them; `membershipId`/`horseId` resolved to names, and #1218 dropped the sheet's former `lessonId`/`lessonRiderId`/`agreementChargeId`/`expenseId` columns — raw UUIDs appearing nowhere else in the workbook, so they cross-referenced nothing a reader could act on. Amounts are written **signed as the ledger stores them**, i.e. `instructor_payout`/`expense` stay negative rather than going through `transactions.ts:positiveAmount` (#1218) — the column then sums to the barn's actual net, and any future negative-direction kind is covered without an enumerated list here). Every `TIMESTAMPTZ` column is rendered in the barn's own local time rather than left as raw UTC. This used to be a deliberate exception to the app's general "instants render viewer-local" convention; #1222 deleted that convention, so the export is now simply doing what every other surface does. Each such column is emitted as a real `Date` cell (#1218, so Excel can apply a date format instead of storing a bare string). The five sourced from a genuine `TIMESTAMPTZ` go through the private `barnLocalCell(instant, timezone)` = `new Date(instantToLocalWallClock(instant, timezone) + 'Z')`: **UTC-anchored barn wall clock**, not the real instant. Excel serials carry no timezone and `exceljs` derives one from `Date#getTime`, so anchoring the barn's wall-clock digits to UTC is what keeps the rendered value identical on every recipient's machine — handing over the true instant, or using `barn-timezone.ts:wallClockToInstant`, would make the rendered value depend on the recipient's own machine, which is exactly what this export avoids. Six sheets carry one such column, always positioned first (Horses `created_at`, Lessons `lesson_at`, Horse Expenses, Members `created_at`, Documents `created_at`, All Transactions `occurred_at`) and named by what the date *is*: the three `created_at` ones say **Date/Time Added** (Horses, Members, Documents), the three recording when the thing itself happened stay on plain **Date/Time** (Lessons, Horse Expenses, All Transactions). The remaining two sheets lead with the `DATE`-only column they already had (Agreements' **Start Date**, Agreement Charges' **Period**), so every sheet in the workbook opens with its own date. One of the six, Horse Expenses, collapses `appointments.expense_date` + `expense_time` into that one cell without calling `barnLocalCell` at all — those two columns are already barn-local digits rather than an instant, so `getExpensesSheet` anchors them to UTC inline and skips the conversion step, and carries a non-column `dateOnly` flag for a row whose `expense_time` is `NULL`, which the workbook builder uses to override just that cell's number format to date-only so a spontaneous appointment doesn't display a fabricated `12:00 AM`. Remaining `DATE`-only columns (`agreement_charges.period`, a document's `reminder_date`, an agreement's `start_date`) are written as-is, no conversion and no format. An unresolved name anywhere falls back to `'Unknown Horse'`/`'Unknown Member'` (a lesson with no instructor gets `'No Instructor'` instead, since that's a valid state, not a resolution failure). `buildBarnDataWorkbook(data)` — pure, no I/O: builds an `exceljs` `Workbook`, one worksheet per row group, via a small private `addSheet` helper that collapses the same repeated lines and (#1218) also applies each column's optional `numFmt` (`'mm/dd/yyyy hh:mm AM/PM'` for a date column, `'"$"#,##0.00'` for every dollar column) and auto-sizes each column to the widest of its header and its values, capped at 60 characters so one long Notes cell can't push the sheet off screen. A formatted value is measured at its *rendered* text rather than its `String()` form, in both directions: a `Date` at `DATETIME_FMT`'s width (`String(date)` is far longer than the digits Excel renders), and a money value at its `en-US` currency text (`String(45)` is *shorter* than the `$45.00` Excel renders, and a 3-character `Fee` header would otherwise leave the column narrow enough that Excel shows `####`). The fold is a `reduce`, not a `Math.max` spread, so the unbounded All Transactions sheet can't exceed the engine's argument limit and fail the export. `addSheet` also (#1218) sorts every sheet **descending by its own first column** via the private `sortByFirstColumn` — newest-first on all eight, driven off `columns[0].key` rather than a per-call-site sort key, so a sheet added later inherits the ordering by construction. Its comparator uses `<`/`>` rather than dispatching on type, which covers the `Date`-valued columns and the ISO `YYYY-MM-DD` string ones (Start Date, Period) in one path since lexical order is already chronological there, and subtracts the two boolean comparisons instead of branching on them so there's no unreachable third case. `addSheet` returns `{ sheet, rows }` rather than just the worksheet **because** of this sort: Horse Expenses' per-cell date-only format override indexes rows positionally, so it must walk the sorted array actually written to the sheet, not the caller's original one. The date format is deliberately **zero-padded** and its column **left-justified** (keyed off the column carrying `DATETIME_FMT`, since that is what makes a column a date column): every rendered date is then exactly 10 characters starting at the same x, which is what makes Horse Expenses legible where date-only and date+time rows interleave — under the unpadded `m/d/yyyy` the dates shifted row to row, and under Excel's right-aligned default for dates a date-only row lined its date up against a timed row's *time*. Finally `addSheet` styles row 1 — **bold at the default font size**, `height` 30 (2× exceljs's 15-point default), vertically centered — so the header reads as a header rather than as another data row. The size stays default on purpose: column widths are denominated in the default font's character width, so a larger header font renders wider than `cellWidth`'s budget and clips the longest headers (#1218 shipped 12pt and had to walk it back). `buildBarnDataBackupBuffer(barnId, timezone, client?)` — orchestrates `getBarnBackupData` + `buildBarnDataWorkbook` + `workbook.xlsx.writeBuffer()`, mirroring `document-backup.ts:buildDocumentsBackupZip`'s fetch → pure-build → orchestrate shape. Unlike the documents zip, there is no "nothing to export" `null` case — a barn always has at least one `barn_memberships` row (its own manager), so the Members sheet (and therefore the export as a whole) is never genuinely empty. |
| `types.ts` | Shared TypeScript types |
| `service-role.ts` | Service-role (RLS-bypassing, `auth.admin.*`-capable) Supabase client helpers — relocated here from `scripts/script-utils.ts` (#505; that file now re-exports them so no script's imports changed) since `/demo`'s `createOrResumeDemoBarn` action needs them at runtime on a public route, not just in dev/CI scripts, and app-runtime code is held to this repo's 100%-branch-coverage bar in a way `scripts/**` (test-exempt) never was. `createServiceClient(url, key)` — thin wrapper constructing a service-role `SupabaseClient` (no session persistence/auto-refresh); `mustSucceed(result, label)` — unwraps a `{data, error}` result or throws `"{label}: {message}"`; `findAuthUserIdsByEmails(emails, client)` — paginates `auth.admin.listUsers` (50/page) to resolve a list of emails to their auth user ids; `findOrCreateAuthUser(email, client)` (#505, new) — finds an existing auth user by email via the above, else calls `auth.admin.createUser`; used by `seed-barn.ts`'s fixed-email fixture identities (3 trainers, 3 riders, a 4th trainer, a pending rider, plus the demo action's own "Morgan Manager" persona) so they're a shared pool reused idempotently across every demo barn rather than erroring on a duplicate email the 2nd time `seedBarn()` runs; if `createUser` errors (e.g. a concurrent `/demo` request won the race to create the same fixture identity first), re-checks `findAuthUserIdsByEmails` once before giving up, so concurrent visitors racing on the same shared fixture email don't surface a raw creation error; `teardownBarnData(barnId, client)`/`teardownAllData(client)` — delete all of one barn's (or every barn's) data, including non-cascading storage cleanup (`horse_documents`/`staff_documents`/`rider_documents` files, horse/profile photos) and `lesson_tiers` (the one barn-scoped table whose `barn_id` FK isn't `ON DELETE CASCADE`) — everything else barn-scoped cascades automatically once the `barns` row itself is deleted, which callers do separately — `/demo`'s reap-oldest-on-cap step and `/api/cron/reset-demo`'s (#506) hourly reap loop each pair `teardownBarnData` with `deleteBarn`, while `scripts/reset-db.ts`'s full-teardown path calls `teardownAllData`, whose own final step already deletes every `barns` row, so it needs no separate `deleteBarn` call. |
