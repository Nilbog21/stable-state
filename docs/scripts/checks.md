# Scripts — CI gates and correctness checks

The per-script detail behind `scripts/CLAUDE.md`'s one-liner index (moved there from that file by #1354, so it loads only when followed). The conventions themselves — the `.sh`/`.ts`/`.test.ts` responsibility split, the `.test.sh` policy, the shell-hazards rule — stay in `scripts/CLAUDE.md`.

## `assert-dev-project`

Files: `.sh`

#1291. Shell-only, single caller: `/sync-migrations` step 8 runs `bash scripts/assert-dev-project.sh && npx supabase db push` so the push can't run alone. Closes the last gap in the `assertDevProject` coverage described on the `script-utils` row — `db push` is the repo's only *schema* write and was its only destructive path with no dev-project check, which matters more than the count suggests: an applied migration is never edited, so a wrong push is repaired by another migration rather than rolled back, and one `.env.local` is shared across every worktree. It checks **two** things, not one, because `db push` resolves its target differently from every other script here: the CLI writes to the project `npx supabase link` recorded in `supabase/.temp/project-ref`, while the seed/teardown scripts read `.env.local`. So it asserts `NEXT_PUBLIC_SUPABASE_URL` equals `DEV_SUPABASE_URL` (the same declaration `assertDevProject` verifies, keeping the scripts run alongside honest) *and* that the linked ref equals the ref inside `DEV_SUPABASE_URL` — the second is the one that actually guards the push, and dropping it would leave a re-link free to write schema anywhere with a dev-pointed `.env.local` in place. Each of the two mismatch aborts prints both values so the operator can see what was about to be written; the three fail-closed aborts that have no pair to print (`.env.local` absent, `DEV_SUPABASE_URL` unset, nothing linked) name the missing thing instead. `--allow-prod` (same shape and meaning as everywhere else) skips both checks but still prints the linked ref rather than passing silently. The linked-ref half is the check `replace-all-migrations.sh` has carried since #684, deliberately duplicated rather than extracted — four lines of grep/sed read cheaper twice than through a shared file, and that script's abort is on its own terms. Not folded into the TS `assertDevProject`: its seven callers push no schema and none of them should grow a `supabase/.temp` read. No `.test.sh` (see the policy above) — one was written and run once during #1291's own `/testIssue`, covering the two mismatch aborts, the three fail-closed aborts, the unparseable-URL abort and both `--allow-prod` behaviours against temp git repos, then deleted rather than carried in `ci.sh`: the script is nine straight `if` branches with no pure logic to extract, and every one of them is exercised the moment `/sync-migrations` step 8 runs

## `ci`

Files: `.sh`

Shell-only

## `check-coverage`

Files: `.sh`, `.test.sh`

Shell-only

## `check-pipefail-race`

Files: `.sh`, `.test.sh`

#1285. Shell-only, CI-only. Gate for the `pipefail` + early-exit-consumer race documented under "Shell hazards" above — it reads every `scripts/*.sh` that sets `pipefail` line by line and flags a pipe into `grep -q`/`grep -m`/`head` unless the line carries `# pipefail-safe:`. The test is deliberately **coarse**: a pipe segment whose command is `head`, or whose command is `grep` and whose text contains `-q`/`-m`/`--silent` anywhere (`--quiet` and `--max-count` need no alternative of their own — they contain `-q` and `-m`). It does **not** parse grep's flag grammar. An earlier cut did, walking flag by flag so it could tell whether a `-q`-looking token was a *flag* or an *argument*, and that shape cost two fail-open bugs before it was replaced. The lesson generalizes past this script: telling a flag from an argument means tokenizing, and word-splitting desynchronizes wherever a quoted value holds a space, so `grep -e "foo bar" -q` — a real race — read as safe. Not tokenizing costs the mirror cases instead: `grep -- -q`, `grep -e -q`, `grep -f -q` and `grep --regexp -q`, where the `-q` *is* the search pattern, now flag, and `# pipefail-safe:` is the one-line answer. That trade is the whole design — over-flagging is affordable on a gate nothing currently trips, missing a race is the thing it exists to prevent, and the annotation it demands *is* the polarity argument this issue asked for. It was measured before it was chosen: on this repo the coarse rule finds exactly what the grammar walk found, which is nothing, so the precision was buying verdicts on cases that don't occur here. No live site was ever affected either way — the sweep found none, and `parse_var`'s nine `grep -m1` copies are **producers**, not consumers (above), so no cut ever matched them. Pipes are found by splitting the line on `|` after masking `||`, so `cmd || grep -q …` doesn't register: a `||` fallback has no producer to take SIGPIPE. Only segments after the first are tested, which is what keeps a pipe opening a continuation line matching. The split isn't quote-aware, which is harmless in the common shape — `grep -qE "fix|feat"` still flags, the `-q` riding in the same segment as the `grep` — and is a documented miss in the rare inverse `grep -e "a|b" -q`, joining the older ceiling that a flag placed after the pattern (`grep needle -q`) is a miss. Not being quote-aware also makes a line-leading `|` inside a *quoted multi-line string* matchable, the same fixture-text ambiguity that disqualifies `*.test.sh` wholesale. `workflow-ci-wait.sh`'s jq filter carries several, safe today only because they continue with `verdict(` rather than `grep`/`head`. Ordinary scripts hold one or two such blocks rather than being made of them, so they stay in scope and `# pipefail-safe:` is the per-line answer if one ever collides. Written because the sweep that motivated it came back **clean**: a point-in-time verdict on a hazard nothing enforces is worth nothing the next time someone writes `| grep -q`, and this class fails *open* under half its polarities, so the wrong verdict arrives looking self-verified. Deliberately not scoped to shell in general — it reads only `scripts/*.sh`, since `.claude/commands/*.md` prose isn't executed and the repo's one other shell file (`.claude/hooks/test-on-save.sh`) sets no `pipefail`, which is the precondition the whole hazard needs. The `*.test.sh` exclusion is load-bearing rather than tidy: `check-pipefail-race.test.sh` writes hazardous fixture scripts as string literals, and those are indistinguishable from real pipelines to a line scanner. `.test.sh` (rather than manual verification, per the policy above) because there *is* pure logic here — the consumer test, the `pipefail` precondition, the annotation escape hatch and the file read — and `ci.sh` invokes it, which is the condition that policy attaches. That file read is `while IFS= read -r line || [ -n "$line" ]`, and the `||` half is load-bearing rather than defensive: `read` returns non-zero on a final line carrying no trailing newline, so testing its status alone drops that line — fail-open, and invisible to every fixture, since the harness writes them with `printf '%s\n'`. 31 assertions, which are the regression guard each rewrite was carried out against: the 24 predating the coarse cut held unchanged through it, and 4 of the other 7 are the mirror cases inverting from "must not flag" to "flags, annotate it"

## `check-e2e-tags`

Files: `.sh`, `.test.sh`

#1386. Shell-only, CI-only. Gate for the drift between a checklist `(e2e: <test name>)` tag and the test it names. `/runChecklist` Step 0.5 maps one green suite run onto every `(e2e:)` checkbox, and nothing verified either half of what that tag claims — so a rename in an unrelated PR launders an unverified checkbox into "green run — every `(e2e:)` checkbox passed" months later, at the one moment nobody is looking at that line. Same class `select-specs.sh --lint` catches for `covers:` globs, and written for the same reason: the 2026-08-06 measurement came back **clean** (713 unique tags across 733 occurrences, 722 static test titles, 0 orphans), and a point-in-time verdict on a convention nothing enforces is worth nothing the next time someone renames a test. It checks **four** things: **orphan** — the tag matches no `test('…')` title in `e2e/*.spec.ts` — **never-executes** — the tag resolves, but its test carries no Playwright project tag, so no project's `grep` collects it and it never runs — **wrong-role** (#1392) — the tag's test runs only as identities the phase is never walked by — and **malformed** — the tag doesn't parse at all, so none of the other three can be run on it. The second is the subtler silent-green path and had zero occurrences at filing; it exists because a test that never runs is indistinguishable from a passing one in a suite verdict. The fourth exists because the outer substring guard and the extraction regex disagree about what a tag is: a line reading `(e2e: some_test` with no closing paren, or `(e2e:some_test` with no space, looks like a tag to a reader and to the guard, but not to the regex — so an unguarded scanner drops it silently, which is this gate's own failure class arriving through a typo. Tags claimed by a line are counted before parsing and the counts compared after, which is what turns a dropped tag into a report rather than a `0`. Matching strips trailing ` @project` suffixes off the *title*, repeatedly (a title may carry several, `… @trainer @rider`); checklist tags carry none, verified 0 of 713, so the tag side needs no stripping and the "tag names a nonexistent project" worry doesn't exist there. Two titles can strip to the same key, and last-wins would make the verdict depend on **declaration order** — a `@manager`-tagged declaration masking a bare-titled sibling that never runs, or the reverse, from the same two tests. The collision is resolved toward whichever title *fails* the never-executes check: the tag cannot say which of the two it meant, and fail-closed is this gate's polarity throughout. The **wrong-role** check (#1392) enforces the phase-partitioning Convention `PRE_RELEASE_TEST_CHECKLIST.md` had only *asserted* since #1292 — *"a Phase 4 line asserting trainer-visible UI can only ever be covered by a `@trainer` test, so its `(e2e: …)` tag would be a lie"* — which #1386 left unenforced by accepting *any* configured project tag, so a `phase-6-rider.md` line tagged with a `@manager`-only test passed cleanly while its OK line read as though the header's rule were what got checked. A gate landing green while enforcing less than the prose beside it claims is worse than none, because the green becomes the evidence. Both halves of the mapping were already declared in-tree, which is why #1386's stated obstacle ("how a phase maps to a project needs its own decision") dissolved on re-reading rather than needing one. Phase → roles comes from each phase file's `<!-- Asserting role: <roles> — <free prose> -->` comment: only the head, before the **first** ` — `, is parsed, split on `, `, against exactly four tokens — `manager`/`trainer`/`rider`/`role-agnostic`, the last meaning unconstrained. The strict head is load-bearing, because prose-grepping the whole comment is fail-open **in this gate's own failure class**: phase 4's prose contains "trainer" and "rider", phase 5's contains "manager", and phase 1's sentence ends "…and as its manager", so a comment-wide grep resolves every phase to every role and reports the tree clean forever. **Phase 4's head is `manager` alone**, though its prose also says "role-agnostic" — there that phrase describes the *assertion's* nature rather than the identity walking the phase, and admitting it as a token would exempt 560 of the 733 tags, the largest file and the one where a stray non-manager tag is likeliest; `role-agnostic` survives as a token only for phase 1, which genuinely has no single walker. Role → projects comes from each project's **`storageState`**, not its name: `mobile` is a viewport project running `manager.json` and phase 4 legitimately carries one `@mobile` tag, so a phase-name-to-project-name rule false-positives on the real tree immediately. One matching role suffices — 6 tags name `@rider @trainer` tests from phase 5 and phase 6 alike. Everything fail-closes: a project whose `storageState` doesn't parse **aborts** (like the empty-projects case, and for the same reason one level in — a project silently mapped to no role would fail every tag naming it for a reason the config rather than the checklist owns); an unparseable head, an unknown token, **two** `Asserting role:` comments in one file (neither last-wins nor union is defensible), and a tagged phase file with no comment at all are each a FAIL. A comment-level failure reports **once per file** and skips that file's per-tag role checks, or a typo in phase 4's head would print 560 identical errors; orphan and never-executes still run there. A declaration that *exists* is parsed wherever it sits, tagged file or not — only the requirement that one exist is scoped to files carrying tags, so a typo in a phase file with no tags yet fails in the PR that wrote it rather than years later in the unrelated PR adding that phase's first tag. Measured at `94626d2e`: **0 violations across 733 tags** — phase 4 is 559 `@manager` plus 1 `@mobile`, phase 5 is 82 `@trainer` plus 3 dual, phase 6 is 85 `@rider` plus 3 dual, and phases 1/2/3/7 carry none — so it landed green as a pure drift gate. The projects are **parsed out of `playwright.config.ts`** (`grep: /@name/`, and its `storageState` on the same line) rather than restated, and the script **fail-closes if it parses none** — a run that resolved zero projects clears every tag on the never-executes check and prints the same `OK` as a clean tree, which is `select-specs.sh --lint`'s rule about a lint that read nothing. The **reverse direction is deliberately not linted**: a test claimed by no checklist line is legitimate (5 today, each with a recorded reason in #1393) and flagging it buys busywork, so an unclaimed test is not a defect. Sources tags from a `CHECKLIST_GLOBS` constant holding `checklists/pre-release/*.md` — a **glob rather than the seven filenames**, deliberately: a literal list is fail-open in this gate's own failure class, since a phase file added by a future split would be silently unlinted and say so nowhere. `PRE_RELEASE_TEST_CHECKLIST.md` is excluded because all 4 of its `(e2e:` hits are convention prose (`<test name>`, `…`), not data; the constant's comment marks where `POST_RELEASE_TEST_CHECKLIST.md` joins if POST is ever automated, which needs the same placeholder consideration first since POST is its own index. The **template-literal ceiling**: only `test('…')` is collected, so the 5 `` test(`…`) `` sites (4 in `smoke.spec.ts`, 1 in `behaviors.spec.ts`) are skipped rather than reported — they can't be statically resolved, and 4 would be permanent false positives on the never-executes check, their project tag being `@${role}` rather than a literal. The cost is that a checklist tag naming a *generated* title would read as an orphan; none does, and the generated names (`${role}_no_error_on_…`, `manager_finance_${param}_…`) aren't checklist-shaped. Reporting iterates **occurrences**, not unique tags, so each offending `file:line` prints and a tag shared by two checkboxes is checked at both — 23 tags are shared today, which is why a per-tag uniqueness rule would have failed the tree it was written against. `.test.sh` (rather than manual verification, per the policy in `scripts/CLAUDE.md`) because there is pure logic here — the four checks, the suffix strip, the collision rule, the template-literal skip, the config parse and the file read — and `ci.sh` invokes it, which is the condition that policy attaches. 24 assertions over temp git repos holding a phase file, a spec file and a `playwright.config.ts`; six of them are mirrors that guard the checks against over-broadening (a project-less test *nobody claims* must pass; a template-literal title beside a valid tag must pass; a `@mobile` tag under a manager phase, a dual-project tag under each of its two phases, and any project-tagged test under a `role-agnostic` phase must all pass). `make_repo`'s 4th parameter is the phase's declaration, defaulting to `role-agnostic` so the fixtures predating the role check keep their verdicts — spelled `${4-…}` rather than `${4:-…}`, since the empty string is itself a fixture (the phase file carrying no declaration at all). Every failing case asserts on the **message**, not only the exit status: a test that accepts any non-zero exit cannot tell the failure it names from an unrelated crash on the same fixture, so `runs_under_a_project` inverted, or a `set -u` trip, would leave it green. The file read is the same `while IFS= read -r line || [ -n "$line" ]` as `check-pipefail-race`, for the same fail-open reason recorded there

#1410 added a **fifth** check, and it is the only one that runs over file *text* rather than over tags — it rejects a comment citing a checklist item by **line number** instead of by quoted fragment. Not to be confused with the deliberately-unlinted reverse direction above: this never reads a test title. The convention is #1366's, which swept ~304 such citations out of `e2e/*.spec.ts` after finding `checklist-phase4-calendar-feed.spec.ts` citing `607-615` for a section that started at 615, unnoticed for a release — a line number cited from a spec rots the moment anything is inserted above it in the phase file, and rots *silently*, because the citation still reads as correct. The rule lives here rather than in `e2e/CLAUDE.md`'s spec-maintenance section for two reasons: that file was **76 characters** under its `check-doc-size.sh` budget at the time (13,924 of 14,000) against the ~200 the rule needs. That half has since **expired**: #1420 split the framework facts out to `docs/e2e-framework-facts.md` and #1433 the spec-maintenance rules out to `docs/e2e-spec-maintenance.md`, and while both **lowered** the budget rather than banking the slack, the file came out 319 characters under at the time (5,831 of 6,150, after #1468's one-line-per-entry cap) — more than the rule needs, so space is no longer the reason. What keeps the rule here is the other reason, which was always the decisive one and is budget-independent: the convention regressed **twice while the sweep establishing it was still in flight** (#1391 reintroduced one, #1395 added more), which prose structurally cannot prevent, since neither PR could read a rule being written in a PR open concurrently with it. Same polarity as `check-pipefail-race` and `check-e2e-tags` themselves: all three were written *because a sweep came back clean*, a point-in-time verdict on an unenforced convention being worth nothing the next time someone writes the old form. Three dialects, counted off #1366's merge (`2554b976^1..2554b976`, removed lines in `e2e/*.spec.ts`): **`line N`/`lines N`** (278 lines, 292 occurrences), **`P<digit> N`** — `P5 866` (16), and a number **directly after the phase filename** with no keyword at all, `…/phase-4-manager-verification.md 149-159` (10). The third is the one that survived longest precisely because no keyword draws the eye, and it escapes the ceiling below only because the filename anchors it (`\.md,? +[0-9]`). **Documented ceiling:** the bare-number dialect (`#938's 990`) is not statically distinguishable from ordinary prose and is deliberately out of scope. All three are banned **outright** — **case-insensitively**, anywhere in **any `e2e/**/*.ts` file**, on any line rather than only in a comment, with no requirement that a checklist file be named nearby — and that is a measured decision rather than a lazy one: pre-sweep, only **13 of 135** `line N` hits named a checklist file on their own line, the rest reading `line 523's test` or `lines 858/862/863` with the file named once at the top of the comment block, so a context-scoped check would have caught about a tenth of the class. What makes the unconditional ban safe is that source lines are cited **colon-style** here (`ExhaustionBar.tsx:57`, and spec files the same way), which is lexically distinct, leaving `line N` with no legitimate use in a spec comment. The cost, stated plainly: a comment wanting to cite a *source* line in prose form must use the colon form instead. Each of the three widenings answers a way the first cut was narrower than the prose describing it, which is the failure this gate exists to catch turned on itself: **52 of those 278** lines read `Line 696's …` or `Lines 702-712 …`, so a case-sensitive pattern readmitted a fifth of the commonest dialect through a shift key; a scan scoped to `*.spec.ts` left `e2e/support/*.ts` and `global-setup.ts` permanently unlinted, the same fail-open shape `CHECKLIST_GLOBS` avoids by being a glob; and comment-only matching would have bought a parser plus a way to smuggle a citation past inside a string literal, against 0 near-misses tree-wide in ordinary test code. Each widening carries its own assertion (a capitalized `Lines N-M`, a citation in `e2e/support/helper.ts`, one outside any comment), so the narrower reading fails rather than being re-argued. Three mirror assertions pin the false-positive classes — a phase file named with **no** trailing number must pass (that is the whole point of the sweep, naming the file stays legal), a colon-form source citation must pass, and a **clock time** must pass, `12:00` being everywhere in the specs by the 12-hour-display UI convention and the false positive a naive `:NNN` pattern would produce. The two halves keep **separate counters** feeding one exit status, and separate failure trailers: folding the citation count into the tag counter would work today and mis-attribute the failure in the trailer, which is the only place a reader learns what to fix. The `OK:` line names the citation check too — a gate landing green while its summary describes less than it enforced is worse than no gate, which is the defect #1392 found in this script's previous OK line. Landed **green** on the current tree (0 survivors of all three dialects across all 62 `e2e/**/*.ts` files, specs and helpers alike), and verified to bite by running it against the pre-sweep tree: **304 violations across 31 files**, matching the dialect counts exactly

## `check-function-grants`

Files: `.sh`, `.test.sh`

#1535. Shell-only, CI-only. Gate for the function-`EXECUTE` invariant stated in [`docs/architecture/rls.md`](../architecture/rls.md): a `REVOKE … ON FUNCTION <name> … FROM PUBLIC` must exist **at or after** that function's last plain `CREATE FUNCTION` or `DROP FUNCTION`, in filename-then-line order across `supabase/migrations/*.sql`. Without it, Postgres' default `EXECUTE` for `PUBLIC` survives and PostgREST serves the function to an anon-key caller. Written for the same reason as `check-pipefail-race` and `check-e2e-tags`: the sweep that motivated it produced a point-in-time verdict, and this class has already reopened once — #972's squash re-listed ~30 REVOKE/GRANT pairs and dropped `set_instructor_cut`'s, which #1158 restored, and nothing stopped the next squash doing it again.

Two design points carry the whole gate. **Ordering, not mere existence:** a bare "a REVOKE exists somewhere" check passes `20260731081403_appointment_functions.sql`, which `DROP`s and recreates `create_expense_with_horses` — discarding its ACL — and re-grants only because someone remembered; that file is the live proof the weaker rule is theatre. **`CREATE OR REPLACE` does not restart the clock:** it preserves the existing ACL, so counting it as a reset would false-fail every body-only amendment in the set — `20260807132021_update_horse_notes_admits_manager.sql`'s header says exactly this — and, worse, would train the reader to add redundant REVOKEs to silence it. A plain `CREATE` errors on an existing function, so it only ever follows a `DROP` or nothing, which is why "plain `CREATE` or `DROP`" is the complete reset set. An `OR REPLACE` still *registers* the name, so a function whose only statement is a `CREATE OR REPLACE` and which was never revoked anywhere still fails rather than being laundered by the exemption.

`RETURNS trigger` functions are exempt — not PostgREST-reachable, and Postgres checks `EXECUTE` on a trigger function at `CREATE TRIGGER` time rather than at fire time. Two qualify (`enforce_lesson_participant_counts`, `set_updated_at`), and both spellings are handled: the baseline puts `RETURNS trigger` on the `CREATE` line, later migrations give it its own, so the scan carries the name forward from the `CREATE` until the `LANGUAGE` clause that always closes a signature. Because `[[ =~ ]]` overwrites `BASH_REMATCH`, the name and the `OR REPLACE` group are captured into locals on the matching line before any further match runs — reading them afterward silently reads the *trigger* regex's groups instead, which is a `set -u` trip in the lucky case and a wrong verdict in the unlucky one.

Tracking is by function **name**, not signature. Signature parsing buys nothing here: an overload set shares one ACL story in this repo, and a REVOKE naming any arity of a name is one someone wrote deliberately. The documented cost is that a genuinely divergent overload pair would read as guarded; there are none, and `Verify Migrations` replays the SQL either way, so a mis-typed signature in a REVOKE fails there rather than here. Name matching is anchored on both ends (`([a-zA-Z_][a-zA-Z0-9_]*)` followed by the argument list's `(`), because prefix matching is where this fails **open** — a `REVOKE` on `do_thing_else` must not clear `do_thing`.

Deliberately a **static parse** rather than a live `pg_proc.proacl` assertion. The property that broke is the *replay* one — what a from-scratch migration run produces — and an already-migrated dev/prod hides exactly that failure, since `CREATE OR REPLACE` preserves an ACL the migration set no longer reproduces. A static check also needs no database in CI. The half a static check **cannot** do is decide which roles a function's callers actually hold: tracing DAL → RPC → service-role-script reachability in shell would be brittle, so `reset-db` and a `generate-recurring-lessons` run against dev are the real check on the `service_role` side, and `docs/architecture/rls.md` records why a blanket "grant `authenticated`" is the known way to break it.

Only text Postgres would read as **DDL** is matched, and every keyword match is case-insensitive (`shopt -s nocasematch`, with names folded at capture so a mixed-case `CREATE` and a lowercase `REVOKE` share a key). Neither is polish — each closes a shape where the gate reports OK on a function it never guarded, and each was found by review rather than by design, after the gate had already been called done once. Lowercase DDL doesn't merely go unguarded, it never enters the set, so the reporting loop has nothing to say about it. And a line that *looks* like DDL to a line-anchored regex is DDL to this gate unless something excludes it: a `RETURNS trigger` or a whole `REVOKE … FROM PUBLIC` sitting inside a `RAISE NOTICE` string, or inside a `/* … */` note about a revoke someone meant to add. `/* */` is live in three migrations, this issue's own included, so that one is a shape in the tree rather than a hypothetical. `strip_body` therefore removes dollar-quoted bodies, `/* */` block comments, `--` line comments and string literals, carrying its state across lines because bodies, block comments and strings can each span them — a `--` comment ends at the newline and so needs no carried state, which is why its branch clears the rest of the line without setting one. It is a **scanner and not a `$$`-parity toggle** for a reason worth stating: parity has no notion of whether an occurrence is a delimiter, so a single `$$` inside a comment or a string literal — `-- the $$ delimiter below`, `IS 'the price is $$5'` — opens a body that never closes, and *every* `CREATE` in the rest of that file is stripped to nothing and silently dropped from the check. That is one bad line disabling the gate for a whole migration, which is why the scan **fail-closes** on reaching end of file still inside a body, comment or string: it lost track, so its verdicts for that file are worthless and it says so instead of reporting OK. The documented ceiling is that block comments don't nest, though Postgres nests them.

The two quoting rules the scanner has to get exactly right, both found by review after the scanner itself was called done, and both in the same handful of lines. **`\` escapes only inside `E'…'`** (`standard_conforming_strings` is on, so a bare `'…'` has no backslash escape at all), and there an *odd* run of backslashes escapes the quote while an even run is escaped backslashes whose following quote still closes. Reading `\'` as a terminator puts the scan one quote out of phase, and out of phase is worse than it sounds: the literal's real closing quote is then taken as an *opening* one and the rest of the file — a whole `CREATE FUNCTION` included — becomes string content, so the function is invisible rather than merely unguarded and the reporting loop says nothing about it. The EOF valve does not reliably catch this, because any later stray quote resyncs the phase; a `--` reached in code state does it too, by clearing its line without setting state. Whether a literal is an E-string is decided from the tail of the **raw** text between the previous token and the quote, not from the tail of the stripped code accumulated so far. Postgres requires the `E` to be *character-adjacent* to the quote, and the two differ exactly where something was stripped in between: `e/* c */'…'` is a generic `typename 'literal'` cast — legal, given a type actually named `e`, and comments are whitespace to the lexer — but the accumulator has the comment removed by then and still ends in `e`, so the literal is read as escape-bearing and the scan drifts. That one is fail-**closed** too, and in its nastiest form: a migration `psql -f` applies cleanly draws a false `FAIL … reached end of file`, and — where the drift starts after the `CREATE FUNCTION` and swallows the `REVOKE` that follows, as in the assertion covering it — a second false `FAIL` accusing a properly-revoked function of having none. **Dollar-quote tags are case-sensitive**, which is the one comparison `shopt -s nocasematch` must not reach: with it, a body opened `$Body$` closes on the text `$body$` inside itself. That one fails **closed** rather than open — the drift ends the file inside a string and a valid migration draws a false `FAIL … reached end of file`, blocking CI — but it is the same class of defect and the same fix site. `nocasematch` governs `case` and `[[` but not parameter expansion, so the tag compare is a `${rest#*"$tag"}` where its three sibling branches are `case`, with no `shopt` toggling; `$tag` is never empty, so a match always shortens the string and an unchanged result reliably means "not found". The scanner's `''` branch is, by contrast, **behaviour-preserving to delete** — the second quote of the pair sits at position 0, so the code-state scan would re-enter string state with identical `rest` and `code` — and a mutation run confirms no assertion kills its removal. It is kept regardless, so that an E-string's escape flag survives a `''` structurally rather than by that coincidence.

27 assertions over temp git repos holding fixture migrations; `check-function-grants.test.sh`'s per-test comments are the per-assertion record, so what follows is the rationale for the set rather than an inventory of it. Beyond the shapes the issue asked for (passing, missing-`REVOKE`, `DROP`-after-`REVOKE`), mirrors guard each exemption against widening — a re-revoked `DROP`+`CREATE` must pass, so the ordering case fails for the ordering and not merely for containing a `DROP`; a `CREATE OR REPLACE` after a `REVOKE` must pass, while one never revoked at all must fail; both `RETURNS trigger` spellings must pass. `DROP FUNCTION IF EXISTS` has its own assertion because that is the spelling `20260731081403` uses, and missing it would read that file's recreations as clock-untouched — fail-open on the one file that motivated the ordering rule. `REVOKE EXECUTE` and an unqualified (`public.`-less) name are covered as well, both being live spellings in the set (`20260729032550` and `20260716005944`), as is a `REVOKE` on a name the function under test is a prefix of. The fail-open shapes above each get one, every one of them added because a review round reproduced it against a fixture repo rather than because it was foreseen: `returns trigger` and a whole `REVOKE` inside a body, lowercase DDL, `$$` inside a `--` comment and inside a string literal, a `REVOKE` inside a `/* … */` block, an escaped quote in an `E'…'` literal, and the unterminated-at-EOF valve. Several of those carry a **mirror** rather than standing alone, since the fix for each has a fail-**closed** degenerate form that the fail-open assertion alone would accept: a lowercase `REVOKE` must still clear a lowercase `CREATE` (or "lowercase always fails" passes), a real `REVOKE` after a closed block comment must still count (or "never leave comment state" passes), an `E'…'` whose closing quote follows an escaped *backslash* must still close (or "any backslash escapes" passes test 21 while never closing an E-string again), and the EOF assertion greps the valve's own message, because its fixture happens to fail for an unrelated reason too and a bare non-zero exit reads as a working valve on a script that has none. The fail-**closed** shapes are covered too — a `$Body$` body must not close on the text `$body$` inside it, and `e/* c */'…'` must not be read as an E-string, both of which turn a valid migration into a false `FAIL … reached end of file` — as is quoting behaviour in its own right: a doubled `''`, which has no killable mutant of its own (see above), and a string spanning a line break, which does have one. That second one kills on the function name rather than the exit status, since a scanner that drops the carried state trips the EOF valve on its way to the wrong verdict. Every failing case asserts on the **message**, not only the exit status, so an unrelated crash on the same fixture can't be mistaken for the failure it names. One assertion runs the gate against the **repo's own migration set**, which is what makes the "passes once #1535's migration is in place" acceptance criterion a standing regression test rather than a one-time observation. The file read is the same `while IFS= read -r line || [ -n "$line" ]` as `check-pipefail-race`, for the same fail-open reason recorded there. Verified to bite by running it before the migration landed: **exactly the nine functions the issue's hand sweep found**, from an independent implementation of the rule — which is the evidence the gate is calibrated to the real tree rather than to its own fixtures.

## `check-ceremony-tags`

Files: `.sh`, `.test.sh`

#1542. Shell-only, CI-only. Gate for the `(auto)`/`(prompt)`/`(manual)` tags on `RELEASE_CEREMONY.md`'s checkboxes. `/releaseCeremony` reads those tags to decide what it may run unattended, confirm first, or hand back to the user, so an untagged checkbox has no defined behaviour — the skill refuses to start on one. Without the gate that refusal arrives weeks into a release ceremony, at the one moment nobody wants to stop and re-litigate a tag; with it, the PR that added the line fails instead. Same polarity and same reason as `check-e2e-tags` and `check-function-grants`: a convention that only prose enforces is worth nothing the next time someone adds a line.

**Fail-closed on all four shapes.** No tag, two tags, an unrecognised tag, and a file whose parse collected zero checkboxes are each a FAIL. The last is `select-specs.sh --lint`'s rule about a lint that read nothing: a parse collecting nothing clears every check and prints the same `OK:` as a fully tagged file, so it has to say so instead. A missing `RELEASE_CEREMONY.md` likewise fails rather than reading empty or tripping `set -u`.

Two parsing decisions carry the check. **The tag is the checkbox's opener**, the first token after `- [ ] ` — not a line-wide count. The runbook's own Automation tags blockquote names all three tags in prose, and several steps explain in their checkbox text why a *neighbouring* box is tagged differently; a line-wide count fails those immediately, so "exactly one tag" is enforced against the opener and the token directly behind it. **Checkboxes under an `Acceptance criteria to paste into that issue:` lead-in are exempt** — steps 1, 4, 5 and 6 each end in a block of text destined for a GitHub issue body, which are not ceremony steps and would be meaningless to tag. The exemption ends at the next `### ` heading **or** a `---` divider. All four of today's blocks are closed by a `### `; the divider arm is for the shape that has none after it — an acceptance-criteria block ending a part, where the next heading is Part 2's `## ` behind a `---`. A `###`-only rule would carry that exemption across the part boundary and silently unlint the whole of Closeout, which is this gate's own failure class, so it has its own assertion against a fixture of that shape rather than against the current file.

The box is matched as one of the three literal spellings (`- [ ] `, `- [x] `, `- [X] `) rather than by stripping a `- [?] ` prefix. `[?]` is a bracket expression matching a literal `?`, not a single-character wildcard, so that strip silently no-ops and *every* checkbox reads as untagged — a fail-closed bug rather than a fail-open one, but it took a fixture run to see, and the literal spellings can't drift that way. Ticked boxes are checked too: the runbook ships unticked, and a stray `- [x]` must not be a way past the gate.

`.test.sh` (rather than manual verification, per the policy in `scripts/CLAUDE.md`) because there is pure logic here — the tag vocabulary, the exactly-one rule, the acceptance-criteria exemption and its two terminators, and the file read — and `ci.sh` invokes it, which is the condition that policy attaches. 12 assertions over temp git repos holding a small fixture runbook. Every failing case asserts on the reported **`file:line`**, not only the exit status, so an unrelated crash on the same fixture can't be mistaken for the failure it names. Three are mirrors guarding the checks against over-broadening: all three tags must be accepted, a tag word appearing later in a checkbox's prose must not read as a second tag, and an untagged checkbox *inside* an acceptance-criteria block must pass. One assertion runs the gate against the repo's own `RELEASE_CEREMONY.md`, which is what makes #1542's "every checkbox carries exactly one tag" acceptance criterion a standing regression test rather than a one-time observation — it was the last of the twelve to go green, and only did so once the file was tagged. The file read is the same `while IFS= read -r line || [ -n "$line" ]` as `check-pipefail-race`, for the same fail-open reason recorded there.

## `verify-migration-equivalence`

Files: `.sh`

#1542. Shell-only, **not** in `ci.sh`. Proves two migration sets produce the same schema — the check `RELEASE_CEREMONY.md` Wrapup step 4 requires of a squash, and the one thing `Verify Migrations` CI does not do. That workflow is two lines, `supabase start && supabase db reset`: it proves the set **replays**, and a squash that silently drops a REVOKE, a default or a policy replays perfectly.

Before this, the equivalence proof was hand-rolled at each of the three squashes and survived only as prose in `supabase/migrations_archive/README.md`, where it had already drifted — #657 used plain `migra`, #658 and #972 `migra --with-privileges`. The word `migra` appears nowhere else in the repo: no script, no pinned flags, no recorded invocation.

**Interface:** `--before-ref <git-ref>` takes `supabase/migrations/` as of that ref; `--after-ref` defaults to the working tree. Refs rather than directories, because at squash time the "before" files have already been moved to `migrations_archive/`, and a ref is what the two criteria ("replay the release's current migrations", "replay the consolidated set") actually name. `--keep` leaves the databases for inspection.

**`migra` is deliberately not used.** `pg_dump --schema-only` + `diff` needs no Python install this repo has nowhere else, and is *stricter*: it compares rendered schema text including every ACL, so a dropped GRANT/REVOKE is a diff line rather than something a `--with-privileges` flag has to be remembered for. The documented cost of being stricter is column **order** — a squash flattening `CREATE` + `ALTER ADD` into one `CREATE`, where the original had since dropped a column, is a real difference to `pg_dump` and invisible to `migra`. The failure output names that case so it isn't mistaken for a regression.

**Replay runs with `role=postgres`, and that is load-bearing.** The set carries `ALTER DEFAULT PRIVILEGES FOR ROLE postgres IN SCHEMA public`, which grants `authenticated`/`service_role` on every table created afterwards. Replay as anyone else and those defaults never fire, so the older set comes out missing grants the consolidated set writes explicitly — measured at **75 diff lines of pure artifact** on the #657 pair before the role was pinned, which is exactly the noise that gets a verification tool ignored. The script fail-closes if the role is absent rather than emitting it. Peer auth generally blocks connecting *as* `postgres`, so it is set per-connection via `PGOPTIONS`, and unset for teardown, since `DROP DATABASE` under an assumed role is not universally permitted.

Normalisation is exactly one rule — strip PG18's random `\restrict`/`\unrestrict` nonce lines. Nothing else is filtered, so nothing real can hide in the filter. `--no-owner` is passed because ownership is environmental (whoever ran the script) while ACLs are the point. A minimal `auth`/`storage` prelude stands in for what Supabase provides and the migrations reference (`auth.uid` 162×, `storage.foldername` 88×, `storage.objects` 41×, `auth.users` 11×, `auth.email` 2×, `storage.buckets` 1×); it is identical in both databases so it cancels in the diff, and its job is to let the SQL parse, not to model Supabase. The three PostgREST roles are cluster-global and may be in use by other databases, so they are created if absent and never dropped. No `CREATE EXTENSION` is needed — the set's only external function is `gen_random_uuid()`, core since PG13.

**`--self-check` runs both polarities against real history, not a fabricated fixture.** #657's squash supplies both: `97855071^` (93 pre-baseline files) vs `bf620567` (the 3 consolidated ones) must be **identical**, re-deriving the verdict the archive README records; and `97855071^` vs `97855071` — the squash *as first pushed* — must **fail**, reporting 11 removed ACL lines. The negative half is what makes the positive half mean anything: a script that reported "identical" unconditionally passes the positive check alone. That negative case is not hypothetical — `bf620567` is titled *"[#657] Fix review findings: repair script bug, **missing grants**, stale README claim"*, so a human reviewer caught those 11 GRANTs across 6 tables (`horse_documents`, `lesson_tiers`, `notifications`, `rider_documents`, `seeded_accounts`, `trainer_documents`) and this reproduces the catch mechanically. It is the evidence the tool is calibrated against the real tree rather than its own fixtures, and it found the defect independently before the commit title was read.

The assertion reads the `ACL changes: N removed, M added` summary rather than counting `-GRANT` lines in the printed diff, because that body is truncated at 80 lines and under-reports whenever drift is large — which is when the check matters. That summary is printed **before** the truncated diff for the same reason a reader needs it: a dropped GRANT reads as ordinary context in a long diff.

**No `.test.sh`, deliberately.** The policy in `scripts/CLAUDE.md` attaches on being wired into `ci.sh`, and this cannot be: CI has no PostgreSQL server, and a check that skips in CI reports the same green as one that ran — this gate's own failure class. `--self-check` is the runnable check instead, and it lives with the script so it cannot rot separately from it.

## Shell-hazards sweep history

Swept 2026-08-04 (#1285): clean. Four things look like instances and are not, so they don't need re-deriving —

- The `echo "$x" | grep -q` sites in `check-coverage.test.sh` / `check-doc-size.test.sh` / `select-specs.test.sh` — none of those files sets `pipefail`, and a builtin `echo` of a sub-64KB string can't block. Safe twice over.
- `parse_var`'s `grep -m1 "^$1=" .env.local | cut | sed` (nine copies) — `grep -m1` is the **producer** here, so it can't take SIGPIPE. Its real `pipefail` interaction is exit 1 on a missing var, which every call site already absorbs with `|| true`.
- `workflow-context.sh`'s `… | sort | tail -1` and `workflow-ci-wait.sh`'s `awk | sort -u | paste` — `tail`/`sort`/`paste` all drain their input.
- `replace-all-migrations.sh`'s `{ grep -m1 … || true; } | cut` — producer again, and already guarded.
